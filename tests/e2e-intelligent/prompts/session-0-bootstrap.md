# Bootstrap Session

Follow the protocol in `tests/e2e-intelligent/bootstrap/setup.md` to install Playwright MCP and prepare the environment. This is a one-time session.

After completion, end this session and start a fresh one before running persona sessions — MCP tools are not available in the session that installed them.
