{
  "_README": "READ. Every key here was surfaced on 2026-09-04 by widening this lint's walk to the whole server AND teaching it the inline route-handler shape, and every key has since been opened and ruled on individually — the reason and the evidence are in _TRIAGED. Until that day the route layer was not merely unscanned but UNSCANNABLE: a route file's units are anonymous `async (req, res) => {}` callbacks, which neither extractor could see, so every query in every handler was attributed to the enclosing registerRoutes() — a 1,400-line unit that mentions organizationId, so rule 1 could never fire on any of them. Adding extractRouteHandlers took the visible unit count from 0 route handlers to 2,672 and the offender count from 57 to 210.",
  "_WHY_A_SEPARATE_FILE": "These are NOT the same kind of entry as the registers inside check-org-scoped-fetch.mjs. Those are hand-verified exemptions that the gate's own down-only ratchets bound. These are DEBT: pre-existing findings that became visible when the population grew, held apart so the burn-down is countable and so growing the population never silently raises a security ratchet. They now carry reasons too, but the distinction still matters — an entry here is 'read and judged not to be a defect', not 'approved as an exemption', and the right end state for most of them is a scoped query rather than a permanent line in a file.",
  "_THE_OBLIGATION": "This file may only SHRINK. It is a debt register, not an exemption list, and only the 8 keys listed under _TRIAGED have been read. A debt register whose entries nobody has opened is not a smaller problem than an unscanned directory; it is the same problem with a number attached. Triage moves an entry either into a real fix or into the hand-verified register inside check-org-scoped-fetch.mjs with a written reason. A sample of ~57 of these was triaged before this landed and produced two REAL cross-tenant defects out of the sample (routes-doc-system.ts's seller/buyer reads and POST /api/investor-profiles' tenant-assignment injection, both fixed in 779bc251), so the expectation is that more remain in here.",
  "_WHAT_IT_DOES_NOT_EXCUSE": "A NEW offender is not covered by this file and fails the gate immediately. That is the point of landing the widening before the burn-down rather than after: eight live cross-tenant paths were found by hand in files this lint had never opened, and every day it stayed blind was another day a ninth could be written without anything noticing. Reading the backlog does not turn it into permission.",
  "counts": {
    "rule1": 147,
    "rule2": 85,
    "rule3": 68,
    "total": 300
  },
  "rule1": {
    "method": [
      "server/services/adaptiveStrategyV13.ts::deactivateStrategy",
      "server/services/adaptiveStrategyV13.ts::getStrategyLineage",
      "server/services/autonomyScoreV14.ts::resolveDependencyEvent",
      "server/services/cognitiveMemoryV13.ts::clearExpiredWorkingMemory",
      "server/services/cognitiveMemoryV13.ts::consolidateMemories",
      "server/services/cognitiveMemoryV13.ts::getMemoryStats",
      "server/services/cognitiveMemoryV13.ts::getWorkingMemory",
      "server/services/cognitiveMemoryV13.ts::recallEpisodes",
      "server/services/collaborationProtocolV13.ts::addMessage",
      "server/services/collaborationProtocolV13.ts::checkConsensus",
      "server/services/collaborationProtocolV13.ts::getAgentReputation",
      "server/services/collaborationProtocolV13.ts::resolveDialogue",
      "server/services/collaborationProtocolV13.ts::updateDelegation",
      "server/services/confidenceCascadeV14.ts::getResolutionDetails",
      "server/services/eventMeshV12.ts::acknowledge",
      "server/services/eventMeshV12.ts::deadLetter",
      "server/services/eventMeshV12.ts::getDeadLetterEvents",
      "server/services/eventMeshV12.ts::getEventsByChannel",
      "server/services/eventMeshV12.ts::getEventsByType",
      "server/services/eventMeshV12.ts::getRecentEvents",
      "server/services/eventMeshV12.ts::getStats",
      "server/services/eventMeshV12.ts::getUnprocessedEvents",
      "server/services/eventMeshV12.ts::replayEvents",
      "server/services/feedbackLoopV14.ts::getOverrideDetails",
      "server/services/feedbackLoopV14.ts::reinforceLearning",
      "server/services/feedbackLoopV14.ts::retractLearning",
      "server/services/governanceBrainV13.ts::getExplanation",
      "server/services/integrationFrameworkV12.ts::consumeRateLimit",
      "server/services/integrationFrameworkV12.ts::getCircuitBreakerStatus",
      "server/services/integrationFrameworkV12.ts::getCostReport",
      "server/services/integrationFrameworkV12.ts::getCredentials",
      "server/services/integrationFrameworkV12.ts::getStats",
      "server/services/integrationFrameworkV12.ts::incrementCircuitBreaker",
      "server/services/matchmaking.ts::calculateSimilarity",
      "server/services/outcomeVerificationV12.ts::getAgentVerificationStats",
      "server/services/outcomeVerificationV12.ts::getAllVerificationStats",
      "server/services/outcomeVerificationV12.ts::getDiscrepancies",
      "server/services/outcomeVerificationV12.ts::getPendingVerifications",
      "server/services/outcomeVerificationV12.ts::getRecent",
      "server/services/outcomeVerificationV12.ts::processVerifications",
      "server/services/outcomeVerificationV12.ts::recordVerification",
      "server/services/selfHealingMeshV13.ts::detectAnomalies",
      "server/services/selfHealingMeshV13.ts::evaluatePlaybooks",
      "server/services/selfHealingMeshV13.ts::getHealthDashboard",
      "server/services/selfHealingMeshV13.ts::getStats",
      "server/services/selfHealingMeshV13.ts::performRootCauseAnalysis",
      "server/services/selfHealingMeshV13.ts::resolveAnomaly",
      "server/storage/growthConfigRepo.ts::countUnreadBorrowerMessages",
      "server/storage/growthConfigRepo.ts::getBorrowerMessages",
      "server/storage/growthConfigRepo.ts::markBorrowerMessagesRead"
    ],
    "function": [
      "server/ai/executive.ts::compactConversationIfNeeded",
      "server/ai/executive.ts::getConversation",
      "server/ai/executive.ts::updateConversation",
      "server/jobs/autonomousHealthMonitor.ts::checkAICosts",
      "server/jobs/autonomousHealthMonitor.ts::createAlert",
      "server/jobs/decisionExecutorTick.ts::sumExecutorAiSpendUsdSince",
      "server/jobs/embeddingRefresh.ts::refreshStaleEmbeddings",
      "server/jobs/featureEngineeringJob.ts::processFeatureEngineeringJob",
      "server/jobs/featureEngineeringJob.ts::storeFeatures",
      "server/jobs/founderChatBackgroundTaskRunner.ts::postFollowUpMessage",
      "server/jobs/founderChatBackgroundTaskRunner.ts::providerSavingsAnalysisRunner",
      "server/jobs/landCreditScoreRecalculation.ts::findStaleProperties",
      "server/jobs/morningBrief.ts::collectBriefData",
      "server/jobs/morningBrief.ts::findDefaultFounderThread",
      "server/jobs/morningBrief.ts::runMorningBriefForFounder",
      "server/jobs/runScheduledJobs.ts::processOutcomeAnalyzerJob",
      "server/jobs/runScheduledJobs.ts::processTelemetryOptimizerJob",
      "server/routes-finance-ledger.ts::computeTrailing30dMrrCents",
      "server/routes-finance-ledger.ts::rangeAgg",
      "server/routes-founder-cockpit.ts::fetchCurrentLetter",
      "server/routes-founder-pulse.ts::loadReservesLive",
      "server/routes-title-partners.ts::authenticatePartnerByApiKey",
      "server/services/autonomousDecisionExecutor.ts::buildAlertContext",
      "server/services/autonomousDecisionExecutor.ts::buildFeatureRequestContext",
      "server/services/ceoReminders.ts::getContextForReminder",
      "server/services/founderBriefing.ts::getTopActions",
      "server/services/outcomeVerificationV12.ts::verifyApiResponse",
      "server/services/scpGoldenSuite.ts::getAllGoldenCases",
      "server/services/scpGoldenSuite.ts::getGoldenCases"
    ],
    "route": [
      "server/routes-activation.ts::GET /api/founder/activation/recent",
      "server/routes-admin.ts::GET /api/admin/evolution-proposals",
      "server/routes-admin.ts::GET /api/founder/ai/stats",
      "server/routes-admin.ts::GET /api/founder/briefing",
      "server/routes-admin.ts::GET /api/founder/monthly-checkin",
      "server/routes-admin.ts::POST /api/founder/support/:ticketId/reply",
      "server/routes-ai-cost.ts::GET /api/founder/ai-cost/ceiling-status",
      "server/routes-autopilot.ts::GET /api/founder/autopilot/receipts/wedge",
      "server/routes-campaigns.ts::GET /api/campaigns/:id/analytics",
      "server/routes-campaigns.ts::GET /api/campaigns/:id/response-trend",
      "server/routes-cmo.ts::GET /api/founder/cmo/dashboard",
      "server/routes-cmo.ts::POST /api/founder/cmo/approve",
      "server/routes-cmo.ts::POST /api/founder/cmo/reject",
      "server/routes-dsar.ts::GET /api/founder/dsar",
      "server/routes-dsar.ts::GET /api/founder/dsar/:id",
      "server/routes-dsar.ts::POST /api/founder/dsar/:id/deny",
      "server/routes-dsar.ts::POST /api/founder/dsar/:id/erasure",
      "server/routes-dsar.ts::POST /api/founder/dsar/:id/fan-out",
      "server/routes-dsar.ts::POST /api/founder/dsar/:id/verify",
      "server/routes-error-boundary.ts::GET /api/founder/error-boundary-trips/recent",
      "server/routes-finance-ledger.ts::GET /buckets",
      "server/routes-finance-ledger.ts::GET /cost-mix",
      "server/routes-founder-bridge.ts::GET /",
      "server/routes-founder-chat.ts::GET /threads",
      "server/routes-founder-chat.ts::GET /threads/:id/messages",
      "server/routes-founder-compliance.ts::GET /api/founder/compliance/fair-lending",
      "server/routes-founder-compliance.ts::GET /api/founder/pricing-experiments",
      "server/routes-founder-financials.ts::GET /api/founder/financials/subscription-events",
      "server/routes-founder-inspector.ts::GET /api/founder/inspector/agent/:codename",
      "server/routes-founder-inspector.ts::GET /api/founder/inspector/decision/:id",
      "server/routes-founder-intelligence.ts::GET /audit-log/explain-by-decision/:decisionId",
      "server/routes-founder-intelligence.ts::GET /automation",
      "server/routes-founder-intelligence.ts::GET /business-intelligence",
      "server/routes-founder-intelligence.ts::GET /company-agents/:codename/detail",
      "server/routes-founder-intelligence.ts::GET /decision-log",
      "server/routes-founder-intelligence.ts::GET /decision-log/:id",
      "server/routes-founder-intelligence.ts::GET /growth",
      "server/routes-founder-intelligence.ts::GET /morning-briefing",
      "server/routes-founder-intelligence.ts::GET /mrr",
      "server/routes-founder-intelligence.ts::GET /sophie-activity",
      "server/routes-founder-intelligence.ts::POST /company-briefing",
      "server/routes-founder-intelligence.ts::POST /decision-log/:id/reverse",
      "server/routes-founder-intelligence.ts::POST /decisions-inbox/purge",
      "server/routes-founder-recourse.ts::POST /api/founder/recourse/:id/dismiss",
      "server/routes-founder-recourse.ts::POST /api/founder/recourse/:id/draft",
      "server/routes-legal-holds.ts::GET /api/founder/legal-holds",
      "server/routes-legal-holds.ts::GET /api/founder/legal-holds/active",
      "server/routes-lifecycle.ts::GET /api/founder/nps/recent",
      "server/routes-misc.ts::GET /api/investor-profiles/directory",
      "server/routes-misc.ts::POST /api/webhooks/twilio/recording-status",
      "server/routes-misc.ts::POST /api/webhooks/twilio/sms-status",
      "server/routes-ml-snapshots.ts::GET /api/founder/ml-snapshots/recent",
      "server/routes-onboarding-funnel.ts::GET /api/founder/onboarding-funnel/orgs",
      "server/routes-organization.ts::GET /api/founder/safety-status",
      "server/routes-pax-audit.ts::GET /api/founder/pax-audit/findings",
      "server/routes-pax-audit.ts::GET /api/founder/pax-audit/recent",
      "server/routes-pax-calibration.ts::GET /api/founder/pax-calibration",
      "server/routes-platform-features.ts::GET /api/community/county-reviews",
      "server/routes-public-sign.ts::GET /api/public/sign/consent/status",
      "server/routes-rosy-river.ts::GET /api/founder/agent-notifications",
      "server/routes-rosy-river.ts::GET /api/founder/proposed-changes",
      "server/routes-rosy-river.ts::PATCH /api/founder/agent-notifications/:id",
      "server/routes-rosy-river.ts::PATCH /api/founder/proposed-changes/:id",
      "server/routes-support-saved-replies.ts::DELETE /:id",
      "server/routes-support-tickets.ts::GET /api/founder/support/analytics",
      "server/routes-support-tickets.ts::GET /api/founder/support/tickets",
      "server/routes-support-tickets.ts::POST /api/founder/escalations/:id/resolve",
      "server/routes-title-partners.ts::GET /api/founder/title-partners/billing-report"
    ]
  },
  "rule2": {
    "method": [
      "server/services/cognitiveMemoryV13.ts::setWorkingMemory",
      "server/services/decisionsInbox.ts::createFromAlert",
      "server/services/decisionsInbox.ts::createFromEscalation",
      "server/services/integrationFrameworkV12.ts::execute",
      "server/services/integrationFrameworkV12.ts::rollbackExecution",
      "server/services/marketplace.ts::getListing",
      "server/services/marketplace.ts::placeBid",
      "server/services/outcomeVerificationV12.ts::verify"
    ],
    "function": [
      "server/ai/paxSupportResolver.ts::resolveTicketWithPax",
      "server/ai/supportAgent.ts::executeSupportTool",
      "server/jobs/indexAnalyzer.ts::saveReport",
      "server/jobs/redemptionClockRefresh.ts::runRedemptionClockRefresh",
      "server/routes-borrower.ts::validateBorrowerSession",
      "server/routes-contractors.ts::recomputeContractorTotals",
      "server/routes-rehabs.ts::recomputeRehabSpend",
      "server/routes-tax-researcher.ts::handoffWonLotToCertificate",
      "server/services/autonomousDecisionExecutor.ts::buildSupportEscalationContext",
      "server/services/communityEnhancements.ts::gatherCaseStudyData",
      "server/services/financialOSService.ts::generateTaxReport",
      "server/utils/auditLogChain.ts::chainAndInsertAuditLog",
      "server/utils/auditLogPurge.ts::sealAndPurgeAuditLogs"
    ],
    "route": [
      "server/routes-admin.ts::POST /api/admin/pax-observations/:id/execute",
      "server/routes-admin.ts::POST /api/founder/support/:ticketId/ai-draft",
      "server/routes-autonomous-agent.ts::POST /tasks/:id/run",
      "server/routes-bid-estimates.ts::POST /api/bids/:id/accept",
      "server/routes-billing.ts::POST /api/subscription/refund-request",
      "server/routes-borrower.ts::GET /api/borrower/autopay",
      "server/routes-borrower.ts::GET /api/borrower/periodic-statements/:statementId/pdf",
      "server/routes-borrower.ts::GET /api/borrower/statements/generate",
      "server/routes-borrower.ts::POST /api/borrower/autopay",
      "server/routes-borrower.ts::POST /api/borrower/autopay/mandate",
      "server/routes-borrower.ts::POST /api/borrower/autopay/mandate/confirm",
      "server/routes-borrower.ts::POST /api/borrower/messages",
      "server/routes-borrower.ts::POST /api/borrower/payment",
      "server/routes-borrower.ts::POST /api/borrower/verify-payment",
      "server/routes-buyer-blasts.ts::PATCH /api/buyer-blasts/recipients/:id",
      "server/routes-buyer-blasts.ts::POST /api/properties/:id/blast-buyers",
      "server/routes-comments.ts::DELETE /:id",
      "server/routes-deliverability.ts::POST /api/org/email-identity/verify",
      "server/routes-founder-appeals.ts::POST /api/founder/appeals/:id/resolve",
      "server/routes-founder-chat.ts::POST /stream",
      "server/routes-founder-inspector-finance.ts::GET /api/founder/inspector/cost-event/:id",
      "server/routes-founder-intelligence.ts::POST /decisions-inbox/:id/approve",
      "server/routes-founder-intelligence.ts::POST /decisions-inbox/:id/override",
      "server/routes-founder-intelligence.ts::POST /decisions-inbox/:id/reject",
      "server/routes-founder-recourse.ts::POST /api/founder/recourse/:id/send",
      "server/routes-leads.ts::DELETE /api/leads/:id",
      "server/routes-leads.ts::PATCH /api/leads/:id/restore",
      "server/routes-legal-holds.ts::POST /api/founder/legal-holds/:id/release",
      "server/routes-lot-basis.ts::POST /api/lots/:childId/realize-cogs",
      "server/routes-lot-pricing.ts::POST /api/parcels/:id/pricing-rules/lock",
      "server/routes-lot-pricing.ts::PUT /api/parcels/:id/pricing-rules",
      "server/routes-marketplace.ts::GET /investors/:id",
      "server/routes-move-inspections.ts::POST /api/inspections/:id/reconcile-deposit",
      "server/routes-note-acquisitions.ts::POST /api/note-acquisitions/:id/promote",
      "server/routes-notes.ts::POST /api/loss-mit-cases/:id/actions",
      "server/routes-notes.ts::POST /api/notes/:id/payments",
      "server/routes-organization.ts::GET /api/nps/pending",
      "server/routes-organization.ts::POST /api/organization/invitations/accept",
      "server/routes-outreach-mail.ts::POST /api/outreach/mail/cancel/:shipmentId",
      "server/routes-outreach-mail.ts::POST /api/outreach/mail/queue",
      "server/routes-permit-tracker.ts::PATCH /api/permit-gates/:gateId",
      "server/routes-public-sign.ts::GET /api/public/sign/:docId",
      "server/routes-public-sign.ts::POST /api/public/sign/:docId",
      "server/routes-public-sign.ts::POST /api/public/sign/consent",
      "server/routes-rehab-photos.ts::POST /api/rehabs/:rehabId/photos",
      "server/routes-rentals.ts::POST /api/leases/:id/renew",
      "server/routes-rentals.ts::POST /api/tenants/:id/adverse-action",
      "server/routes-subdivision-plans.ts::PATCH /api/plans/:planId",
      "server/routes-support-saved-replies.ts::PATCH /:id",
      "server/routes-support-tickets.ts::GET /api/support/tickets/:id",
      "server/routes-support-tickets.ts::POST /api/founder/escalations/:id/generate-prompt",
      "server/routes-support-tickets.ts::POST /api/support/tickets/:id/close",
      "server/routes-support-tickets.ts::POST /api/support/tickets/:id/messages",
      "server/routes-support-tickets.ts::POST /api/support/tickets/:id/pax-resolve",
      "server/routes-support-tickets.ts::POST /api/support/tickets/:id/resolve-human",
      "server/routes-tax-researcher.ts::POST /listings/:id/bid-log",
      "server/routes-team-messaging.ts::PATCH /api/team-messaging/presence",
      "server/routes-team-messaging.ts::POST /api/team-messaging/channels/:id/join",
      "server/routes-team-messaging.ts::POST /api/team-messaging/conversations/:id/messages",
      "server/routes-team-readiness.ts::POST /api/team-readiness/offer-approvals/:id/decision",
      "server/routes-team-readiness.ts::POST /api/team-readiness/slack-integrations",
      "server/routes-title-partners.ts::POST /api/webhooks/title-orders/:orderId/status",
      "server/routes/lob-webhooks.ts::POST /api/webhooks/lob",
      "server/routes/public-qr-redirect.ts::GET /r/:code"
    ]
  },
  "rule3": [
    "server/auth/routes.ts::GET /api/auth/organizations::teamMembers",
    "server/jobs/costOptimizer.ts::collectOptimizerSnapshot::aiUsageDaily",
    "server/jobs/growthAutomation.ts::runWinBackEngine::subscriptionEvents",
    "server/jobs/lifecycleDispatch.ts::runLifecycleDispatch::activationEvents",
    "server/jobs/pendingActionExpiryJob.ts::readDueRows::pendingActions",
    "server/jobs/redemptionClockRefresh.ts::runRedemptionClockRefresh::taxCertificates",
    "server/jobs/runScheduledJobs.ts::runScheduledJobs::properties",
    "server/mcp-server.ts::resolveOrgFromApiKey::organizationIntegrations",
    "server/mcp/auth.ts::resolveMcpAuth::apiKeys",
    "server/mcp/streamableHttp.ts::authenticate::apiKeys",
    "server/middleware/getOrCreateOrg.ts::getOrCreateOrg::teamMembers",
    "server/routes-acquisition-utm.ts::backfillTouchIdentity::teamMembers",
    "server/routes-ai-cost.ts::GET /api/founder/ai-costs::aiTelemetryEvents",
    "server/routes-ai-cost.ts::GET /api/founder/ai-costs::aiUsageDaily",
    "server/routes-borrower.ts::POST /api/borrower/verify-payment::payments",
    "server/routes-buyer-blasts.ts::GET /api/buyer-blasts/:id::buyerBlastRecipients",
    "server/routes-campaigns.ts::PUT /api/optimizations/:id/implement::campaignOptimizations",
    "server/routes-finance-ledger.ts::GET /contribution-margin::financialLedger",
    "server/routes-finance-ledger.ts::GET /mrr::financialLedger",
    "server/routes-misc.ts::POST /api/webhooks/twilio/sms::organizationIntegrations",
    "server/routes-notes.ts::GET /api/notes/:id/basis::notePayments",
    "server/routes-notes.ts::GET /api/notes/:id/payments::notePayments",
    "server/routes-notes.ts::GET /api/notes/:id/reconciliation::notePayments",
    "server/routes-notes.ts::GET /api/notes/:id/yield::notePayments",
    "server/routes-notes.ts::computeAcquiredNotePayoff::notePayments",
    "server/routes-organization.ts::POST /api/organization/invitations/accept::organizationInvitations",
    "server/routes-outreach-mail.ts::GET /api/outreach/mail/results/funnel/:shipmentId::mailShipmentPieces",
    "server/routes-outreach-mail.ts::GET /api/outreach/mail/results/response-timeline/:shipmentId::mailQrScanEvents",
    "server/routes-outreach-mail.ts::GET /api/outreach/mail/shipments::mailShipmentPieces",
    "server/routes-pax-appeals.ts::POST /api/pax/appeals::paxDecisionAppeals",
    "server/routes-public-sign.ts::POST /api/public/sign/:docId::signingConsentAudit",
    "server/routes-quiet-title.ts::GET /api/quiet-title/cases/:id::quietTitleSteps",
    "server/routes-support-tickets.ts::GET /api/founder/escalations::supportTickets",
    "server/routes-support-tickets.ts::POST /api/founder/escalations/batch-prompt::supportTickets",
    "server/routes-title-partners.ts::GET /api/founder/title-partners::titlePartners",
    "server/routes-title-partners.ts::routeTitleOrder::titlePartners",
    "server/routes.ts::GET /api/founder/executive-dashboard::churnRiskScores",
    "server/routes.ts::GET /api/founder/executive-dashboard::deals",
    "server/routes.ts::GET /api/founder/executive-dashboard::leads",
    "server/routes.ts::GET /api/founder/executive-dashboard::npsResponses",
    "server/routes.ts::GET /api/founder/executive-dashboard::properties",
    "server/routes/lob-webhooks.ts::POST /api/webhooks/lob::mailShipmentPieces",
    "server/routes/public-qr-redirect.ts::GET /r/:code::mailQrScanEvents",
    "server/services/adaptiveStrategyV13.ts::getStats::agentStrategies",
    "server/services/adaptiveStrategyV13.ts::getStats::strategyAssignments",
    "server/services/adaptiveStrategyV13.ts::getStats::strategyProposals",
    "server/services/autonomyFinalMile.ts::checkDelegationCompletions::agentDelegations",
    "server/services/cognitiveMemoryV13.ts::setWorkingMemory::agentWorkingMemoryV13",
    "server/services/decisionsInbox.ts::createDirectDecisionCard::decisionsInboxItems",
    "server/services/decisionsInbox.ts::createFromAlert::decisionsInboxItems",
    "server/services/decisionsInbox.ts::createOutcomeCheckIn::decisionsInboxItems",
    "server/services/decisionsInbox.ts::createShadowPromotionRequest::decisionsInboxItems",
    "server/services/feedbackLoopV14.ts::getLearningImpact::feedbackLearnings",
    "server/services/feedbackLoopV14.ts::propagateLearning::feedbackLearnings",
    "server/services/governanceBrainV13.ts::getStats::complianceSnapshots",
    "server/services/governanceBrainV13.ts::getStats::policyEvaluations",
    "server/services/governanceBrainV13.ts::getStats::regulatorySandboxRuns",
    "server/services/marketplace.ts::createListing::marketplaceListings",
    "server/services/marketplace.ts::getMarketplaceStats::marketplaceListings",
    "server/services/marketplace.ts::getMarketplaceStats::marketplaceTransactions",
    "server/services/marketplace.ts::placeBid::marketplaceBids",
    "server/services/matchmaking.ts::findBuyersForListing::investorProfiles",
    "server/services/matchmaking.ts::findMatchesForInvestor::marketplaceListings",
    "server/services/matchmaking.ts::getRecommendations::marketplaceListings",
    "server/services/onboarding/firstValueInstrumentation.ts::getFunnelSummary::onboardingFunnelMetrics",
    "server/utils/auditLogChain.ts::verifyAllOrgAuditLogChains::auditLog",
    "server/webhookHandlers.ts::processBorrowerPaymentRefund::notes",
    "server/webhookHandlers.ts::processBorrowerPaymentRefund::payments"
  ],
  "_POPULATION_CORRECTION_2026_09_04": [
    "These 8 entries were added the same day the register was created, and they are",
    "the ONLY entries in it that carry an individual reason — because they are the",
    "only ones that have been read.",
    "",
    "The route extractor originally took 'the LAST `async (` inside the registration",
    "call' as the handler. The call text spans the handler's whole body, so a nested",
    "`db.transaction(async (tx) => ...)` is later in the string than the handler",
    "itself and won: 51 of 2620 handlers were read at the wrong boundary. It also",
    "dropped every handler written with a TRAILING COMMA before the closing paren",
    "(432 of them) and every handler registered through a wrapper such as",
    "`asyncHandler(...)` (40). Fixing the boundary raised the readable population",
    "from 2142 to 2668 handlers and query chains from 1831 to 2040, and these 8",
    "findings are what had been hiding behind it.",
    "",
    "Each was read before being written down. None is a live cross-tenant read; the",
    "reasons are in _TRIAGED. The other 210 keys have NOT been read — that is the",
    "difference between this block and the rest of the file, and the obligation",
    "above is to close that gap, not to copy this note onto them.",
    "",
    "Nine further entries were REMOVED the same day, not added: each was a query",
    "inside a route handler that had ALSO been attributed to the file's enclosing",
    "registrar (registerRoutes, registerMiscRoutes), so every finding in a route",
    "file was being reported twice — once against a handler you can act on, and",
    "once against a 1,400-line function whose name tells a reader nothing. Queries",
    "are now attributed to their INNERMOST unit; a query written directly in a",
    "registrar body, outside every handler, still belongs to it and is still read."
  ],
  "_TRIAGED": {
    "server/ai/executive.ts::compactConversationIfNeeded": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). Module-private (no `export`); the only bare-id write is the contextSummary update at 1302-1305, and both call sites (processChat:1386, processChatStream:1893) pass `conversation.id` from the same org-verified `getOrCreateConversation(org.id, ...)` const. The `messages` it summarizes come from `getMessages(conversation.id)` on that same verified id, so nothing from another tenant enters the summary either. Same verified-parent reasoning as updateConversation: the id reaching the WHERE has already been proven to carry organizationId === org.id (or was just created with it).",
    "server/ai/executive.ts::getConversation": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). Module-private helper (no `export`), and its ONLY call site in the repo is getOrCreateConversation at line 1323, which discards the row unless it belongs to the caller's org: `if (conv && conv.organizationId === orgId) return conv;`. A caller in org A who POSTs org B's conversationId falls through to createConversation({organizationId: orgId,...}) and gets a brand-new conversation in org A — org B's row is never returned, rendered, or written. The lint's rule1 heuristic reads the helper in isolation and cannot see the caller-side org gate. (Defence-in-depth would still prefer an orgId parameter with eq(aiConversations.organizationId, orgId) in the WHERE, since the ownership check is one edit away from being lost, but as written no cross-tenant read occurs.)",
    "server/ai/executive.ts::updateConversation": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). Module-private (no `export`); both call sites — processChat:1799 and processChatStream:2501 — pass `conversation.id`, where `conversation` is a const bound at 1360 / 1867 to `await getOrCreateConversation(org.id, userId, options.conversationId)`, which returns either a row whose organizationId === org.id or a freshly created row owned by org.id. `org` in both HTTP entrypoints is session-derived (`const org = req.organization;` in server/routes-ai.ts, feeding processChat at 331 and processChatStream at 523), not body-supplied. So the id in the bare `where(eq(aiConversations.id, id))` is always an id already proven to belong to the caller's org — the verified-parent pattern, only with the verification living in the shared factory rather than inline. An org-A caller submitting org-B's conversationId retitles a new org-A row, not org B's.",
    "server/ai/paxSupportResolver.ts::resolveTicketWithPax": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The bare-id ticket read is the parent read of a helper, and BOTH of its only two call sites ownership-check that same id first. `POST /api/support/tickets/:id/pax-resolve` selects the ticket with the same id and refuses on org mismatch (`ticket.organizationId !== org.id && !org.isFounder` -> 403) before importing and calling the helper; `createSupportTicket` in server/ai/supportAgent.ts passes `ticket.id` of a row it just INSERTed with `organizationId: org.id`. Grep across the repo shows no third production caller (other hits are comments/tests). I can state no request that reaches this function with a ticketId outside the caller's org (the only bypass is the explicit `org.isFounder` branch). Noting the fragility: the helper itself trusts its caller — it takes `org` but never adds `eq(supportTickets.organizationId, org.id)`, so a future third caller would silently be a cross-tenant write path (it inserts an agent message and mutates status/resolution).",
    "server/ai/supportAgent.ts::executeSupportTool": "READ AND FIXED, 2026-09-04. executeSupportTool became visible to this lint for the first time on that date: the bracket walkers desynchronised on a nested template literal in the same file, so the whole 91-case model-driven switch — reachable by any authenticated org member via POST /api/support/tickets/:id/pax-resolve — had never been in the population. Rule 2 matches two things here, both read: (a) update_ticket_status, draft_customer_response and record_customer_feedback key on the ticketId PARAMETER; all three call sites happened to check ownership first, so nothing was live, but that was a property of the callers, and an ownership check now runs once at the top of executeSupportTool (founders excepted, matching the pax-resolve route's own rule) so a fourth caller cannot forget it; (b) the paxMemory UPDATE keys on existing[0].id, read three lines above with eq(paxMemory.organizationId, org.id) — a verified parent. Four cross-org READS found in the same pass were FIXED, not registered: search_resolved_tickets, get_similar_resolutions, get_best_resolution_approach and the resolution-analytics tool each read support_resolution_history with no organization predicate, returning another tenant's resolutionApproach and lessonLearned free text into the context of a model then talking to a different paying customer.",
    "server/auth/routes.ts::GET /api/auth/organizations::teamMembers": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). This is the org-resolution lookup itself — the org-switcher must enumerate which orgs the caller belongs to, so it cannot be organizationId-scoped. It is scoped by the session-derived identity instead: userId comes from req.user.id, which clerkAuth's hydrateUser sets from the verified Clerk session (req.user = user, selected by users.clerkUserId from req.auth.userId), never from a param or body. The downstream org reads are then bounded by that result: inArray(organizations.id, orgIds) and eq(organizations.ownerId, userId). No request input reaches any predicate, so no caller can enumerate another tenant's memberships.",
    "server/jobs/autonomousHealthMonitor.ts::checkAICosts": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Platform-ops sweep with no request context. checkAICosts is module-private (the file's only exports are runAutonomousHealthMonitor:535 and registerAutonomousHealthMonitorJob:577), and runAutonomousHealthMonitor is invoked only by the in-process scheduler — server/jobs/runScheduledJobs.ts:571/580 under withJobLock('autonomous_health_monitor', ...) plus the hourly BullMQ repeatable — with no route mounting it anywhere (grep for the symbol finds only runScheduledJobs.ts and doc comments). The three queries deliberately aggregate ai_telemetry_events across all orgs to guard AcreOS's OWN platform AI budget (aiTelemetryEvents.organizationId is nullable — `organizationId: integer(\"organization_id\").references(() => organizations.id)` — so it is a platform-wide telemetry table, not a tenant record set). Only scalar sums leave the function, into a founder-facing systemAlert and log lines; no per-org row is read or returned. There is no caller in org A, so no cross-tenant request exists to state.",
    "server/jobs/autonomousHealthMonitor.ts::createAlert": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Platform-ops sweep, same reachability as checkAICosts: createAlert is module-private and only called from checkJobHealth / checkAICosts / checkPlatformHealth / checkRevenueHealth, all of which run only inside runAutonomousHealthMonitor, which is wired solely into the scheduler/BullMQ (runScheduledJobs.ts:571/580) with no session and no org. The dedupe SELECT is cross-org by design but returns only `{ id }` and is used purely as an insert-or-not decision, so it discloses nothing; the INSERT omits organizationId entirely, and systemAlerts.organizationId is nullable (`organizationId: integer(\"organization_id\").references(() => organizations.id)` — no .notNull()), so the row is created as a platform-level alert rather than being written into any tenant's namespace. No request can reach this code, so no org-A caller can touch an org-B row through it.",
    "server/jobs/costOptimizer.ts::collectOptimizerSnapshot::aiUsageDaily": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Platform-ops aggregate with no request context and no org parameter at all — the signature is collectOptimizerSnapshot(now: Date), and the whole point is a cross-tenant spend/MRR roll-up (it also reads db.select().from(organizations).limit(50_000) in the same unit). Its only two entry points are the nightly scheduler (runScheduledJobs.ts registers it via scheduleSelfRescheduling) and POST /api/founder/cost-optimizer/run-now, whose handler I verified carries a real in-handler founder guard, as do the runs/apply readers. There is no org-scoped caller that could pass an org A identity and get org B rows — the per-org breakdown is founder-only output by construction.",
    "server/jobs/decisionExecutorTick.ts::sumExecutorAiSpendUsdSince": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Module-private (deliberately not exported, per the file header) and called only from runDecisionExecutorTickBounded at lines 98 and 115, which is itself invoked only by the scheduler — runScheduledJobs.ts:1003/1016 under withJobLock('autonomous_decision_executor', ...) — with no HTTP route anywhere (grep for the symbol finds only runScheduledJobs.ts and the definition). The all-org sum is the intended scope: it bounds AcreOS's own autonomous AI spend against a platform cap constant, and the only value crossing the boundary is a scalar USD number compared to AUTONOMOUS_DECISION_EXECUTOR_MAX_USD_PER_TICK and printed to the ops log. ai_telemetry_events carries a nullable organizationId (platform telemetry, not tenant records), and no per-org row or field is returned. No request context exists, so there is no org-A caller that could reach org-B data here.",
    "server/jobs/embeddingRefresh.ts::refreshStaleEmbeddings": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Platform-ops maintenance sweep with no HTTP caller and no org context to scope by. Its only two entry points are the cron self-rescheduler (startEmbeddingRefreshJob -> scheduleSelfRescheduling, same file line 236) and the worker-substrate handler server/worker.ts:294 handleEmbeddingRefresh, which ignores its payload entirely (`_payload`) and hardcodes (100, 7) — so no request-supplied value reaches the query. deal_patterns IS org-scoped (`organizationId: integer(\"organization_id\").references(() => organizations.id).notNull()`, shared/schema.ts:11045) but the sweep is identity-preserving per row: it reads row N's own `fingerprint`, embeds it, and writes the vector back to `WHERE id = ${row.id}` — the same row it read. No org A value is ever read into an org B row, and no caller can select which rows are touched. Note (non-tenancy): the DEFAULT ORDER BY on `embedding_refreshed_at NULLS FIRST` plus LIMIT means the sweep is globally FIFO, so a single large tenant can starve others of refresh slots — a fairness issue, not an isolation one.",
    "server/jobs/featureEngineeringJob.ts::processFeatureEngineeringJob": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Weekly platform sweep, no HTTP caller. Its only production entry is runFeatureEngineering(), called from server/jobs/runScheduledJobs.ts:3234 inside startFeatureEngineeringJob() under withJobLock('feature_engineering', ...) on a Sunday 02:30 UTC time gate — a cron tick with no request and therefore no org to scope to. The cross-org enumeration is per-row identity-preserving: each property's features are computed from that property's own lat/lng/floodZone/soilQuality and written back to that same property id via storeFeatures(property.id, ...). The one cross-row read, computeMarketFeatures(property.state, property.county), hits transaction_training, which is deliberately org-free — shared/schema/marketplace.ts:436 comments the table `// Anonymized (no orgId)` and it has no organization_id column — so the county aggregate is a shared anonymized corpus, not another tenant's private rows. background_jobs (shared/schema/ai-telemetry.ts:393) likewise has no organization_id; it is the platform job-audit table and the two writes key on bgJobId returned by this run's own insert. Note (non-tenancy, real): `.limit(1000)` with NO ORDER BY silently picks an arbitrary 1000 properties platform-wide, so on >1000 rows coverage is nondeterministic and some tenants may never get features.",
    "server/jobs/featureEngineeringJob.ts::storeFeatures": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). A bare-id write into an org-scoped table (properties.organizationId is NOT NULL, shared/schema.ts:1218), but the function is module-private — `async function storeFeatures(` with no `export` — and a repo-wide grep of server/, client/ and shared/ finds exactly one call site: featureEngineeringJob.ts:219, inside the cron sweep above. The propertyId it receives is `property.id` from the row the same sweep just SELECTed, so the write lands on the row it was derived from; there is no request-supplied id and no path by which an org-A caller supplies an org-B property id. Note (non-tenancy, worth a separate look): `.set({ aiInsights: features } as any)` REPLACES the whole aiInsights jsonb column rather than merging, so any other AI insight stored on that property is destroyed weekly — and the `as any` is exactly the cast-past-the-typechecker smell CLAUDE.md warns about.",
    "server/jobs/founderChatBackgroundTaskRunner.ts::postFollowUpMessage": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). ai_messages has NO organization_id at all (shared/schema.ts:2910) — it is scoped through its conversationId FK — and the ai_conversations touch is a bare-id update. But the threadId is not attacker-supplied at this layer: it comes from `task.threadId` read in runNextQueuedTask from founder_chat_background_tasks, and rows in that table are created only by startBackgroundTask (server/services/founder-chat/background-tasks.ts:35). Grepping every importer of that module gives exactly two: server/services/founder-chat/tools/synthesis.ts (the start_idor_audit / start_provider_savings_analysis tool handlers, which pass ctx.threadId) and server/services/founder-chat/sub-agents.ts. Both live under the founder-chat tool registry, whose only mount is registerFounderChatRoutes -> app.use(\"/api/founder/chat\", isAuthenticated, getOrCreateOrg, requireFounder, router), and requireFounder (server/auth/clerkAuth.ts:342) 404s anyone failing isFounderIdentity. A grep for the founder-chat executor/tools outside server/services/founder-chat/ finds only routes-founder-chat.ts. So no org-A caller can queue a task at all. Residual worth reporting to the parent (founder-only, so not a tenant defect): routes-founder-chat.ts:362 does `let threadId = parsed.data.threadId;` straight from the request body and never checks `eq(aiConversations.userId, userId)` before using it — the sibling GET at line 199 DOES check it — so the founder alone could aim a background task's follow-up message at any conversation id, including a customer's Pax thread.",
    "server/jobs/founderChatBackgroundTaskRunner.ts::providerSavingsAnalysisRunner": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). The query genuinely sums financial_ledger across every tenant — financialLedger DOES carry organizationId (shared/schema/finance.ts:73) and the where clause names only provider and postedAt — so this is a platform-wide spend total by construction. It is founder-only by reachability: the runner is registered under key \"provider_savings_analysis\" and is invoked solely by runNextQueuedTask draining founder_chat_background_tasks; the only producer of such a row is the start_provider_savings_analysis tool in server/services/founder-chat/tools/synthesis.ts:224, reachable only through the requireFounder-guarded /api/founder/chat mount verified above. Platform-wide vendor spend is precisely the founder cost view (the ledger's own comment describes org-null rows as platform-level postings), and the founder is the only identity that can ask for it. A customer-side caller cannot enqueue this runner key at all.",
    "server/jobs/growthAutomation.ts::runWinBackEngine::subscriptionEvents": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Scheduled platform sweep, not a request handler: runWinBackEngine takes no arguments, is private to the module, and its only reachable entry point is runGrowthAutomation invoked from runScheduledJobs.ts under withJobLock('growth_automation', ...) — I grepped for other callers and there is no route. The unscoped subscriptionEvents read is the sweep's candidate enumeration; every action taken afterwards is re-scoped to the orgId carried by the row itself (wasRecentlyEmailed, the three revenueProtectionInterventions counts, getOwnerEmail, the organizations name read, sendEmail's organizationId, logGrowthEmail), so org A's owner can only ever receive org A's win-back mail.",
    "server/jobs/indexAnalyzer.ts::saveReport": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The flagged `eq(organizationIntegrations.id, existing.id)` UPDATE is keyed on a row read three lines earlier under `and(eq(organizationIntegrations.organizationId, SYSTEM_ORG_ID), eq(organizationIntegrations.provider, PROVIDER))` in the same function — a verified parent, and the id is not caller-supplied. There is no request path at all: saveReport takes only an IndexAnalysisReport and is called by the daily platform job (line 343), writing AcreOS's own founder-plane row at SYSTEM_ORG_ID = 1 (shared/tenancy/systemOrg.ts).",
    "server/jobs/landCreditScoreRecalculation.ts::findStaleProperties": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Daily platform sweep, no HTTP caller: the only production entry is runLandCreditScoreRecalculation(), called from server/jobs/landCreditScoreRecalcJob.ts:19 inside a 01:00-UTC time gate under withJobLock('land_credit_score_recalc', ...), registered from runScheduledJobs.ts:30 — no request, no org context. Two reasons the cross-org enumeration cannot leak: (a) land_credit_scores has NO organization_id column (shared/schema/marketplace.ts:512-515 — `propertyId: integer(\"property_id\").references(() => properties.id).notNull()`), so eq(landCreditScores.propertyId, property.id) IS the correct and only available scoping, and property ids are globally unique serial PKs; (b) the downstream recalculation in the same file passes the property's OWN tenant key through — `landCredit.calculateCreditScore(property.organizationId, property.id)` — so scoring for org A is computed under org A and inserted against org A's property id. No org A row is read into an org B computation. Note (non-tenancy): findStaleProperties loads every active property and then issues one query per property (N+1), and processLandCreditRecalcJob applies no cap, unlike the sibling feature-engineering job.",
    "server/jobs/lifecycleDispatch.ts::runLifecycleDispatch::activationEvents": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Cross-org candidate enumeration inside a scheduler-only sweep. runLifecycleDispatch(now) takes only a clock and is called exclusively from startLifecycleDispatchJob in runScheduledJobs.ts under withJobLock('lifecycle_dispatch', ...) — no route reaches it. The activationEvents read collects the orgs that first paid inside the d30 window; every subsequent read and every send is keyed on an org id that came out of that scan (inArray(organizations.id, [...firstPaidByOrg.keys()]), inArray(lifecycleMessageSends.organizationId, orgIds), inArray(churnReasons.organizationId, cancelledIds)), and the recipient is that same org's own owner via ownerEmailOf(org.ownerId). No mixing of one org's facts with another's address is possible.",
    "server/jobs/morningBrief.ts::collectBriefData": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Platform-ops sweep with no request context. collectBriefData is a module-private helper (not exported) whose only production caller chain is cron: runScheduledJobs.ts startAtlasMorningBriefJob -> processAtlasMorningBrief -> runMorningBriefForAllFounders -> runMorningBriefForFounder -> collectBriefData. The recipient set is enumerated by `eq(organizations.isFounder, true)`, so the platform-wide aggregates (bucket balances, pending decisions count, DLQ count, per-org margin) are only ever composed into a founder's own Atlas thread. There is no HTTP surface and no caller-supplied parameter — collectBriefData() takes no arguments — so no request from org A can reach it at all. The per-org read in computePerOrgMargin is deliberately cross-org (it GROUPs BY financialLedger.organizationId to tell the founder which orgs burn margin) and is reachable only by that founder-gated cron path.",
    "server/jobs/morningBrief.ts::findDefaultFounderThread": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). This is a resolution lookup: it answers \"which Atlas thread belongs to THIS user\", keyed on the user's own identity, so it cannot itself be org-scoped without breaking. The predicate is eq(aiConversations.userId, founderUserId) AND scope='founder' AND isDefault=true, and the schema carries a partial unique index `ai_conversations_one_default_per_founder` on (userId, scope) WHERE is_default = true AND scope = 'founder' — so at most one row can match and it is by construction that user's own row. founderUserId is never caller-supplied: the function is module-private (not exported) and the only production path supplies userIds from the isFounder=true owner enumeration. No cross-tenant read is reachable; the missing organizationId predicate is redundant given the userId key.",
    "server/jobs/morningBrief.ts::runMorningBriefForFounder": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Cron-only, and every id it touches is derived from the founder's own userId inside the same call. threadId comes from findDefaultFounderThread(founderUserId) (keyed on aiConversations.userId = that founder, scope='founder', isDefault, backed by a partial unique index), and every subsequent statement — the wasBriefPostedToday read, the aiMessages insert, and the bare-id `update(aiConversations).where(eq(aiConversations.id, threadId))` — is keyed on that just-resolved id, i.e. the verified-parent pattern. founderUserId itself is not attacker-controlled: the only production caller is runMorningBriefForAllFounders, which sources userIds from team_members owners of organizations where isFounder = true, invoked from the 07:00 cron. I checked every reference in the repo (grep for runMorningBriefForFounder): the sole non-test call site is inside this file at line 139; no Express route, tool dispatch, or job endpoint accepts a userId and forwards it here, so there is no request an org-A caller can make.",
    "server/jobs/pendingActionExpiryJob.ts::readDueRows::pendingActions": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Declared platform-ops bypass through the repo's own greppable escape hatch, in a scheduler-only sweep. readDueRows() takes no arguments and is called only from runPendingActionExpirySweep, which is reachable only from startPendingActionExpiryJob's trackInterval + withJobLock — there is no HTTP path. The cross-org read is required because the flip itself (approvalKernel.sweepExpiredPendingActions) is cross-org; each row's org is then re-derived from the row (getPaxControls(row.organizationId), recordPaxEffect({ orgId: row.organizationId }), dispatchPaxAskEvent({ orgId: row.organizationId })), so every receipt and notification lands in the owning tenant.",
    "server/jobs/redemptionClockRefresh.ts::runRedemptionClockRefresh": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Platform-ops sweep with no request, no session and no parameters: `runRedemptionClockRefresh()` takes zero arguments and its only caller is the scheduled job in server/jobs/runScheduledJobs.ts:5510. It deliberately scans every org's active certs, then updates each row by the primary key it just read from that scan (`row.id`), and every derived write carries that row's own `row.organizationId` — the systemAlerts dedupe reads and inserts are all `eq(systemAlerts.organizationId, row.organizationId)`. No org A input can steer it at an org B row, because there is no input.",
    "server/jobs/redemptionClockRefresh.ts::runRedemptionClockRefresh::taxCertificates": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Nightly platform sweep over every tenant's active certificates; runRedemptionClockRefresh() takes no arguments and its only caller is the scheduler registration in runScheduledJobs.ts (line 5487/5510) — no route invokes it. The bare-id UPDATE is keyed on row.id from this same unscoped scan, not on any external input, so it can only rewrite the row it just read; and every write that carries tenancy uses the row's own organizationId (all three systemAlerts dedupe reads use eq(systemAlerts.organizationId, row.organizationId) and all three inserts set organizationId: row.organizationId). No org A request can steer it at org B data because no request reaches it at all.",
    "server/jobs/runScheduledJobs.ts::processOutcomeAnalyzerJob": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Module-private cron job (declared `async function`, never exported, no route mounts it) with no request and no session. The only query inside the unit is a platform-wide `count(*)` over outcomeTelemetry used purely as a should-we-run gate — it returns a scalar to `mustRunCondition` and no row content ever leaves the function, so there is nothing for an org-A caller to read even in principle. Its work delegate runOutcomeAnalysis() is itself a documented per-org platform sweep (\"iterates all active orgs\") that writes back per-org, not a cross-tenant read served to a caller. Triggered by trackInterval at 02:00 plus a 3-minute post-startup pass, guarded by withJobLock.",
    "server/jobs/runScheduledJobs.ts::processTelemetryOptimizerJob": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Same shape as the outcome analyzer: module-private cron job (`async function`, not exported, no route mount), fired by trackInterval at 03:00 under withJobLock, with no req/res and no caller-supplied input. Its single query is a platform-wide `count(*)` over aiTelemetryEvents compared against a >= 100 sample threshold; only that boolean escapes, so no per-tenant row is exposed. The optimization it gates (runTelemetryOptimizer) tunes AcreOS's own model routing tiers — platform-level state, not org data. Note that routes-admin.ts:4307 also calls runTelemetryOptimizer, but that is a different unit outside this entry.",
    "server/jobs/runScheduledJobs.ts::runScheduledJobs::properties": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Platform-ops sweep, not a request handler. `runScheduledJobs()` is exported and called from exactly one place — `server/worker.ts:833-834` (`const { runScheduledJobs } = await import(\"./jobs/runScheduledJobs\"); await runScheduledJobs();`) — a background worker process with no Express request, no session and no caller-supplied input. The flagged unit is the `land_profile_prewarm` self-rescheduling job registered inside it: it scans ALL orgs' properties for rows with coordinates and `enriched_at IS NULL` deliberately, and it carries each row's OWN `organizationId` forward into `prewarmLandProfile(..., { organizationId: p.organizationId, propertyId: p.id, ... })`, so enrichment is written back under the owning org. There is no request in org A that can reach this query or steer its predicate. (Lint mechanics: the unit is org-scoped because `properties.organizationId` is selected and `p.organizationId` is passed on; the `where` names no org — that is the sweep's whole point.)",
    "server/mcp-server.ts::resolveOrgFromApiKey::organizationIntegrations": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). This is the credential→org RESOLUTION lookup: it answers \"which organization owns this bearer token?\", so it cannot itself be org-scoped — there is no org in hand until it returns. It scans `organizationIntegrations` for `provider = 'mcp_api_key'` and compares each stored credential to the presented token; the org id is taken from the row whose SECRET matched (`return integration.organizationId`). `mcpHandler` then uses that resolved id as the sole org for every tool call — `runTool(tool, params, orgId)`, and each tool passes it straight to the org-scoped storage method (`storage.getLeads(orgId)`, `storage.createLead({ organizationId: orgId, ... })`). The route is `app.post('/api/mcp/execute', isAuthenticated, mcpHandler)` (server/routes.ts:1452); the session identity is NOT used to pick the org, but a body-supplied org is explicitly refused (`if (bodyOrgId !== undefined && Number(bodyOrgId) !== orgId) → 403`), so no caller can name another org — they would have to already hold that org's secret. Worth noting as a separate (non-tenancy) hygiene issue: the credential compare `storedKey === bearerToken` is a plain, non-timing-safe equality, unlike the `verifySecret`/`verifyHash` used on the other two MCP paths.",
    "server/mcp/auth.ts::resolveMcpAuth::apiKeys": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Credential→org RESOLUTION lookup, unscoped by necessity. The row is selected by the SECRET (`eq(apiKeys.hashedKey, hashed)`) plus `isNull(apiKeys.revokedAt)`; the hash is derived from the presented token, not from any caller-supplied id, and `hashedKey` is the api_keys uniqueness key. The org binding is the OUTPUT of the function (`return { status: \"ok\", organizationId: row.organizationId }`), so an org predicate could not exist here. It is further hardened after the read — timing-safe re-compare `verifyHash(hashed, row.hashedKey)` and an expiry check — and the file header documents that this function exists precisely to stop the old unbound behaviour ('any caller with the static key could read ANY org's data'). A caller can only obtain org B's binding by presenting org B's `ak_` token.",
    "server/mcp/streamableHttp.ts::authenticate::apiKeys": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Same credential→org RESOLUTION shape, and the only reachable identity source for this endpoint. `app.post('/api/mcp', mcpStreamableHttpHandler)` (server/routes.ts:1458) mounts it with NO `isAuthenticated`/`getOrCreateOrg`, so the `ak_` bearer token IS the whole identity. The apiKeys read is keyed on `hashApiKey(token)` (the secret), not on any caller-supplied id, and is followed by the timing-safe `verifyHash` re-compare plus an expiry check. The second query in the unit — `.from(organizations).where(eq(organizations.id, row.organizationId))` — is a bare-id read but on the id that the verified key itself carries, i.e. the key's own org, so it is scoped by the credential; that org object is what every downstream intent runs against.",
    "server/middleware/getOrCreateOrg.ts::getOrCreateOrg::teamMembers": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). This IS the org-resolution middleware — it is the thing that produces `req.organizationId` for every other handler, so by construction it runs before any org context exists. Two teamMembers reads: the first (line 96) is fully scoped (`eq(teamMembers.organizationId, candidate.id) AND eq(teamMembers.userId, userId) AND eq(teamMembers.isActive, true)`) and is the ownership check that stops a stale `acreos_active_org` cookie from letting a revoked seat operate in someone else's org. The SECOND (line 126, the one the lint flags) has no org predicate but is keyed on `teamMembers.userId = userId`, where `userId = (req.user as any).id` from the session — it reads only the CALLER'S OWN membership rows, to answer 'which org do I belong to'. It then loads that membership's org by id (`eq(organizations.id, active.organizationId)`), i.e. an org the caller is an ACTIVE member of. There is no caller-supplied input anywhere in this path, so no request can steer it at another tenant.",
    "server/routes-acquisition-utm.ts::backfillTouchIdentity::teamMembers": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). The flagged teamMembers read resolves the CALLER'S OWN org membership and cannot be org-scoped — the route runs at signup flush, before an org is attached (`app.use('/api/me/acquisition-utm', isAuthenticated, acquisitionUtmRouter)`, server/routes.ts:1560 — isAuthenticated only, no getOrCreateOrg, as the file header states). The `userId` argument is session-derived, not body-derived: the handler does `const userId = getUserId(req)` (which reads `req.user.id` and throws if absent) and passes it in; `anonymousId` is the only body-supplied value and it is never used in the teamMembers predicate. So the query reads only rows belonging to the authenticated user, and the resulting `organizationId` is one of the caller's own orgs. No request can make this read another tenant's membership. (Out of scope for this key, but noted: the subsequent `marketingTouch` update is keyed on the body-supplied `anonymousId` with `isNull(marketingTouch.userId)`, so a guessed anonymous_id could claim an unclaimed pre-signup touch chain — an attribution-poisoning concern, not a read of another org's data, and not the flagged query.)",
    "server/routes-activation.ts::GET /api/founder/activation/recent": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). The route carries isAuthenticated at registration and an explicit in-handler founder gate on the first line of the body: `if (!requireFounder(areq, res)) return;`. The file-local requireFounder (line 24) resolves the caller's email and Clerk userId and calls the canonical isFounderIdentity, 404ing otherwise — it is a real check, not a name. isFounderIdentity is env-driven (FOUNDER_EMAILS / FOUNDER_USER_IDS) with no dev bypass. The unscoped read of activation_events is the surface's purpose: this is the cross-tenant activation funnel instrument (the sibling /funnel and /by-event endpoints likewise aggregate over all orgs), so it is a platform-ops sweep behind a verified founder guard.",
    "server/routes-admin.ts::GET /api/admin/evolution-proposals": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). agentTasks is genuinely org-scoped (organization_id NOT NULL, shared/schema.ts:1966) and this query carries no org predicate — but the route is registered behind the same isFounderAdmin middleware (routes-admin.ts:724, isFounderIdentity by env FOUNDER_EMAILS/FOUNDER_USER_IDS, 404 for everyone else), and the surface is a platform-level readout of self-assessment proposals produced by the evolution engine across the deployment. No tenant session can reach it, so there is no org-A-reads-org-B request. It is a real unscoped read, correct only because of the founder gate: if that gate is ever removed or this handler is copied to a non-founder route, it becomes a cross-tenant leak of every org's agent_tasks rows (input/output JSON included).",
    "server/routes-admin.ts::GET /api/founder/ai/stats": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). The route is registered with isAuthenticated followed by the isFounderAdmin middleware defined in the same file at line 724, which 404s when req.user is absent and otherwise requires isFounderIdentity({ email, userId }) to pass before calling next(). That is a verified guard on this specific registration, not an inference from the /api/founder prefix. The 24h aggregation over ai_telemetry_events is deliberately platform-wide: the response is four scalars (totalCallsToday, totalCostTodayCents, avgLatencyMs, cacheHitRate) describing AcreOS's own AI spend for the founder observatory page, and no per-row or per-org data is returned, so there is no path by which an org-A caller obtains an org-B row.",
    "server/routes-admin.ts::GET /api/founder/briefing": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). The route carries an in-registration founder gate: isAuthenticated + isFounderAdmin, where isFounderAdmin (server/routes-admin.ts:724) 404s unless isFounderIdentity({email,userId}) is true — a real env-driven check (server/services/founder.ts:98 → isFounderEmail/isFounderUserId over FOUNDER_EMAIL(S)/FOUNDER_USER_IDS), not a stub. The handler is a whole-platform aggregate (MRR across every org, unresolved system alerts, escalated tickets, campaign counts); an org predicate would make it meaningless. No duplicate unguarded registration of this path exists (grep over server/**/*.ts finds only line 3538). A customer request gets 404 before any query runs.",
    "server/routes-admin.ts::GET /api/founder/monthly-checkin": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Same verified gate as /api/founder/briefing — isAuthenticated + isFounderAdmin (routes-admin.ts:724, isFounderIdentity, 404 on failure). The body is a platform-ops monthly rollup: every organization row, subscription cancel events, support-ticket counts, systemAlerts, agentEvents and featureRequests, all bounded only by a 30/60-day window. These are cross-tenant platform metrics by construction, and the only registration of this path in the repo is this founder-gated one.",
    "server/routes-admin.ts::POST /api/admin/pax-observations/:id/execute": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Genuinely cross-org by design and founder-gated. The route is registered with the in-file isFounderAdmin middleware (routes-admin.ts:724), which requires req.user and then isFounderIdentity({ email, userId }) — env-driven FOUNDER_EMAILS / FOUNDER_USER_IDS in server/services/founder.ts — returning 404 to every non-founder, so no tenant caller can reach it. The unscoped reads/writes are the point of the surface: it resolves WHICH org the observation belongs to (obs.orgId) and then acts on that org (churn rescue reset, acknowledge). Scoping it to the founder's own org would break the function. Note this is a genuine platform-ops surface, not a customer route — the only writes are organizations.churnRescueSentAt = null for the observation's own org and the observation's own status.",
    "server/routes-admin.ts::POST /api/founder/support/:ticketId/ai-draft": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Genuinely cross-org by design and guarded. The registration itself carries `isAuthenticated, isFounderAdmin`, and isFounderAdmin (defined at routes-admin.ts:724) 404s any caller that is not a founder identity — it resolves the Clerk userId/email and gates on `isFounderIdentity({ email, userId })` (server/services/founder.ts:98-103 = isFounderEmail || isFounderUserId), calling next() only on a match. The founder support console must read any tenant's ticket; the handler then resolves the owning org FROM the ticket (`eq(organizations.id, ticket.organizationId)`), which is an ownership resolution, not an org-scoped read. No customer session can reach it.",
    "server/routes-admin.ts::POST /api/founder/support/:ticketId/reply": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). This handler genuinely does write to a ticket by bare id (insert into supportTicketMessages with a caller-supplied ticketId, and an update of supportTickets keyed on eq(supportTickets.id, ticketId)) with no organization predicate — but it is the founder support desk, gated by isAuthenticated + isFounderAdmin (routes-admin.ts:724), which returns 404 to anyone failing isFounderIdentity. Cross-org reach is the point: the founder answers tickets from every tenant, and agentName/resolvedBy are hardcoded \"Founder\". A customer in org A cannot reach it at all, so there is no request that lets org A touch org B's ticket. The only registration of this path is this one.",
    "server/routes-ai-cost.ts::GET /api/founder/ai-cost/ceiling-status": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). In-handler founder check: `if (!req.isFounder) return Errors.forbidden(...)`. req.isFounder is set only by hydrateUser (server/auth/clerkAuth.ts:279, reached from isAuthenticated) when isFounderIdentity matches, and by requireFounder — it is never defaulted true, so a non-founder is refused before the query. The single query is a deliberate platform-wide SUM of aiTelemetryEvents.estimatedCostCents over a rolling 24h, compared against the platform ceiling AI_PLATFORM_DAILY_CEILING_CENTS; scoping it to one org would report a number that does not match what assertWithinPlatformCostCeiling() enforces. Unscoped by design.",
    "server/routes-ai-cost.ts::GET /api/founder/ai-costs::aiTelemetryEvents": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Same unit, same gate: the `if (!req.isFounder) return Errors.forbidden(...)` check at the top of the handler precedes all three aiTelemetryEvents reads (the two raw-SQL rollups and the top-20 Drizzle select). These are per-call platform cost aggregates across all orgs by design (cost by feature, top expensive calls, per-model distribution); org-scoping them would defeat the dashboard. No customer-reachable path exists into this handler.",
    "server/routes-ai-cost.ts::GET /api/founder/ai-costs::aiUsageDaily": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). The handler's FIRST statement is an in-handler founder gate that returns 403 for anyone else, so every query below it (including both aiUsageDaily reads) runs only for the founder. req.isFounder is set server-side by isAuthenticated via isFounderIdentity({email,userId}) in server/auth/clerkAuth.ts:278-279 — it is never client-settable — and this is a deliberate platform-wide rollup (\"today's usage by org (top 10)\"), i.e. an aggregate that cannot be org-scoped and still be what it is. Registered once, at server/routes.ts:2061-2062 (registerAiCostRoutes(app)); no second registration of the path exists.",
    "server/routes-autonomous-agent.ts::POST /tasks/:id/run": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The task is fetched with a compound predicate that includes eq(agentTasks.organizationId, org.id) and 404s if it misses; both later db.update(agentTasks).where(eq(agentTasks.id, taskId)) calls are keyed on that already-ownership-checked id, and executeAgentTask receives context.organizationId: org.id. A caller in org A passing org B's task id gets Errors.notFound before any write. (Belt and braces: the route also carries requireFounder — server/auth/clerkAuth.ts:342, isFounderIdentity by FOUNDER_EMAILS/FOUNDER_USER_IDS, 404 otherwise — and the router-level router.use(isAuthenticated, getOrCreateOrg) supplies req.organization.)",
    "server/routes-autopilot.ts::GET /api/founder/autopilot/receipts/wedge": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Registered with isAuthenticated + requireFounder, imported at routes-autopilot.ts:15 from \"./auth\", which re-exports clerkAuth.requireFounder (server/auth/index.ts:1); that middleware 404s unless isFounderIdentity({email,userId}) holds (clerkAuth.ts:342-356). The three branches read campaignDeliveryEvents, messages and offers platform-wide with only a 7-day cutoff — these are the receipts behind the founder Letter's own Outreach/Replies/Offers tiles, which are whole-platform counts, so the rows must match the same unscoped gatherer. No customer-reachable path exists; the only registration of this URL is this founder-gated one.",
    "server/routes-bid-estimates.ts::POST /api/bids/:id/accept": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). `target` is resolved with `and(eq(bidEstimates.id, req.params.id), eq(bidEstimates.organizationId, orgId))` and 404s on miss. Inside the transaction the bulk reject is org-scoped (`eq(bidEstimates.organizationId, orgId)` AND `eq(bidEstimates.rehabId, target.rehabId)`), and the flagged bare-id accept is keyed on `target.id` — the id of the row that check returned, not on unvalidated input. A caller in org A sending org B's bid id gets 404 before the transaction opens. Route chain is isAuthenticated + getOrCreateOrg (registerBidEstimateRoutes at server/routes.ts:2416).",
    "server/routes-billing.ts::POST /api/subscription/refund-request": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). Every bare-id write in the handler targets a row this same request just created for the caller's own org. The insert sets organizationId: org.id and returns the row, so `request.id` is by construction the caller's row; the rate-limit read and the org update are both explicitly org-scoped (eq(refundRequests.organizationId, org.id), storage.updateOrganization(org.id, ...)), and the Stripe charge is taken from org.stripeCustomerId. There is no caller-supplied id anywhere — the body schema is only { reason }. No request from org A can touch org B's refundRequests row.",
    "server/routes-borrower.ts::GET /api/borrower/autopay": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The 'bare id' is `session.noteId`, not a request parameter — the handler reads no id from req.params/req.query/req.body at all. The note id is fixed at session-mint time by POST /api/borrower/verify, which resolves the note from an access token and then requires the borrower's email to match the note's linked lead within note.organizationId. The `validateBorrowerSession` middleware (registered on this route) re-asserts `linkedNote.organizationId === session.organizationId` on every request and destroys the session otherwise, so the note cannot drift into another tenant. Every downstream call is then scoped with the note's own org (`getAchMandateSummary(note.organizationId, note.id)`, `resolveAchAvailability(note.organizationId)`, `storage.getOrganization(note.organizationId)`). There is no request a borrower in org A can send to reach org B's note.",
    "server/routes-borrower.ts::GET /api/borrower/periodic-statements/:statementId/pdf": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The statement row itself — the only thing a caller can name — is triple-scoped: `eq(periodicStatements.id, req.params.statementId)` AND `eq(periodicStatements.loanId, session.noteId)` AND `eq(periodicStatements.organizationId, session.organizationId)`, with an explicit 401 when the session carries no org pin. What the lint matched is the follow-up `eq(notes.id, session.noteId)`, whose id is not caller-supplied at all: it comes from the server-side borrower session row, and validateBorrowerSession (applied in this route's registration, defined line 386) already re-read that same note and destroyed the session unless `linkedNote.organizationId === session.organizationId`. The borrower lookup that follows is org-scoped too (`storage.getLead(session.organizationId, note[0].borrowerId)`). A borrower guessing another org's statement UUID gets 404.",
    "server/routes-borrower.ts::GET /api/borrower/statements/generate": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). `parsedNoteId` is never client-supplied. It comes from one of two server-side sources: (a) the DB borrower_sessions row looked up by the opaque cookie/header token (`String(dbSession.noteId)`, with an expiry check), or (b) `verifyBorrowerSession(stmtCookie, ip)`, which is HMAC-SHA256 over the payload with BORROWER_SESSION_SECRET, timing-safe compared, expiry-checked and IP-bound, and whose `scope` was minted server-side as `note:${note.id}` only after the exchange resolver verified accessToken -> note AND `borrower.email === email` for that note's own org. A caller cannot pick a note id, so the bare `eq(notes.id, parsedNoteId)` cannot cross tenants; everything downstream is derived from that note's own org (`storage.getLead(note.organizationId, ...)`, `storage.getPayments(note.organizationId, note.id)`, `storage.getOrganization(note.organizationId)`), and the query params (type/year/startDate/endDate) are only filters. Unlike the other three, this route does NOT run validateBorrowerSession, so it skips the Lens-23 note-org re-assert — a weaker defense-in-depth posture if a note is ever moved between orgs, but not itself a cross-tenant path.",
    "server/routes-borrower.ts::POST /api/borrower/autopay": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). Same mechanism as the GET: the only id used is `session.noteId`, which the borrower cannot influence (the request body carries only `{ enabled }`), and `validateBorrowerSession` re-asserts note.organizationId === session.organizationId before the handler runs. The write itself is org-scoped — `storage.updateNote(note.id, {...}, note.organizationId)` appends `eq(notes.organizationId, organizationId)` to its WHERE — and the mandate revocation is likewise scoped by `organizationId: note.organizationId`. The org value is derived from the verified note, never from the caller.",
    "server/routes-borrower.ts::POST /api/borrower/autopay/mandate": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The 'bare id' is not attacker-controlled: session.noteId is a server-stored column on the borrower_sessions row, resolved from an opaque cookie/header token by validateBorrowerSession (mounted on this route at line 1351). That middleware additionally re-asserts the note's current organizationId against the session's org snapshot and destroys the session on mismatch, so a session minted for note X can only ever load note X. Every org value used afterwards (getAchMandateSummary, storage.getOrganization, startAchMandateSetup) is derived from note.organizationId, not from any caller-supplied field. There is no request a borrower of org A can make to reach an org B note.",
    "server/routes-borrower.ts::POST /api/borrower/autopay/mandate/confirm": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). Same mechanism as the mandate POST — the note is keyed on the server-held session.noteId behind validateBorrowerSession (line 1492), which pins the note's org to the session's org snapshot. I also checked the one client-supplied identifier here, setupReference: confirmAchMandateSetup looks the mandate up by that reference but then explicitly refuses when mandate.noteId !== input.noteId, so a borrower cannot confirm another note's (or another org's) mandate. The subsequent write, storage.updateNote(note.id, {...}, note.organizationId), passes the org derived from the verified note.",
    "server/routes-borrower.ts::POST /api/borrower/messages": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The note is read by `session.noteId` (server-held, org-pinned by validateBorrowerSession), and the request body supplies only `content`. The inserted message is keyed on that same verified note id with the tenant key taken from the note itself (`orgId: note.organizationId`), so a borrower can only append to their own note's thread. No caller-supplied noteId or orgId exists on this route, so there is no request that reaches another org's borrower_messages row.",
    "server/routes-borrower.ts::POST /api/borrower/payment": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The \"bare id\" the lint saw is `notes.id = session.noteId`, and `session` is the server-minted borrower_sessions row attached by `validateBorrowerSession` — the client never supplies a note id (no :id param, no body id; the body is only `{ amount }`). The middleware additionally re-asserts `linkedNote.organizationId === session.organizationId` and destroys the session on mismatch, so the note reached here is the one the session was minted for. Every subsequent call is scoped by the org derived from that note: `resolveOrgCardProcessor(note.organizationId)`, `storage.getLead(note.organizationId, note.borrowerId)`, `storage.getOrganization(note.organizationId)`, and the write `storage.updateNote(note.id, {...}, note.organizationId)` passes organizationId as the third arg (noteRepo.updateNote adds `eq(notes.organizationId, organizationId)`). No request shape lets an org-A caller reach an org-B row.",
    "server/routes-borrower.ts::POST /api/borrower/verify-payment": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). Same shape as /api/borrower/payment: the note is resolved from `session.noteId` (server-minted session row, org-pinned by validateBorrowerSession at lines 405-420), never from client input — the body carries only `{ sessionId }`. The payment INSERT is written with `organizationId: note.organizationId`, the balance update is `eq(notes.id, note.id)` on that same session-derived note, and the schedule write is `storage.updateNote(note.id, {...}, note.organizationId)`. The one query with no org predicate is the conflict re-read `eq(payments.transactionId, sessionId)` at line 1127, but it is only reachable after `stripe.checkout.sessions.retrieve(sessionId, undefined, customerMoneyReadOptions(routing.processor, ...))` succeeds and reports `paid` — and customerMoneyReadOptions returns `{ stripeAccount: processor.accountId }`, i.e. the CALLER's org's own connected account. So the transaction id must be a real Stripe object on org A's own account; a conflicting row in org B would require org B to have pre-planted that exact high-entropy `cs_...` id. I can state no concrete cross-tenant request. Worth a note, not a defect: the conflict branch returns the full `payments` row it finds, so this read is the residual place a cross-org row could surface if a transaction id ever collides.",
    "server/routes-borrower.ts::POST /api/borrower/verify-payment::payments": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The flagged query is the ON-CONFLICT re-read keyed on payments.transactionId (a globally UNIQUE external Stripe checkout-session id, shared/schema.ts:1715). Ownership of that id is proven earlier in the SAME handler: the note comes from the org-pinned borrower session, and the session id is retrieved from the LENDER'S connected Stripe account via customerMoneyReadOptions(routing.processor), which throws if there is no accountId and otherwise sets {stripeAccount: acct_of_this_org}. A session id belonging to another org's connected account cannot be retrieved on this org's account (Stripe returns resource_missing → catch → 500), so control never reaches the payments read with a foreign transactionId; and any payments row carrying a Stripe cs_ id was written by this same gate with organizationId = that account's org. Residual (not a defect in this handler): if two AcreOS orgs ever connected the identical Stripe account, the id space would overlap.",
    "server/routes-borrower.ts::validateBorrowerSession": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). This is exactly the 'lookup that RESOLVES which org owns something' case, and it is the tenant defense rather than a hole. The note id is not client-supplied: `session.noteId` comes from the borrower_sessions row fetched by the secret session token (platformOpsRepo.getBorrowerSession -> `eq(borrowerSessions.sessionToken, token)`). The query reads the note's CURRENT organizationId precisely so it can be compared against the org pinned on the session at mint time; adding `eq(notes.organizationId, session.organizationId)` would make the mismatch indistinguishable from a deleted note. On mismatch the session is destroyed and the request 401s.",
    "server/routes-buyer-blasts.ts::GET /api/buyer-blasts/:id::buyerBlastRecipients": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). Textbook verified-parent. The parent blast is read in the same handler with BOTH eq(buyerBlasts.id, req.params.id) AND eq(buyerBlasts.organizationId, orgId), and a miss 404s; the recipients query is then keyed on blast.id — the id that was just ownership-checked (buyer_blast_recipients.blast_id is an FK to that uuid PK). The two innerJoins are join conditions (buyerProfiles.id = recipients.buyerProfileId, leads.id = buyerProfiles.leadId), not id lookups, and the recipient rows they traverse are only ever created from org-scoped matches (POST path filters eq(buyerPropertyMatches.organizationId, orgId) and stamps organizationId: orgId on every recipient row).",
    "server/routes-buyer-blasts.ts::PATCH /api/buyer-blasts/recipients/:id": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The only bare-id query is the parent-blast counter bump, and its key (row.blastId) comes from a recipient row the SAME handler just resolved under eq(buyerBlastRecipients.organizationId, orgId). The attacker-controlled value (req.params.id) is org-scoped; row.blastId is a DB-derived FK on that verified row, not request input. Mount is isAuthenticated + getOrCreateOrg + requireRole([\"owner\",\"admin\"]) at registerBuyerBlastRoutes. No request from org A can name a blast in org B.",
    "server/routes-buyer-blasts.ts::POST /api/properties/:id/blast-buyers": "Rule-2 false positive. Every read in the handler carries eq(*.organizationId, orgId), including the property ownership check; what rule 2 matched are innerJoin conditions (buyerProfiles.id = buyerPropertyMatches.buyerProfileId, leads.id = buyerProfiles.leadId), which are join keys, not id lookups.",
    "server/routes-campaigns.ts::GET /api/campaigns/:id/analytics": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The parent campaign is ownership-checked in this same handler and every later query is keyed on that verified campaignId. storage.getCampaign(org.id, campaignId) is and(eq(campaigns.organizationId, orgId), eq(campaigns.id, id)) (server/storage/campaignRepo.ts:21) and the handler returns 404 when it misses, so a caller in org A cannot get past line 4 with org B's campaign id. getCampaignResponses(org.id, campaignId) is additionally org-filtered; the campaignDeliveryEvents aggregate and the deals/properties/leads join are counts keyed on the verified campaign id (the join conditions eq(deals.propertyId, properties.id) / eq(properties.sellerId, leads.id) are relational, not id lookups). No row of another org is reachable.",
    "server/routes-campaigns.ts::GET /api/campaigns/:id/response-trend": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The handler ownership-checks the parent campaign in the SAME handler via storage.getCampaign(org.id, campaignId), whose implementation is and(eq(campaigns.organizationId, orgId), eq(campaigns.id, id)) and 404s on miss. The campaignResponses aggregate is then keyed on that verified campaignId. The only writer that sets campaign_responses.campaignId (routes-campaigns.ts:279-287) attaches it only when campaign.organizationId === org.id, so a response row can never point at another org's campaign. The lint's rule1 misfired because the org predicate lives inside the storage helper, not as a literal eq(...organizationId) in the unit.",
    "server/routes-campaigns.ts::PUT /api/optimizations/:id/implement::campaignOptimizations": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The select is by bare id, but the very next statement compares the fetched row's organizationId to req.organization.id and 404s on mismatch, before anything is returned to the caller and before any write. A caller in org A passing org B's optimization id gets 404 and zero bytes of B's row; the subsequent unscoped storage.markOptimizationImplemented(optimizationId, ...) is reachable only after that check passes, so the write is ownership-gated too. The lint misfires because the org predicate is enforced in JS rather than in the WHERE clause (an in-handler ownership check on the same row).",
    "server/routes-cmo.ts::GET /api/founder/cmo/dashboard": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Registered with isAuthenticated + requireFounder imported directly from \"./auth/clerkAuth\" (routes-cmo.ts:36), the verified 404-on-non-founder gate. The data is not tenant data at all: it is AcreOS's own advertising instrument — the brand profile is pinned to the literal slug \"acreos\", and cmoAdRenders / cmoHookArchetypes / cmoBudget (keyed on that brand's id) hold AcreOS's own ad creative and spend caps, matching the standing rule that paid advertising is a founder instrument on AcreOS's own account. The decisionsInboxItems read is narrowed to itemType \"cmo_ad_review\", the same founder-only ad-review lane. Nothing here is reachable by, or belongs to, a customer org.",
    "server/routes-cmo.ts::POST /api/founder/cmo/approve": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). The handler is registered with the `requireFounder` middleware inline in its own `app.post(...)` registration (not merely a `/api/founder` path convention). I verified `requireFounder` in server/auth/clerkAuth.ts:342 — it 404s unless `req.user` exists AND `isFounderIdentity({email, userId})` passes, which resolves against the env allowlists FOUNDER_EMAIL/FOUNDER_EMAILS/FOUNDER_USER_IDS (server/services/founder.ts:78-102) and returns false when unset, so it fails closed. `isAuthenticated` (clerkAuth.ts:293) hydrates `req.user` from the DB `users` row keyed by verified Clerk `sub`, so the email compared is not attacker-controlled. This is the CMO ad-engine approval surface — AcreOS's own advertising, which the constitution designates a founder-only instrument — so it is unscoped by design: there is no tenant whose `decisions_inbox_items` / `cmo_ad_renders` rows a non-founder could reach, because no non-founder reaches the handler at all. registerCmoRoutes is mounted at server/routes.ts:2017-2018 with no competing earlier registration of this path.",
    "server/routes-cmo.ts::POST /api/founder/cmo/reject": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Same verified gate as the approve handler: `requireFounder` is present in this handler's own registration and is the real, fail-closed env-allowlist middleware at server/auth/clerkAuth.ts:342. The unscoped reads/writes here (`decisionsInboxItems` by bare id, `cmoAdRenders` by inArray, `brandProfiles` by slug \"acreos\", insert into `cmoRejectionNotes`) are the founder's own ad-review queue for AcreOS's brand profile, not tenant data; no session that is not the founder's reaches the body of the handler, so no caller in org A can act on org B's rows. Note the handler also has no org context of its own to scope by — it resolves the AcreOS brand profile by slug, which is the platform's own record.",
    "server/routes-comments.ts::DELETE /:id": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). Classic verified-parent shape, and the parent here is the row being deleted: the select at 191-195 carries eq(entityComments.organizationId, org.id) alongside the id, 404s if absent, then 403s unless existing.userId matches the caller. The delete at line 203 is keyed on exactly that verified commentId, so a foreign-org comment id can never reach it. Mounted authenticated: each route declares isAuthenticated, getOrCreateOrg inline (line 181), since server/routes.ts:1494 mounts the router bare.",
    "server/routes-contractors.ts::recomputeContractorTotals": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). Verified parent in the calling handler, and the helper's own aggregate reads are org-scoped. Its single call site (line 386) is POST /api/contractors/:id/payments, which first loads the contractor with `and(eq(contractors.id, req.params.id), eq(contractors.organizationId, orgId))` and 404s if absent, then passes that verified `c.id` plus the request's orgId. Both SUM queries inside carry `AND organization_id = ${orgId}`, so the totals written can only reflect the caller's own payments, and the `eq(contractors.id, contractorId)` UPDATE targets a row already proven to be in orgId.",
    "server/routes-deliverability.ts::POST /api/org/email-identity/verify": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The bare-id select IS the ownership check. The handler reads only the identity's organizationId by id, then compares it to the caller's org and returns 403 on mismatch BEFORE calling verifyIdentity(identityId). A caller in org A passing org B's identityId gets Errors.forbidden and no side effect, so the lint's rule2 heuristic matched the guard itself.",
    "server/routes-dsar.ts::GET /api/founder/dsar": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). The gate here is an in-handler check, and I confirmed it is real and short-circuits: line 173 calls the module-local `requireFounder(areq, res)` and returns immediately on false; that helper (routes-dsar.ts:86-95) writes `Errors.notFound(res, \"Resource\")` and returns false unless `isFounderIdentity({email, userId})` passes against the same env allowlists, so a non-founder gets a 404 before any query runs — fail-closed when the env vars are unset. `dsar_requests` does carry a nullable `organization_id` (shared/schema.ts:5768), which is what tripped rule1, but the public intake at POST /api/public/dsar never sets it (the insert at lines 132-146 omits it entirely), so it is an informational back-reference, not a tenancy key. The full unfiltered list is the point of the surface: AcreOS is the data controller receiving GDPR/CCPA requests from anonymous members of the public with no org context, and the founder triages the whole queue.",
    "server/routes-dsar.ts::GET /api/founder/dsar/:id": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Guarded by the same verified in-handler `requireFounder` at line 189, which returns 404 and stops the handler before the query for any non-founder identity. The bare-id lookup `eq(dsarRequests.id, req.params.id)` is unscoped by design: the row is a public DSAR intake with a null `organization_id` (the intake insert never sets it), so there is no owning org to scope against, and the only caller who can reach it is the founder acting as data controller. No org-A caller can read an org-B row here because no org-scoped caller reaches the handler at all.",
    "server/routes-dsar.ts::POST /api/founder/dsar/:id/deny": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Line 335 runs the verified in-handler `requireFounder` and returns on failure, before both the zod parse and any DB access. The subsequent select/update on `dsarRequests` by bare `req.params.id` is a write to a public-intake row with no owning tenant (`organization_id` is nullable and never set by POST /api/public/dsar), and the denial decision — with its `deniedReason` and audit-log entry — is the data controller's to make. Denying a DSAR is precisely a founder/controller act, so unscoped is correct here rather than an isolation gap.",
    "server/routes-dsar.ts::POST /api/founder/dsar/:id/erasure": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Line 291 applies the same verified in-handler founder gate before any query. The only write the handler performs is flipping the DSAR row's own status to \"fulfilling\" keyed on the id it just read; the actual erasure is `runErasureStub` (line 105), which unconditionally throws \"Implement: erasure (legal-hold check + soft-delete)\" and is converted to a 501, so no tenant rows are touched at all today. Even if it were implemented, only the founder identity reaches it. The row itself is a public-intake DSAR with a nullable, unpopulated `organization_id`, so there is no owning org to scope the lookup by. Worth noting for the future: when the erasure stub is made real it will cross every org-scoped table by email and will need its own tenancy reasoning — but that is not a defect in this handler as written.",
    "server/routes-dsar.ts::POST /api/founder/dsar/:id/fan-out": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Founder-gated by an in-handler check that returns 404 before any query runs. The local requireFounder() (line 86) resolves the caller's email + Clerk userId and calls isFounderIdentity(), which matches only FOUNDER_EMAIL/FOUNDER_EMAILS or FOUNDER_USER_IDS (server/services/founder.ts:98) — it fails closed (404) when the env vars are unset or req.user is absent. isAuthenticated hydrates req.user from the DB (server/auth/clerkAuth.ts:274) so user.email is real, not client-supplied. dsar_requests is a platform-level intake table fed by the UNAUTHENTICATED POST /api/public/dsar (line 112), which never sets organizationId, so the operator surface cannot be org-scoped and still see the requests it exists to process. registerDsarRoutes(app) is called once (server/routes.ts:2603) and grep shows no competing/unguarded registration of this path.",
    "server/routes-dsar.ts::POST /api/founder/dsar/:id/verify": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Identical mechanism to the fan-out handler: isAuthenticated plus the in-handler `if (!requireFounder(areq, res)) return;` gate (routes-dsar.ts:86 -> isFounderIdentity), which 404s any non-founder before the select/update runs. The row is then updated on its own verified id (eq(dsarRequests.id, row.id)), the same row the founder just read. dsar_requests carries a nullable organizationId that the public intake at line 132 never populates, so there is no tenant key to scope on; this is the platform DSAR operator queue by design.",
    "server/routes-error-boundary.ts::GET /api/founder/error-boundary-trips/recent": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). The route is registered with the real requireFounder middleware imported from ./auth (server/auth/index.ts re-exports it from clerkAuth.ts:342), which 404s unless isFounderIdentity() matches by founder email or Clerk user id — verified in the middleware source, not inferred from the /api/founder prefix. Cross-org reading is the point of the surface: error_boundary_trips.organizationId is nullable and the companion POST /api/client/error-boundary-trip is deliberately unauthenticated (\"the boundary may fire pre-hydration\"), so most rows have organizationId = null; a founder diagnostic that scoped to one org would miss exactly the anonymous pre-auth trips it exists to catch. registerErrorBoundaryRoutes(app) is called once at server/routes.ts:2548 and no other file registers this path.",
    "server/routes-finance-ledger.ts::GET /buckets": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). The whole router is mounted behind requireFounder (server/routes.ts:1830), the same clerkAuth.ts:342 middleware verified above; a non-founder in any org gets 404 before the handler runs. routeManifest.ts:151 and a repo-wide grep show this is the router's only mount, so there is no unguarded second path to it. The endpoint is a platform-treasury read, not a tenant read: it groups financial_ledger by the five allocation buckets with no per-org output, and financial_ledger.organizationId is explicitly nullable for exactly the platform-level postings (founder bucket transfers, platform credits) that these balances are made of — scoping it to one org would produce a wrong reserve balance, not a safer one. The handler takes no request input at all (signature is `_req`), so there is no parameter a caller could aim at another tenant.",
    "server/routes-finance-ledger.ts::GET /contribution-margin::financialLedger": "Founder financial surface; reads across orgs deliberately. Verified 2026-09-04: mounted as app.use('/api/founder/finance', isAuthenticated, getOrCreateOrg, requireFounder, financeLedgerRouter).",
    "server/routes-finance-ledger.ts::GET /cost-mix": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Same single mount and same verified requireFounder gate as /buckets (server/routes.ts:1830). The only request input read is `req.query.days`, clamped to 1..365 — there is no org, id, or filter parameter a caller could point at another tenant. The response is a platform cost aggregate by feature/provider (byCategory), containing no org-identifying rows; org-attributed cost is served by the separate /contribution-margin endpoint. Aggregating opex_spent across all orgs is the definition of the founder's platform cost mix.",
    "server/routes-finance-ledger.ts::GET /mrr::financialLedger": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Verified guard at the mount, not inferred from the path: the whole router is app.use'd behind isAuthenticated + getOrCreateOrg + requireFounder (server/routes.ts:1829-1830), and requireFounder (server/auth/clerkAuth.ts:342-355) 404s any request whose identity fails isFounderIdentity. The endpoint is by definition a platform-wide financial aggregate (trailing-30d MRR, 90-day trend, revenue split by organizations.subscriptionTier) — org-scoping it would make it meaningless. The leftJoin to organizations is a join condition, and the router is imported/mounted in exactly one place.",
    "server/routes-finance-ledger.ts::computeTrailing30dMrrCents": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Founder-only by an mount guard I verified, not by route naming. The entire router is mounted once in server/routes.ts with `isAuthenticated, getOrCreateOrg, requireFounder`, and requireFounder (server/auth/clerkAuth.ts:342) is a real guard that 404s anyone whose email/userId fails isFounderIdentity. The helper is module-private and its two callers (GET /mrr line 145, GET /triggers/active line 439) both live inside that same guarded router. The aggregate is intentionally platform-wide: it sums AcreOS's OWN subscription revenue across all orgs to drive the founder's scale-up trigger ladder, which is meaningless if scoped to one org. A non-founder in org A cannot reach the endpoint at all, so there is no request that leaks org B's revenue.",
    "server/routes-finance-ledger.ts::rangeAgg": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). rangeAgg is a local closure inside the GET /contribution-margin handler of the finance-ledger router, which server/routes.ts mounts as app.use('/api/founder/finance', isAuthenticated, getOrCreateOrg, requireFounder, financeLedgerRouter). requireFounder (server/auth/clerkAuth.ts:342) 404s any caller whose email/Clerk id fails isFounderIdentity, so no tenant caller can reach it. The platform-wide financial_ledger aggregation is the point of the surface — it computes AcreOS's own revenue vs opex contribution margin across all orgs, so an organizationId predicate would defeat it. The same handler goes on to break the result out per org via .groupBy(financialLedger.organizationId), which is only meaningful unscoped.",
    "server/routes-founder-appeals.ts::POST /api/founder/appeals/:id/resolve": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Verified guard, not inferred from the path: the route carries isAuthenticated + requireFounder inline in its own registration, and requireFounder is the canonical middleware from server/auth (re-exported from server/auth/clerkAuth.ts:342) which 404s anyone failing isFounderIdentity({email,userId}). The whole point of this surface is a cross-org founder review queue for Pax refusal appeals, so scoping the lookup to the founder's own org would break it; the appeal's own organizationId is read off the row and used for the outcome email. No non-founder principal can reach it.",
    "server/routes-founder-bridge.ts::GET /": "Founder-only surface. Cross-org by design. Verified 2026-09-04: mounted in server/routes.ts as app.use('/api/founder/bridge', isAuthenticated, requireFounder, founderBridgeRouter).",
    "server/routes-founder-chat.ts::GET /threads": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Two independent mechanisms, both verified in code. (1) The router is mounted with isAuthenticated + getOrCreateOrg + requireFounder inside registerFounderChatRoutes (line 569) — the clerkAuth.ts:342 middleware, not the logging-only app.use at server/routes.ts:1882, which merely calls next(). (2) Even without that gate the query cannot cross a tenant: it filters on eq(aiConversations.userId, userId) where userId comes from getUserId(req) (the authenticated caller's own id), so it returns only rows the caller wrote. A caller in org A has no parameter to name another user or org — the handler reads no req.params, req.query or req.body at all. Omitting organizationId is therefore not reachable as a leak; the row set is already narrower than the org.",
    "server/routes-founder-chat.ts::GET /threads/:id/messages": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Two independent mechanisms. (1) The whole router is mounted behind requireFounder at line 569-575 of the same file — verified in server/auth/clerkAuth.ts:342, which 404s unless isFounderIdentity({email,userId}) passes, so no tenant user reaches this path at all. (2) Even setting the guard aside, the parent aiConversations row is ownership-checked by userId in the SAME handler and the child aiMessages query is keyed on that verified threadId, so a caller can only read messages of a thread they own. The only scoping omission is that the parent check uses userId rather than organizationId — intentional, since these are founder-scope threads (scope='founder'). No request in org A can name a threadId belonging to org B and get rows back: the aiConversations lookup returns nothing and the handler 404s before aiMessages is queried.",
    "server/routes-founder-chat.ts::POST /stream": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). The router is mounted in exactly one place (grep for 'routes-founder-chat' across server/ shows only routes.ts:1888 plus a manifest entry and comments), and that mount applies isAuthenticated + getOrCreateOrg + requireFounder before the router. So the client-supplied threadId — which is genuinely unvalidated against aiConversations.organizationId/userId before the aiMessages insert and the history select — is only reachable by the founder identity, the single privileged principal. No caller in org A can read or write org B's rows through it. Worth noting as hygiene (a founder typo on threadId writes into an arbitrary org's conversation), but it is not a tenant-isolation defect.",
    "server/routes-founder-cockpit.ts::fetchCurrentLetter": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). fetchCurrentLetter has exactly one call site — inside the cockpit router's GET \"/\" handler (line 117) — and server/routes.ts mounts that router as app.use('/api/founder/cockpit', isAuthenticated, requireFounder, founderCockpitRouter). requireFounder from clerkAuth.ts 404s non-founders, so only the founder identity can reach the read. The row fetched is the single most recent platform-level 'founder_letter_generated' agent event (the monthly founder letter written by AcreOS's own pipeline about AcreOS), not tenant content, so an organizationId predicate has no correct value to take. Note the parameter is even named _req and unused — the function reads no request state at all.",
    "server/routes-founder-compliance.ts::GET /api/founder/compliance/fair-lending": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Genuinely cross-tenant by design and gated. fair_lending_audit_runs.organizationId is NOT NULL (shared/schema.ts:8463-8478) so the table is org-scoped, and this handler deliberately reads every org's rows to build a platform-wide compliance rollup. requireFounder is present in the registration argument list itself (verified guard, server/auth/clerkAuth.ts:342 → 404 unless isFounderIdentity). registerFounderComplianceRoutes is called once, at server/routes.ts:2587, and grep finds no second registration of this path. A tenant caller gets 404 from the middleware before the handler body runs.",
    "server/routes-founder-compliance.ts::GET /api/founder/pricing-experiments": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Platform-ops aggregate behind a verified founder guard. pricing_experiments carries a nullable organizationId (shared/schema.ts:8436-8455), and the query intentionally aggregates assignment/conversion counts across every org and every anonymous visitor — an experiment readout is meaningless per-tenant. requireFounder sits in the registration middleware chain and 404s any non-founder. Note also the projection selects only experimentKey/variantKey plus COUNT/SUM aggregates grouped by those two columns — no per-org row or identifier is emitted even to the founder.",
    "server/routes-founder-financials.ts::GET /api/founder/financials/subscription-events": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Cross-tenant by design (the founder financials page needs the platform-wide signup/upgrade/churn stream) and gated by requireFounder in the registration argument list. subscription_events.organizationId exists (shared/schema.ts:8256) so the lint is right that the table is org-scoped and the query names no org, but every caller that reaches the handler has already passed isFounderIdentity; a tenant request receives 404 from the middleware. registerFounderFinancialsRoutes is called once, at server/routes.ts:2506, and grep finds no duplicate/unguarded registration of this path.",
    "server/routes-founder-inspector-finance.ts::GET /api/founder/inspector/cost-event/:id": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). The route spreads `...guards`, defined at line 272 as [isAuthenticated, getOrCreateOrg, requireFounder], with requireFounder imported from ./auth/clerkAuth (the canonical 404-ing founder check). This is the founder cost-provenance lens: it deliberately walks a financial_ledger row into whichever org's messages / mail_shipments / ai_call_log row produced it, which is a cross-org read by design and cannot be scoped to the founder's own org. Read-only, founder-gated, no customer-reachable path.",
    "server/routes-founder-inspector.ts::GET /api/founder/inspector/agent/:codename": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Founder-only provenance lens over platform-level agent state. company_agents has no organizationId column at all (shared/schema.ts:14882 — codename is the unique key), so the agent lookup is not org-scoped data; the child reads (trustEvolutionLog, decisionsInboxItems, agentActionLog, agentPromptEvolutions, agentLlmTraces) are all keyed on agentCodename and deliberately span orgs, which is the point of an agent-lifetime view. decisionsInboxItems does carry organizationId (shared/schema.ts ~13080), so cross-org rows are genuinely returned — that is the designed founder view, and requireFounder (imported directly from ./auth/clerkAuth at line 29) is in the registration argument list, 404ing every non-founder before the handler body executes. Only one registration of this path exists (server/routes.ts:2028).",
    "server/routes-founder-inspector.ts::GET /api/founder/inspector/decision/:id": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). A bare-id read of decisions_inbox_items (which does carry organizationId, shared/schema.ts ~13080) plus its LLM traces and founder_audit rows — genuinely unscoped, and correctly so: this is the founder's decision-provenance drill-down across the whole platform, and the founder_audit join is by definition founder-side data. requireFounder is present in the registration argument list (line 160) and is the real guard from server/auth/clerkAuth.ts:342, which returns 404 unless isFounderIdentity({email,userId}) matches FOUNDER_EMAILS/FOUNDER_USER_IDS. A tenant user in org A calling GET /api/founder/inspector/decision/<org-B id> is rejected by the middleware with 404 before the findFirst runs. Single registration site confirmed by grep.",
    "server/routes-founder-intelligence.ts::GET /audit-log/explain-by-decision/:decisionId": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Founder-only by design, guard verified at two layers. The mount in server/routes.ts:1800 is `app.use('/api/founder/intelligence', isAuthenticated, requireFounder, founderIntelRouter)` and the route itself repeats `requireFounder`, whose in-file body 404s anyone failing `isFounderIdentity`. Additionally this is a resolver lookup: it reads decisionsInboxItems solely to resolve `resolvedByActionLogId` and hand it to explainAction, i.e. it establishes which record a decision points at. `isFounderIdentity` is fail-closed — FOUNDER_EMAILS/FOUNDER_USER_IDS are built from env and both sub-checks return false when empty, and clerkAuth.ts contains no dev bypass. No non-founder request can reach the bare-id read.",
    "server/routes-founder-intelligence.ts::GET /automation": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Founder-only platform-ops roll-up. Guard verified: mount-layer `isAuthenticated, requireFounder` (routes.ts:1800) plus per-route `requireFounder`. Every query is a deliberate cross-tenant COUNT aggregate over activityLog / supportTickets / decisionsInboxItems / jobHealthLogs — the endpoint's entire purpose is the platform-wide autonomy score, so it cannot be org-scoped. It returns only integer counts and env-credential booleans, never a tenant row, so there is no request by which org A reads an org B record.",
    "server/routes-founder-intelligence.ts::GET /business-intelligence": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Founder-only company-wide BI. Guard verified at the mount (routes.ts:1800) and on the route. The queries are intentionally whole-platform: SUM of monthly_price_cents across ALL organizations (that is the company's ARR/MRR), a count of cancellations, and a GROUP BY riskBand over churnRiskScores. Scoping these to the caller's org would make the numbers meaningless. Output is aggregates plus a risk-band histogram — no per-tenant row identifiers are returned.",
    "server/routes-founder-intelligence.ts::GET /company-agents/:codename/detail": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Founder-only internal-agent profile; guard verified at the mount (routes.ts:1800) and on the route. The subject is AcreOS's own company agents, not tenant data: agentActionLog and trustEvolutionLog have NO organizationId column at all (checked in shared/schema.ts), so they are not org-scoped tables. The one org-scoped table touched, decisionsInboxItems, is filtered by ownerAgentCodename — attribution to an internal agent, which is inherently cross-tenant and only reachable by a founder.",
    "server/routes-founder-intelligence.ts::GET /decision-log": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Founder-only autonomy audit log; guard verified at the mount (routes.ts:1800) and on the route. The unscoped read of decisionsInboxItems over a time window is the deliberate design: this is the founder's record of every decision the autonomous executor made across the whole platform in their absence. Only a founder identity passes requireFounder, and that check is fail-closed on empty env, so no customer session can retrieve another org's decision rows here.",
    "server/routes-founder-intelligence.ts::GET /decision-log/:id": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Bare-id read of an org-scoped table, but founder-only and therefore unscoped by design — it is the expand-row detail for the platform-wide /decision-log above. Guard verified twice: mount-layer `isAuthenticated, requireFounder` in server/routes.ts:1800, and `requireFounder` in the route registration itself. Only server/routes.ts mounts this router (grep for 'routes-founder-intelligence' returns just routes.ts:1800 and routeManifest.ts), so there is no alternate unguarded path to it.",
    "server/routes-founder-intelligence.ts::GET /growth": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Founder-only platform analytics, guarded twice. The router is mounted in server/routes.ts:1801 behind `isAuthenticated, requireFounder` (server/auth/clerkAuth.ts:342, which 404s unless `isFounderIdentity` matches), and the route itself carries a second in-file `requireFounder` (lines 56-63). Both fail closed: `isFounderIdentity` (server/services/founder.ts:98) only returns true on an explicit FOUNDER_EMAILS / FOUNDER_USER_IDS match, so an empty env denies everyone. The queries are deliberately cross-org roll-ups — a tier histogram over `organizations` and upgrade/downgrade/conversion counts over `subscriptionEvents` — which is exactly what a platform-wide growth metric means; scoping them to one org would make the number wrong. No request parameter selects an org, so there is no shape in which a non-founder caller reaches the handler at all.",
    "server/routes-founder-intelligence.ts::GET /morning-briefing": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Founder-only briefing behind the same double guard (mount-level `isAuthenticated, requireFounder` at server/routes.ts:1801 plus the in-file `requireFounder` on line 72). The three flagged queries are platform-scoped by design: a count of ALL pending `decisions_inbox_items` (the founder's single cross-org inbox — `organizationId` on that table is a nullable provenance pointer, not a tenant partition for this surface), a 24h `agent_action_log` roll-up grouped by agent codename, and `trust_evolution_log` promotion suggestions. `agent_action_log` and `trust_evolution_log` have no `organization_id` column at all (shared/schema.ts:15033, :15053), so there is nothing to scope. The handler takes no org-selecting input — no body, no :id, no query param feeds a WHERE clause — so no request from org A can steer it at a row of org B.",
    "server/routes-founder-intelligence.ts::GET /mrr": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Platform revenue roll-up, founder-only via the mount guard (server/routes.ts:1801) plus the in-file `requireFounder` on line 578. `payments` is genuinely org-scoped (`organizationId ... .notNull()`, shared/schema.ts:1701) and this handler sums it with no org predicate — but that is the point of a platform MRR series, and the only caller who can reach it is the platform operator. The single request input is `req.query.months`, a loop bound; nothing in the request names or selects an organization, so there is no concrete request by which a non-founder, or a founder acting as org A, reads org B's rows through this route. (Separate from tenancy and out of scope for this audit: `payments` is the borrower-note payments table — `noteId ... .notNull()` — so this series measures customer note collections, not subscription revenue to AcreOS. That is a correctness question about the metric, not an isolation defect.)",
    "server/routes-founder-intelligence.ts::POST /company-briefing": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Founder-only company status synthesis, double-guarded (server/routes.ts:1801 mount + in-file `requireFounder` on line 2957). Every read is an intentional platform aggregate: org counts over `organizations`, an open-ticket count over `supportTickets`, a critical-alert count over `systemAlerts`, the top-10 pending `decisions_inbox_items`, and `jobHealthLogs`. The cache table it reads and writes, `companyBriefingCache`, has no `organization_id` column at all (shared/schema.ts:15024) — it is a global singleton cache, which is coherent precisely because the only reader is the founder. The only request input is `req.body.forceRefresh`, a boolean; nothing in the request selects an org, so no cross-tenant read or write can be steered from a request. The cached briefing is likewise only ever served back through this same founder-guarded route.",
    "server/routes-founder-intelligence.ts::POST /decision-log/:id/reverse": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). This is the one entry where the bare-id shape is real — the row is fetched by `eq(decisionsInboxItems.id, id)` and updated by the same bare predicate, with no organizationId in either query, and `decisions_inbox_items` does carry a nullable `organization_id` (shared/schema.ts:13079). It is nonetheless SAFE because the only principal who can reach the route is the founder: the router is mounted at server/routes.ts:1801 behind `isAuthenticated, requireFounder`, and the route repeats the in-file `requireFounder` (lines 56-63) on line 2672. Both guards resolve through `isFounderIdentity` (server/services/founder.ts:98), which returns true only on an explicit FOUNDER_EMAILS / FOUNDER_USER_IDS match and therefore denies every tenant user with a 404. The decisions inbox is a single cross-org founder queue by construction — org-scoping this write would break the surface, not secure it. I could not construct any request from an org-A session that reaches this handler, because a non-founder session is 404'd before the handler body runs.",
    "server/routes-founder-intelligence.ts::POST /decisions-inbox/:id/approve": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Double-gated, both verified. The mount in routes.ts:1801 is app.use('/api/founder/intelligence', isAuthenticated, requireFounder, founderIntelRouter) using the canonical requireFounder imported at routes.ts:9, and the route itself carries a second, file-local requireFounder (line 56) that runs the same isFounderIdentity({email,userId}) check and 404s otherwise. The decisions inbox is the founder's cross-org queue, so resolving decisionsInboxItems by bare id is the intended semantics — item.organizationId is read off the row and passed onward to recordFounderPrecedent rather than used as a filter. No non-founder caller can reach the handler.",
    "server/routes-founder-intelligence.ts::POST /decisions-inbox/:id/override": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Founder-only by two independent gates, both verified in code. The router is mounted in server/routes.ts:1800 as app.use('/api/founder/intelligence', isAuthenticated, requireFounder, founderIntelRouter) where requireFounder resolves to server/auth/clerkAuth.ts:342 (isFounderIdentity -> env FOUNDER_EMAILS / FOUNDER_USER_IDS, 404s everyone else), and the route itself carries a second in-file requireFounder (routes-founder-intelligence.ts:56) with the same isFounderIdentity check. The decisions inbox is the founder's single cross-org queue by design — item.organizationId is carried FORWARD into recordFounderPrecedent as data about which org the decision concerned, it is not a caller scope. No non-founder session can reach the bare eq(decisionsInboxItems.id, id) read.",
    "server/routes-founder-intelligence.ts::POST /decisions-inbox/:id/reject": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Same double founder gate as the override sibling: mounted behind isAuthenticated + requireFounder at server/routes.ts:1800-1801 and carrying the in-file requireFounder (line 56) on the route itself. The bare eq(decisionsInboxItems.id, id) read is the founder's own cross-org inbox lookup; item.organizationId is passed forward to recordFounderPrecedent as metadata about the decision's subject org, never used as (or needed as) a caller scope. There is no non-founder request that reaches this handler — requireFounder 404s before the handler body runs.",
    "server/routes-founder-intelligence.ts::POST /decisions-inbox/purge": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). A deliberate platform-ops sweep over the founder's cross-org decision queue, double-guarded (server/routes.ts:1801 mount + in-file `requireFounder` on line 1493). It bulk-updates or hard-deletes `decisions_inbox_items` selected only by status, age cutoff and optional itemType — no org predicate — which is correct for a queue that spans all orgs and is read by exactly one principal. The destructive path is real (`db.delete(...)` when `hardDelete` is true), so the guard is load-bearing, and it holds: both `requireFounder` layers 404 anyone `isFounderIdentity` does not match, and that function only matches explicit FOUNDER_EMAILS / FOUNDER_USER_IDS entries — it fails closed on an unset env rather than open. There is no request parameter by which an org-A caller could name org B's rows, and no org-A caller can reach the handler at all.",
    "server/routes-founder-pulse.ts::loadReservesLive": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). loadReservesLive has one call site, line 328, inside GET /api/founder/pulse, which is registered with isAuthenticated, getOrCreateOrg, requireFounder (the real clerkAuth.ts guard, imported at line 20). The function measures AcreOS's own constitutional reserve floor: reserve-bucket balances plus trailing-90-day revenue summed across every org, which is exactly the denominator the floor rule needs. Scoping either query to a tenant would produce a wrong number, and no tenant caller can reach it. The helper getBucketBalance (server/services/financial-ledger.ts:330) is likewise a platform-level bucket aggregation by design.",
    "server/routes-founder-recourse.ts::POST /api/founder/recourse/:id/dismiss": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). The handler resolves recourseDrafts by bare id with no org predicate, but the route registration carries requireFounder as middleware (line 359), and that middleware is the real one exported from server/auth (server/auth/index.ts re-exports server/auth/clerkAuth.ts:342), which 404s unless isFounderIdentity({email, userId}) is true against FOUNDER_EMAILS/FOUNDER_USER_IDS. req.user is DB-sourced by hydrateUser (email taken from Clerk, not from the request), so the gate is not client-influenceable and fails closed when neither env list matches. Only the founder can reach any draft, so no org-A caller can touch an org-B row.",
    "server/routes-founder-recourse.ts::POST /api/founder/recourse/:id/draft": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Same mechanism as the dismiss route: the bare-id read of recourseDrafts (and the downstream generateDraftReply(id)) sits behind the verified requireFounder middleware in the registration itself (line 197). No non-founder session reaches the handler at all — requireFounder returns 404 before the handler body runs — so cross-tenant reach is impossible. I confirmed no second, unguarded registration of this path exists anywhere else in server/ (grep across non-test files returned nothing outside this file).",
    "server/routes-founder-recourse.ts::POST /api/founder/recourse/:id/send": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). The route is registered with isAuthenticated + requireFounder inline (server/auth/clerkAuth.ts:342, isFounderIdentity, 404 for non-founders); registerFounderRecourseRoutes(app) is called at server/routes.ts:2342. The recourse queue is deliberately cross-org — GET /api/founder/recourse returns 'open drafts (status=draft) across all orgs' — so a bare eq(recourseDrafts.id, id) read is the design, not a leak. row.organizationId is then used FORWARD (resolveRecipient's org-owner fallback, sendRegisteredEmail's organizationId, the log line) to decide who the reply goes to; it is not a caller scope. Idempotence is additionally protected by the status guard and the conditional update WHERE and(id, status='draft').",
    "server/routes-leads.ts::DELETE /api/leads/:id": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The row is ownership-checked before the bare-id write. `storage.getLead(org.id, leadId)` is org-scoped in its implementation (server/storage/leadRepo.ts:222-226: `.where(and(eq(leads.organizationId, orgId), eq(leads.id, id)))`) and a miss returns 404, so by the time `db.update(leads)...where(eq(leads.id, leadId))` runs, leadId has been proven to belong to org.id. A caller in org A passing org B's lead id gets `Errors.notFound` at line 749. The lint matched the bare `eq(leads.id, leadId)` on the update, not an unscoped lookup.",
    "server/routes-leads.ts::PATCH /api/leads/:id/restore": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The handler's own lookup already carries the org predicate — `and(eq(leads.id, leadId), eq(leads.organizationId, org.id))` — and 404s on a miss, so the subsequent `where(eq(leads.id, leadId))` update can only reach the row just proven to belong to org.id. A PATCH from org A on org B's soft-deleted lead id returns 404 at line 790. The lint matched the bare id on the UPDATE, not on the resolving SELECT.",
    "server/routes-legal-holds.ts::GET /api/founder/legal-holds": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). The listing is deliberately cross-org (a platform-ops view of every FRCP 37(e) hold), and the handler's FIRST statement is an in-handler founder gate: `if (!requireFounder(areq, res)) return;` (line 77). That local requireFounder calls isFounderIdentity({email, userId}) against the Clerk-derived identity and returns 404 otherwise, so a non-founder never reaches the db.select(). The gate is a real early return, not a comment.",
    "server/routes-legal-holds.ts::GET /api/founder/legal-holds/active": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Identical in-handler founder gate (line 93) executes before the query; the where clause filters on status/releasedAt only because the surface is the platform-wide active-hold roster, which is by definition not org-scoped. Non-founders get Errors.notFound and never reach db.select(). Verified isFounderIdentity is a real server-side check (server/services/founder.ts:98-103) reading req.user hydrated from the DB/Clerk, not from client input.",
    "server/routes-legal-holds.ts::POST /api/founder/legal-holds/:id/release": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Guarded by an in-handler founder check that returns before any query: `if (!requireFounder(areq, res)) return;`, where the file-local requireFounder (line 48) calls isFounderIdentity({email, userId}) from server/services/founder.ts:98 and 404s otherwise. Legal holds are an inherently cross-org platform-ops instrument (POST takes organizationId in the body; GET lists all holds), so resolving a hold by bare id is correct — row.organizationId is read out of the fetched row and used forward for the audit target and log, never as a caller scope. No non-founder session reaches the eq(legalHolds.id, req.params.id) read.",
    "server/routes-lifecycle.ts::GET /api/founder/nps/recent": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). The unfiltered read of npsMicroSurveys across all orgs is the point of the route (founder-wide NPS roll-up), and the registration carries the real requireFounder middleware imported from ./auth (line 25 import, line 103 in the middleware chain), which 404s any non-founder before the handler runs. getOrCreateOrg preceding it only hydrates req.organization; the authorization is requireFounder. No duplicate unguarded registration of this path exists elsewhere in server/.",
    "server/routes-lot-basis.ts::POST /api/lots/:childId/realize-cogs": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The allocation row is resolved by `and(eq(lotBasisAllocations.childParcelId, childId), eq(lotBasisAllocations.organizationId, orgId))` and 404s on miss, so the flagged `update(lotBasisAllocations).where(eq(lotBasisAllocations.id, alloc.id))` is keyed on the id of the row that predicate returned — `alloc.id` never comes from the request. The second write, the properties flip to sold, carries `eq(properties.organizationId, orgId)` explicitly. A caller in org A posting org B's lot id gets 404 before either write. Chain is isAuthenticated + getOrCreateOrg (registerLotBasisRoutes at server/routes.ts:2396).",
    "server/routes-lot-pricing.ts::POST /api/parcels/:id/pricing-rules/lock": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The ruleset is resolved by `and(eq(lotPricingRules.organizationId, orgId), eq(lotPricingRules.parentParcelId, parentId))` and 404s on miss, so the flagged `update(lotPricingRules).where(eq(lotPricingRules.id, rules.id))` uses the id of that org-owned row, not request input. Every other query in the unit is scoped: the per-lot listPrice writes carry `eq(properties.organizationId, orgId)`; `deriveBasePerAcreCents(orgId, parentId, ...)` reads the parent with `and(eq(properties.id, parentId), eq(properties.organizationId, orgId))`; `loadChildFacts(orgId, parentId)` filters on `eq(properties.organizationId, orgId)`; and the later snapshot-link update adds the org predicate back. The `overrides` body map cannot introduce a foreign lot because it is only consulted by `overrides[String(row.childParcelId)]` over rows produced from loadChildFacts. Chain is isAuthenticated + getOrCreateOrg (registerLotPricingRoutes at server/routes.ts:2398).",
    "server/routes-lot-pricing.ts::PUT /api/parcels/:id/pricing-rules": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The only bare-id write in the handler targets `existing.id`, and `existing` was selected two statements earlier in the SAME handler under `eq(lotPricingRules.organizationId, orgId)` + `eq(lotPricingRules.parentParcelId, parentId)`. A caller in org A therefore cannot reach an org-B ruleset: a foreign parentId simply finds no `existing` and falls through to the INSERT, which stamps `organizationId: orgId` (the row created is org A's own, and every reader in this file — GET, preview, lock, loadChildFacts, deriveBasePerAcreCents — filters on organizationId). Verified-parent misfire, not a defect.",
    "server/routes-marketplace.ts::GET /investors/:id": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). investor_profiles is a cross-org marketplace directory by design, not a tenant-private table: the table is declared \"Investor Profiles - Public profiles for marketplace trust\" with its columns grouped under \"// Public info\" (shared/schema/marketplace.ts:128-137), and the sibling GET /investors (line 362-378) already returns EVERY org's profile with no organization predicate at all. The detail lookup therefore discloses nothing the directory listing does not, and access is gated at the mount (server/routes.ts:1373 `app.use('/api/marketplace', isAuthenticated, getOrCreateOrg, requireLadderFlag(\"feature_marketplace\"), marketplaceRouter)`). Non-tenancy caveat worth a separate ticket: `profile: investorProfiles` selects the whole row, so `verificationDocuments` (KYC document references) is serialized to every marketplace org — a data-minimization defect present identically in the list endpoint, not a tenant-isolation break.",
    "server/routes-misc.ts::GET /api/investor-profiles/directory": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Cross-org by design: this is the marketplace directory of self-authored investor profiles, gated on isVerified=true, and the rows it returns are content the owning org itself published via POST /api/investor-profiles. The containment mechanism is a real explicit column projection (not select()) that emits only the schema's \"Public info\", \"Specialization\" and \"Reputation\" blocks — organizationId, verificationDocuments (identity-document URLs written by /verify) and lastActiveAt are all excluded, matching the marked blocks in shared/schema/marketplace.ts:129-158. So a caller in org A reads no private column of org B; org-scoping the query would destroy the feature.",
    "server/routes-misc.ts::POST /api/webhooks/twilio/recording-status": "Same shape and same reason as sms-status.",
    "server/routes-misc.ts::POST /api/webhooks/twilio/sms-status": "Signature-verified Twilio callback. No session exists to carry an org; the update is keyed on messages.externalId, the SID Twilio itself issued, which is globally unique and unguessable. Scoping it would need an org the caller cannot supply.",
    "server/routes-misc.ts::POST /api/webhooks/twilio/sms::organizationIntegrations": "Inbound SMS webhook resolving WHICH org owns the receiving number — the lookup that establishes the org, so it cannot itself be org-scoped.",
    "server/routes-ml-snapshots.ts::GET /api/founder/ml-snapshots/recent": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). The cross-org read of ml_training_snapshots (an org-scoped table: shared/schema.ts:6155 `organizationId: integer(\"organization_id\")...`) is gated by an in-handler founder check that returns before any query. `requireFounder()` at routes-ml-snapshots.ts:27-36 resolves the identity from the Clerk session/user and calls `isFounderIdentity()` (server/services/founder.ts:98-103 → email in FOUNDER_EMAILS or userId in FOUNDER_USER_IDS, both env-driven and empty by default), replying 404 otherwise; `if (!requireFounder(areq, res)) return;` runs before `db.select().from(mlTrainingSnapshots)`. A non-founder in org A gets 404, never rows. Route is registered once (server/routes.ts:2611) with no competing earlier definition of the path.",
    "server/routes-move-inspections.ts::POST /api/inspections/:id/reconcile-deposit": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). Both bare-id writes target rows this handler already ownership-checked. `insp` is read under `eq(moveInspections.id, req.params.id)` + `eq(moveInspections.organizationId, orgId)` (404 otherwise), and `deposit` under `eq(securityDeposits.leaseId, insp.leaseId)` + `eq(securityDeposits.organizationId, orgId)` (404 otherwise); the two updates are then keyed on `deposit.id` and `insp.id`. An org-A caller passing an org-B inspection id 404s at the first select. The downstream `startDepositClock({ organizationId: orgId, leaseId: insp.leaseId, ... })` is org-scoped internally too (`eq(moveInspections.organizationId, args.organizationId)`, `and(eq(rentalLeases.id, args.leaseId), eq(rentalLeases.organizationId, args.organizationId))` in server/services/rental/depositClock.ts), so the unverified `leaseId` accepted at inspection-create time cannot pull another org's lease.",
    "server/routes-note-acquisitions.ts::POST /api/note-acquisitions/:id/promote": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The acquisition row is resolved first with both predicates and 404s on miss, so `req.params.id` is a proven-owned id by the time the flagged `update(noteAcquisitions).where(eq(noteAcquisitions.id, req.params.id))` runs. The intervening insert into acquiredNotes stamps `organizationId: orgId`, and the payer/address fields it copies come from the already-ownership-checked `acq` row — no data crosses orgs. Mounted with isAuthenticated + getOrCreateOrg + requireRole([\"owner\",\"admin\"]) (registerNoteAcquisitionRoutes at server/routes.ts:2370).",
    "server/routes-notes.ts::GET /api/notes/:id/basis::notePayments": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). Verified-parent pattern. The handler first reads the parent acquiredNotes row with BOTH eq(acquiredNotes.id, id) AND eq(acquiredNotes.organizationId, orgId) and 404s when it misses, so `id` is proven to belong to the session org before it is used. notePayments.noteId is a notNull FK to acquiredNotes.id (shared/schema/notes-vertical.ts:291), so keying the child on that already-verified id cannot cross tenants. orgId comes from getOrganizationId(req), which reads req.organization set by the membership-verified getOrCreateOrg middleware — a caller in org A gets 404 on org B's note id and never reaches the payments query.",
    "server/routes-notes.ts::GET /api/notes/:id/payments::notePayments": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). Verified-parent pattern, and the handler says so in a comment it actually honours. The ownership probe on acquiredNotes carries eq(acquiredNotes.organizationId, orgId) in the same handler and returns 404 before the ledger read; the notePayments query is keyed on that same verified `id` via the notNull FK notePayments.noteId -> acquiredNotes.id. Route chain is isAuthenticated + getOrCreateOrg + ownerOrAdmin, so orgId is the session org, not client-supplied.",
    "server/routes-notes.ts::GET /api/notes/:id/reconciliation::notePayments": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). Verified-parent pattern. Both flagged notePayments reads (the four-bucket SUM and the last-posting lookup) run only after the acquiredNotes row is fetched with eq(acquiredNotes.organizationId, orgId) and `if (!note) return Errors.notFound(res, \"Note\")`. Requesting another org's note id 404s before any SUM executes, so no cross-tenant totals or lastPostingId leak.",
    "server/routes-notes.ts::GET /api/notes/:id/yield::notePayments": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). Verified-parent pattern, identical shape to /basis. The full acquiredNotes row is selected under and(eq(acquiredNotes.id, id), eq(acquiredNotes.organizationId, orgId)) with a 404 on miss, and the payment history is then keyed on that verified note id through the notNull FK. Nothing in the handler accepts an org or note id from the body/query that would bypass the check.",
    "server/routes-notes.ts::POST /api/loss-mit-cases/:id/actions": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The unit has exactly three statements. The first resolves the case with `and(eq(noteLossMitCases.id, req.params.id), eq(noteLossMitCases.organizationId, orgId))` and 404s on miss; the second inserts the action stamping `organizationId: orgId`; the third — the flagged bare-id `update(noteLossMitCases).where(eq(noteLossMitCases.id, req.params.id))` — is keyed on exactly the id that check just proved belongs to orgId, and is unreachable for a foreign id because the handler already returned. Mounted with isAuthenticated + getOrCreateOrg + requireRole([\"owner\",\"admin\"]).",
    "server/routes-notes.ts::POST /api/notes/:id/payments": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The only bare-id resolution is the balance/schedule write `tx.update(acquiredNotes)...where(eq(acquiredNotes.id, id))`, and `id` is the SAME `req.params.id` that was already resolved inside the same transaction by a SELECT ... FOR UPDATE carrying `eq(acquiredNotes.organizationId, orgId)`, returning 404 when it misses. A caller in org A passing org B's note id gets `not_found` before any write. The other two statements in the unit are the org-scoped FOR UPDATE read and an insert that stamps `organizationId: orgId` on the ledger row. Route is mounted with isAuthenticated + getOrCreateOrg + requireRole([\"owner\",\"admin\"]) (registerNoteRoutes called at server/routes.ts:2368). Separately noted, not a tenancy defect: `originalPaymentId` is stored unvalidated, but it is only ever dereferenced through the org-scoped map in server/services/form1098Batch.ts:1234, so a cross-org value resolves to null rather than leaking.",
    "server/routes-notes.ts::computeAcquiredNotePayoff::notePayments": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). Verified-parent pattern inside a helper that takes orgId as a required parameter. It resolves the note under and(eq(acquiredNotes.id, noteId), eq(acquiredNotes.organizationId, orgId)) and returns null (callers turn into 404) before either notePayments query runs; both are keyed on that verified noteId. I checked every call site in the repo — routes-notes.ts:2744 (GET /api/notes/:id/payoff) and :2803 (POST /api/notes/:id/payoff/quotes) — and both pass `const orgId = getOrganizationId(req)` under isAuthenticated + getOrCreateOrg + ownerOrAdmin, and both `if (!result) return Errors.notFound(res, \"Note\")`. There is no caller that passes an attacker-controlled orgId.",
    "server/routes-organization.ts::GET /api/founder/safety-status": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). The registration itself carries `requireFounder` (imported at routes-organization.ts:7 from ./auth/clerkAuth), which is the real middleware at server/auth/clerkAuth.ts:342-357 — it 404s unless `isFounderIdentity({ email: user.email, userId })` holds. The un-scoped `simulated_actions` sweep is deliberate platform-ops (the comment records that this endpoint previously carried only isAuthenticated + getOrCreateOrg and leaked other tenants' Lob payloads; the fix applied was the guard, not an org filter). The only org-derived values in the response (`isOrgSimulated(org)`, `orgFlag`) come from the caller's own `req.organization`.",
    "server/routes-organization.ts::GET /api/nps/pending": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The bare-id UPDATE marks shown_at on `queued.id`, and `queued` was selected one statement earlier under BOTH eq(npsPromptQueue.organizationId, orgId) AND eq(npsPromptQueue.userId, userId). Every other query in the handler (npsResponses day_14/upgrade/quarterly, subscriptionEvents) carries eq(...organizationId, orgId). No id enters from the request at all — the route takes no params and no body.",
    "server/routes-organization.ts::POST /api/organization/invitations/accept": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). This is a lookup that RESOLVES which org owns the invitation, so it cannot be org-scoped by construction: the caller has no org membership in the target org yet — that is the whole point of accepting. Authorization is the unguessable token (looked up by hashInviteToken, with a rate-limited legacy plaintext fallback that re-verifies via tokenMatchesRow), plus status/expiry checks and an email-match check; every subsequent write is keyed on invite.id / invite.organizationId derived from the token-resolved row, and the shadow-org delete is keyed on eq(orgsTable.ownerId, userId) (the caller's own org). Two things worth flagging to the owner as hardening, neither a tenant-isolation break by itself: (a) the email check is conditional — `if (userEmail && ...)` — so a signed-in account with no email on record skips it and could redeem any token it holds; (b) the shadow-org cleanup takes .where(eq(orgsTable.ownerId, userId)).limit(1) with no ordering and deletes it if leads/properties/deals/notes are all empty, which can delete a real (but empty) org the user owns.",
    "server/routes-organization.ts::POST /api/organization/invitations/accept::organizationInvitations": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). This is the lookup that RESOLVES which org an invitation belongs to, so it cannot be org-scoped: the accepting user is by definition not yet a member of the inviting org, and req has no getOrCreateOrg org for it. The authorization is a secret bearer token, not a tenant predicate — the row is found by hashInviteToken(plaintext) (or, on the legacy path, by exact plaintext plus tokenMatchesRow verification), then gated on status === 'pending', non-expired expiresAt, and invite.email matching the signed-in account, with a per-user 10/hr accept rate limit against token brute force. Every subsequent write derives its scope from the token-resolved invite.organizationId (teamMembers insert, audit log, the status update keyed on invite.id); the only other org query is the shadow-org cleanup keyed on eq(orgsTable.ownerId, userId), i.e. an org the caller owns. There is no request an org-A caller can make that reads or writes an org-B invitation without holding that org's invite token.",
    "server/routes-outreach-mail.ts::GET /api/outreach/mail/results/funnel/:shipmentId::mailShipmentPieces": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). Verified parent. The handler first reads the shipment with BOTH eq(mailShipments.id, shipmentId) AND eq(mailShipments.organizationId, orgId) and 404s when it misses, so shipmentId is proven to belong to the caller's org before any piece is read. mailShipmentPieces.shipmentId is a real FK to mailShipments.id (shared/schema/finance.ts:381 `.references(() => mailShipments.id, ...)`), so every row matched by that predicate belongs to the already-verified shipment. A caller in org A passing org B's shipmentId is rejected at the parent read with 404 before line 865 executes.",
    "server/routes-outreach-mail.ts::GET /api/outreach/mail/results/response-timeline/:shipmentId::mailQrScanEvents": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). Verified parent, same shape as the funnel route. The shipment is read under and(eq(mailShipments.id, shipmentId), eq(mailShipments.organizationId, orgId)) with a 404 on miss, and mailQrScanEvents.shipmentId is an FK to mailShipments.id (shared/schema/finance.ts:441-443), so the scan-event bucket query can only see rows hanging off the caller's own shipment. Nothing else in the handler resolves a row by a bare client-supplied id.",
    "server/routes-outreach-mail.ts::GET /api/outreach/mail/shipments::mailShipmentPieces": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). Verified parent set spread in from a local array. The id list is derived exclusively from the org-scoped shipments query (eq(mailShipments.organizationId, orgId)); `ids` is never client-supplied, and the handler returns early when the org owns no in-flight shipments. Both aggregate queries (stage counts at 713 and attribution at 737) use inArray(mailShipmentPieces.shipmentId, ids) over that derived list, so no row outside the caller's org can enter either aggregate. This is the lint's known array-variable blind spot.",
    "server/routes-outreach-mail.ts::POST /api/outreach/mail/cancel/:shipmentId": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). Textbook verified-parent: the shipment is read at line 622 with eq(mailShipments.organizationId, orgId) and a 404 on miss, and the bare-id UPDATE at line 640 is keyed on the identical idParam that was just ownership-checked. A cross-org shipmentId returns 404 before any write. The refunds also pass organizationId: orgId explicitly.",
    "server/routes-outreach-mail.ts::POST /api/outreach/mail/queue": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The only bare-id query is the QR-code backfill inside the same transaction, keyed on ids RETURNING'd from the mailShipmentPieces insert this handler just performed with organizationId: org.id. There is no request-supplied id anywhere in the handler — the audience is resolved via resolveAudience(org.id, ...), the shipment header carries organizationId: org.id, and the pieces carry shipmentId: row.id from the same tx. No org-B row is reachable.",
    "server/routes-pax-appeals.ts::POST /api/pax/appeals::paxDecisionAppeals": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). Verified parent. The refusal payload is read under and(eq(paxRefusalPayloads.id, refusalPayloadId), eq(paxRefusalPayloads.organizationId, organizationId)) with a 404 on miss, so refusalPayloadId is proven to be the caller's org's refusal before the duplicate-appeal check runs. paxDecisionAppeals.refusalPayloadId is an FK to paxRefusalPayloads.id (shared/schema/pax-decision-appeals.ts:78-80), so the only appeal rows that predicate can reach are appeals filed against the caller's own refusal. The disclosed fields ({appealId, status}) therefore describe the caller's own record. A cross-org refusalPayloadId is rejected at line 179 before line 186 executes.",
    "server/routes-pax-audit.ts::GET /api/founder/pax-audit/findings": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). `requireFounder` sits in the middleware chain of the registration (imported line 19 from ./auth, whose index.ts re-exports it verbatim from clerkAuth: `export { clerkMiddleware, isAuthenticated, requireFounder } from \"./clerkAuth\";`). pax_audit_runs is org-scoped (shared/schema/pax-audit.ts:49 `organizationId: integer(\"organization_id\")...`), so resolving a run by bare `run_id` and then its findings by that runId would be an IDOR — but only a founder identity can reach the handler at all, and a cross-org view is the endpoint's purpose (a platform-wide constitutional-drift audit). Registered once at server/routes.ts:2515; no earlier unguarded definition of the path exists.",
    "server/routes-pax-audit.ts::GET /api/founder/pax-audit/recent": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Same verified guard: the registration chain is `isAuthenticated, requireFounder` with requireFounder resolving through server/auth/index.ts to the 404-on-non-founder middleware at server/auth/clerkAuth.ts:342. The unscoped `paxAuditRuns` listing plus the `inArray(paxAuditFindings.runId, runIds)` roll-up is a platform-wide founder sweep across all orgs by design; the findings query is keyed on ids from the run list already inside the guarded handler, so it adds no reachable surface.",
    "server/routes-pax-calibration.ts::GET /api/founder/pax-calibration": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). The registration imports requireFounder directly from ./auth/clerkAuth (line 33) and places it in the chain after isAuthenticated/getOrCreateOrg, so a non-founder in any org gets 404 before the query runs. The pax_observations aggregate is deliberately platform-wide (a calibration curve over all Pax confidence signals); it projects only `confidenceScore` and `status` and returns bucket counts, never per-org rows. No request from an ordinary org A caller reaches it.",
    "server/routes-permit-tracker.ts::PATCH /api/permit-gates/:gateId": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The one query without an org predicate reads permitChecklists by `beforeGate.checklistId`, and `beforeGate` was fetched in the SAME handler under `eq(permitGates.id, gateId)` AND `eq(permitGates.organizationId, orgId)` — so a caller in org A passing an org-B gateId gets `beforeGate === undefined`, skips this branch entirely, and the subsequent UPDATE (which also carries `eq(permitGates.organizationId, orgId)`) matches nothing and 404s. The single column read (`templateKey`) is never returned to the caller; it only selects a static in-file TEMPLATE to compute expectedReturnAt. The two emit-path reads both carry `eq(permitChecklists.organizationId, orgId)` / `eq(permitGates.organizationId, orgId)`. permitGates/permitChecklists have no other write sites in the repo (grep outside this file and shared/schema returns nothing), so a gate's checklistId is always same-org.",
    "server/routes-platform-features.ts::GET /api/community/county-reviews": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Genuinely unscoped by design: this is the network-wide community pool (server/services/communityIntelligence.ts — \"Community Intelligence — county reviews...\"), where the whole point is reading reviews contributed by other orgs; an org filter would make the surface return only the caller's own reviews. Contribution is gated (POST requires trust score > 300), and the read is anonymized in the handler itself — the map projects state/county/rating/pros/cons/investorTrustScore/verifiedDeals/createdAt and deliberately drops organizationId, so no row is attributable to a tenant. The companion service aggregate is likewise cross-org by construction with a HAVING floor (COUNT(d.id) >= 3), never per-org rows.",
    "server/routes-public-sign.ts::GET /api/public/sign/:docId": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). The bare-id lookup is by design on a documented public signing surface with no session at all — the `organizationId` the lint saw is derived FROM the fetched document (doc.organizationId), not from a caller, so there is no org context to filter by. Authorization is the HMAC bound to (docId, signerId) plus membership in that document's signers roster plus the expiresAt check, so a caller cannot read another org's document without possessing that document's own signing token. The org lookup is precisely the resolve-which-org-owns-this read, and only org.name is projected.",
    "server/routes-public-sign.ts::GET /api/public/sign/consent/status": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Public, session-less signing surface documented in the file header (\"external signers have no AcreOS account and authenticate via an HMAC token in the URL\"); there is no req.organization to scope by. Access is cryptographically gated: verifySigningToken HMAC-SHA256s `${docId}:${signerId}` under the signing keyring and timingSafeEquals it, so docId cannot be walked without forging the key, and the signerId must additionally appear in that document's own signers roster. The response is boolean-only (required + disclosureVersion) — no document content, no PII, and the audit lookup is pinned to the token-proved documentId.",
    "server/routes-public-sign.ts::POST /api/public/sign/:docId": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Same documented session-less signing rail: the write is authorized by the HMAC over (docId, signerId), by the signer existing in that document's roster, and by the not-expired check — no caller-supplied organizationId exists, and the org written to signatures is taken from the token-proved document (organizationId: doc.organizationId). Every subsequent read/write is keyed on that same verified doc.id: the consent gate filters on eq(signingConsentAudit.documentId, doc.id), loadSigningProgress is called with doc.organizationId + doc.id, and updateGeneratedDocument targets doc.id. An attacker without a valid token for a given docId gets 403, so no org-B document can be signed or mutated.",
    "server/routes-public-sign.ts::POST /api/public/sign/:docId::signingConsentAudit": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Genuinely unscoped by design: this is the public external-signer surface, registered with NO isAuthenticated/getOrCreateOrg (server/routes.ts:1584 `registerPublicSignRoutes(app)`, deliberately mounted before the `/api` auth catch-all per the file header), so there is no session and no organizationId to scope by. Authorization is a signature: verifySigningToken(docId, signerId, token) is an HMAC-SHA256 over the literal string `${docId}:${signerId}` compared with crypto.timingSafeEqual, so a caller can only reach a docId whose token they were issued. The signingConsentAudit read is keyed on doc.id (that same token-bound document) plus the disclosure version and the signer's own email taken from the document's roster; it is an existence gate returning only {id}, never another org's data. The doc read itself is by bare id but is likewise gated by the per-(docId,signerId) HMAC.",
    "server/routes-public-sign.ts::POST /api/public/sign/consent": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Public unauthenticated e-sign surface, documented in the file header and in the registration comment in server/routes.ts (registered before the `app.use('/api', isAuthenticated, …)` catch-all on purpose). Authorization is an HMAC-SHA256 token over `${docId}:${signerId}` keyed by a server-side signing keyring (verifySigningToken, timingSafeEqual, unknown kid rejected) — an unauthenticated caller cannot forge a token for another org's document id. The bare-id read of generated_documents IS the lookup that RESOLVES which org owns the document: doc.organizationId is then used as the tenant key on the insert, and the signer must additionally appear in that document's own `signers` roster. Nothing about the org is caller-supplied, and no other org's rows are readable or writable.",
    "server/routes-quiet-title.ts::GET /api/quiet-title/cases/:id::quietTitleSteps": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). Verified parent. The case is read under and(eq(noteQuietTitleCases.id, req.params.id), eq(noteQuietTitleCases.organizationId, orgId)) and 404s on miss, so c.id is a case owned by the caller's org; the steps query is keyed on that verified c.id, not on the raw param. quietTitleSteps.caseId is a varchar carrying the parent's gen_random_uuid() primary key, and the only writer (POST /api/quiet-title/cases, line 134-140) sets caseId: c.id and organizationId: orgId together from the same org-scoped insert, with uniqueIndex(\"quiet_title_steps_case_step_uk\").on(caseId, stepKey) making a colliding foreign row impossible. Route is additionally behind isAuthenticated + getOrCreateOrg + requireRole([\"owner\",\"admin\"]).",
    "server/routes-rehab-photos.ts::POST /api/rehabs/:rehabId/photos": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The only bare-id resolve is the s3Key back-fill keyed on `row.id`, which is the id returned by the insert this same loop iteration just performed with `organizationId: orgId` — it is not attacker-controllable, it is a row the handler created. The path parameter `rehabId` is ownership-checked first (lines 97-101) with both `rehabs.id` and `rehabs.organizationId` and 404s on miss, so a caller in org A cannot attach a photo to org B's rehab. Side note checked and cleared: `lineItemId` comes from the body unvalidated, but `rehabPhotos` has exactly one consumer file in the repo and its read (lines 209-214) is org-scoped, so a foreign lineItemId cannot surface a row across tenants.",
    "server/routes-rehabs.ts::recomputeRehabSpend": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The bare-id write `where(eq(rehabs.id, rehabId))` is only ever reached with a rehab id already ownership-checked in the calling handler. Two callers (POST /api/rehabs/:id/line-items line 392, POST /api/rehabs/:id/seed-template line 562) pass `r.id` from a select carrying `and(eq(rehabs.id, req.params.id), eq(rehabs.organizationId, orgId))`; the other two (PATCH/DELETE /api/rehab-line-items/:id lines 416/430) pass `updated.rehabId`/`deleted.rehabId` off a rehab_line_items row matched with `eq(rehabLineItems.organizationId, orgId)`, and rehabId is not in the PATCH's updatable key list, so it always names a rehab in the caller's org. The helper is module-private (no export) and grep shows no other call sites. Latent hardening note only: the SUM is org-scoped while the UPDATE is not, so if a future caller ever passed a foreign rehabId this would zero another org's spentTotalCents — adding eq(rehabs.organizationId, orgId) to the update would close that.",
    "server/routes-rentals.ts::POST /api/leases/:id/renew": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The bare-id UPDATE inside the transaction flips `parent.id`, and `parent` was read at the top of the same handler under `eq(rentalLeases.id, req.params.id)` + `eq(rentalLeases.organizationId, orgId)` with an explicit 404 on miss — an org-A caller passing an org-B lease id never reaches the transaction. Every other statement in the unit is scoped: the lead-paint property read carries `eq(properties.organizationId, orgId)`, the new lease/leaseTenants/leaseAddendums inserts stamp `organizationId: orgId`, the tenant-link read carries `eq(leaseTenants.organizationId, orgId)`, and `findOrCreateUnitId` filters on `eq(rentalUnits.organizationId, args.orgId)` with orgId passed from the request context.",
    "server/routes-rentals.ts::POST /api/tenants/:id/adverse-action": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The bare-id UPDATE writes `recent[0].id`, and `recent[0]` came from a select carrying `eq(tenantScreenings.organizationId, orgId)` in the same handler; with no org-A screening row for that tenant the handler 404s before any write. The mirrored tenant write is itself scoped (`and(eq(tenants.id, req.params.id), eq(tenants.organizationId, orgId))`), so passing another org's tenant uuid updates zero rows there. The gate `assertScreeningPermitted` also reads only under `eq(tenantScreenings.organizationId, opts.organizationId)` (server/services/fcraAttestation.ts:288-297). Worst case for a caller supplying a foreign tenant id is mutating org A's OWN screening row that references it — no org-B row is read or written.",
    "server/routes-rosy-river.ts::GET /api/founder/agent-notifications": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). `requireFounder` (line 28 import from ./auth → index.ts → clerkAuth.ts:342) is in the registration chain, so the three unscoped decisionsInboxItems queries (page, count, unread) are only reachable by a founder identity. The rows are further narrowed to `itemType = 'agent_event'` — the Rosy River agent feed the founder reviews — which is the platform-ops view this endpoint exists to serve. Registered once at server/routes.ts:2447; no earlier competing route for the path.",
    "server/routes-rosy-river.ts::GET /api/founder/proposed-changes": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Founder-only platform-ops read, and the guard is real and in the registration itself, not merely implied by the /api/founder path. The route is registered as app.get(path, isAuthenticated, requireFounder, handler); requireFounder is imported at line 28 from ./auth, which re-exports server/auth/clerkAuth.ts (server/auth/index.ts line 1), and that middleware 404s unless isFounderIdentity({email, userId}) passes. The unscoped agentTasks/decisionsInboxItems reads are the intended cross-org proposal queue for the founder; there is no non-founder path into the handler and no duplicate unguarded registration of this path anywhere in server/.",
    "server/routes-rosy-river.ts::PATCH /api/founder/agent-notifications/:id": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). The handler does update decisionsInboxItems by bare id with no organizationId predicate, so a non-founder reaching it could write another org's inbox row — but the registration carries isAuthenticated + requireFounder (the same verified middleware as above, which 404s any non-founder identity), so no caller in org A can ever enter the handler. This is a deliberate founder inbox-triage write across all orgs.",
    "server/routes-rosy-river.ts::PATCH /api/founder/proposed-changes/:id": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Same mechanism: the mount itself carries isAuthenticated + requireFounder. Every query inside (the agentTasks select by bare id, the agentTasks update by bare id, and the decisionsInboxItems select/update pairing) is intentionally cross-org because Rosy River proposals are reviewed by the founder for the whole platform. Reachable only by a verified founder identity.",
    "server/routes-subdivision-plans.ts::PATCH /api/plans/:planId": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). Both bare-id queries key off a row already ownership-checked in this handler. The UPDATE uses `existing.id`, where `existing` was read under `eq(subdivisionPlans.id, req.params.planId)` AND `eq(subdivisionPlans.organizationId, orgId)` — a foreign planId returns 404 before any write. The un-scoped `properties` read is keyed on `existing.parentParcelId`, a column of that verified row rather than caller input, and the sole insert path for subdivision_plans (POST /api/parcels/:id/plans, same file) proves the parcel belongs to the org before storing it (`.where(and(eq(properties.id, parentId), eq(properties.organizationId, orgId))); if (!parent) return Errors.notFound(res, \"Parcel\")`). Nothing from that read is returned to the caller — it only labels the workflow event emitted for `updated.organizationId`. Residual note (not a reachable defect): that read is the one query with no org predicate, so it is safe only by the FK invariant, not by its own where-clause.",
    "server/routes-support-saved-replies.ts::DELETE /:id": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Same verified guard as the PATCH: the router is mounted only once, at server/routes.ts:1907, behind `isAuthenticated, requireFounder`, which 404s any non-founder via isFounderIdentity (server/auth/clerkAuth.ts:342-355). The unit mentions no organization because the table is a founder-curated catalog whose rows are deliberately global (organization_id IS NULL) or founder-assigned to one org; deleting by bare id is the founder's own curation of that catalog, and no customer-org request can reach the handler.",
    "server/routes-support-saved-replies.ts::PATCH /:id": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Founder-only surface, guard verified at the mount rather than assumed from the path: server/routes.ts:1907 mounts the whole router behind `isAuthenticated, requireFounder`, and requireFounder (server/auth/clerkAuth.ts:342-355) 404s unless isFounderIdentity({email, userId}) passes. There is exactly one mount of savedRepliesRouter in the repo. supportSavedReplies is deliberately a founder-curated catalog (organization_id NULL = global, scoped rows = one customer org), so editing any row by bare id — including reassigning organizationId at line 81 — is the intended founder curation power, not a tenant crossing; no customer-org session can reach this handler at all.",
    "server/routes-support-tickets.ts::GET /api/founder/escalations::supportTickets": "Founder escalation queue; spans orgs by definition. Verified 2026-09-04: the authority check is IN-HANDLER (`if (!org.isFounder) return Errors.forbidden(...)`) rather than requireFounder middleware, which founderRouteGuardCoverage.test.ts accepts as mechanism 2 and matches on `isFounder\\b`. Not a gap, but the declarative form would be better if this route is ever touched.",
    "server/routes-support-tickets.ts::GET /api/founder/support/analytics": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). This file never imports requireFounder (line 5 imports only isAuthenticated), so the /api/founder prefix alone would be worthless — but the handler carries the in-handler founder check as its first statement and returns before any query runs. org.isFounder is the organizations.is_founder column (shared/schema.ts:97), set only by getOrCreateOrg from isFounderEmail(userEmail) (server/middleware/getOrCreateOrg.ts:75, 223, 276-285); a normal org can never self-set it. The four unscoped count/avg aggregates over supportTickets are the intended platform-wide support metrics.",
    "server/routes-support-tickets.ts::GET /api/founder/support/tickets": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Deliberate all-orgs founder read (the comment says so and the code matches), gated by the in-handler org.isFounder check that returns 403 before the query. Verified there is no second, unguarded registration of /api/founder/support/tickets anywhere in server/, and no blanket app.use('/api/founder', ...) authorization is relied on — the only /api/founder app.use calls in server/routes.ts are promptInjectionMiddleware and unrelated subpath routers.",
    "server/routes-support-tickets.ts::GET /api/support/tickets/:id": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The bare-id SELECT is immediately followed by an explicit in-handler org guard that 403s any cross-tenant read: `if (ticket.organizationId !== org.id && !org.isFounder) return Errors.forbidden(res)`. Nothing is returned to the caller before that check. The only bypass is `org.isFounder`, which is server-derived (getOrCreateOrg sets/upgrades it solely from `isFounderEmail(userEmail)`, middleware/getOrCreateOrg.ts:276-289; the column is organizations.is_founder, shared/schema.ts:97) and is not settable from a request. The follow-on `getTicketMessages(ticketId)` (supportAgent.ts:5701-5705, keyed on supportTicketMessages.ticketId) is a child of the already-verified ticket.",
    "server/routes-support-tickets.ts::POST /api/founder/escalations/:id/generate-prompt": "Founder-only, and cross-org by design. Verified 2026-09-04: the handler's first statements are `const org = req.organization!;` then `if (!org.isFounder) return Errors.forbidden(res, \"Founder access required\")` — the in-handler mechanism founderRouteGuardCoverage.test.ts accepts and matches on `isFounder\\b`. It reads the escalated ticket by id precisely because a founder triages tickets across tenants. It became visible to this lint on 2026-09-04: the paren walk desynchronised on the three-deep nested template literal in its prompt builder, so the registration never closed and the route was dropped from the population entirely.",
    "server/routes-support-tickets.ts::POST /api/founder/escalations/:id/resolve": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). The update targets supportTickets by bare id with no organizationId predicate — genuinely cross-org write — but the founder check is the first thing in the try block and returns 403 before req.params.id is even parsed. Note the contrast with this file's customer-facing ticket routes, which pair the same flag with an ownership test (ticket.organizationId !== org.id && !org.isFounder at lines 103, 153, 235, 280, 356); here the whole point is platform-wide escalation triage, so no org predicate is expected.",
    "server/routes-support-tickets.ts::POST /api/founder/escalations/batch-prompt::supportTickets": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). The unscoped inArray(supportTickets.id, ticketIds) read is reachable only after an in-handler founder check that returns 403 for every non-founder org. org.isFounder is the organizations.is_founder column, set by getOrCreateOrg from isFounderEmail(userEmail) (server/middleware/getOrCreateOrg.ts:75,223,276-289) and otherwise only settable via POST /api/admin/set-founder, which is itself behind isFounderAdmin -> isFounderIdentity (server/routes-admin.ts:724-741,1178). A non-founder caller never reaches the query, so no org A caller can read org B tickets here; the cross-org read is the point of the founder escalation triage surface.",
    "server/routes-support-tickets.ts::POST /api/support/tickets/:id/close": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The handler resolves only `{ organizationId }` by bare id, then 404s on miss and 403s on a foreign org before any write: `if (ticket.organizationId !== org.id && !org.isFounder) return Errors.forbidden(res)`. The `db.update(supportTickets)...where(eq(supportTickets.id, ticketId))` at line 276-285 therefore only reaches a ticket proven to belong to org.id (or a founder org, whose flag is server-derived from founder email, not request input). A POST from org A on org B's ticket id is rejected at line 272 before the update runs.",
    "server/routes-support-tickets.ts::POST /api/support/tickets/:id/messages": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). An explicit guard query runs first and is documented as having to run before any mutation: it selects only organizationId by bare id, 404s on miss, and 403s when `ticketForGuard.organizationId !== org.id && !org.isFounder` (line 153). Every later bare-id statement in the handler — the `prior` status read (line 168-172), the `db.insert(supportTicketMessages).values({ ticketId, ... })` at 188-192, and `processSupportChat(message, org, user.id, ticketId)` — is keyed on that verified ticket id. A caller in org A posting to org B's ticket is forbidden at 153 before the insert.",
    "server/routes-support-tickets.ts::POST /api/support/tickets/:id/pax-resolve": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). Same verified-row shape: `getOrganization(req)` yields the caller's org, the ticket is fetched by bare id, then 404/403-guarded with `if (ticket.organizationId !== org.id && !org.isFounder) return Errors.forbidden(res)` (line 235) before `resolveTicketWithPax(ticketId, org)` is invoked. The resolver is also handed the caller's org, so a cross-tenant id cannot reach it. The founder bypass is server-derived (organizations.is_founder, set only from isFounderEmail in getOrCreateOrg), not request-controlled.",
    "server/routes-support-tickets.ts::POST /api/support/tickets/:id/resolve-human": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The bare-id select at line 335-337 is immediately followed by an in-handler ownership comparison (line 348) that 403s when the ticket's organizationId differs from the caller's org, with a founder bypass via org.isFounder (set from isFounderEmail(userEmail) in server/middleware/getOrCreateOrg.ts:75,155). Every subsequent write is keyed on that same verified ticketId, and the paxMemory insert stamps ticket.organizationId. The lint misfired because the tenant check is a JS comparison, not a SQL predicate — the code comment at 342-347 records that this exact guard was added after a prior audit.",
    "server/routes-tax-researcher.ts::POST /listings/:id/bid-log": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The handler ownership-checks the listing up front with eq(taxSaleListings.organizationId, org.id) and 404s on miss; both bare-id UPDATEs (lines 929 and 960) are keyed on that same verified `id`. The bid-log INSERT carries organizationId: org.id, and the certificate handoff is passed organizationId: org.id. Router mount supplies the org: server/routes.ts:1394 `app.use('/api/tax-researcher', isAuthenticated, getOrCreateOrg, taxResearcherRouter);`",
    "server/routes-tax-researcher.ts::handoffWonLotToCertificate": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The only bare-id resolve in the function is the auction lookup keyed on `lot.auctionId`, an FK read off a listing row that was ownership-checked three statements earlier in the SAME function with `and(eq(taxSaleListings.id, listingId), eq(taxSaleListings.organizationId, organizationId))` (and the function returns listing_not_found otherwise). Every other query is org-scoped: the idempotency check, the natural-key re-read, and the insert all carry organizationId. The FK itself cannot be pointed at a foreign auction — the import path verifies the auction belongs to the org before hanging lots off it (line 334-346, \"Verify the auction belongs to this org\"). Both call sites (line 809 POST /listings/:id/won-certificate, line 935 POST /listings/:id/bid-log) pass the requester's own `getOrganizationId(req)` / `org.id`.",
    "server/routes-team-messaging.ts::PATCH /api/team-messaging/presence": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). There is no client-supplied id in this handler at all — the row is selected by `(organizationId, userId)` from the session, and `existing.id` in the update is the primary key of that very row. The insert branch writes `organizationId: org.id` and the session `userId`. Nothing in the request body names a row (`presenceSchema` accepts only `status` and `deviceInfo`), so a caller cannot address another org's presence row even in principle.",
    "server/routes-team-messaging.ts::POST /api/team-messaging/channels/:id/join": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The channel is resolved with a three-way predicate that includes `eq(teamConversations.organizationId, org.id)` and returns 404 on miss (line 1503), and the subsequent update is keyed on that exact same `channelId` — the id that was just ownership-checked in this handler. A caller in org A passing org B's channel id gets `Errors.notFound` before the update runs, so no cross-tenant participant injection is possible. (Non-tenancy nit, not a defect: `parseInt` with no `isNaN` guard means a non-numeric :id produces a driver error/500 rather than a 400.)",
    "server/routes-team-messaging.ts::POST /api/team-messaging/conversations/:id/messages": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The conversation is ownership-checked in this handler with `eq(teamConversations.organizationId, org.id)` plus a participant check, both of which return before any write. Everything downstream keys on that verified id: the `teamMessages` insert on `conversationId` (the table has no organizationId column at all — `teamMessages` is scoped solely through its conversation FK, see shared/schema.ts), and the `lastMessageAt` bump on the same verified id. The mention lookup and the notification inserts both carry `org.id` explicitly. No path lets org A write into org B's conversation.",
    "server/routes-team-readiness.ts::POST /api/team-readiness/offer-approvals/:id/decision": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). Two bare-id writes, both cleared. (1) `update(offerApprovals).where(eq(offerApprovals.id, id))` at line 547 keys on the id verified against `org.id` at lines 529-531, with a 404 on miss. (2) `update(offers).where(eq(offers.id, existing.offerId))` at 555/560 keys on a FK carried by that org-verified row rather than on the checked id itself, so I confirmed the FK's provenance: the ONLY writer of `offerApprovals.offerId` anywhere in the repo is server/routes-va-engine.ts:259, inside POST /api/offers, which sets `organizationId: org.id, offerId: offer.id` for an offer that same handler just created for that org; no route updates offerId, and there is no raw-SQL insert into offer_approvals. So `existing.offerId` is always an offer in `existing.organizationId`, and the status flip cannot reach another tenant's offer. Handler is additionally behind isAuthenticated + getOrCreateOrg + requireAdminOrAbove().",
    "server/routes-team-readiness.ts::POST /api/team-readiness/slack-integrations": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). This is an upsert keyed on `(organizationId, provider)` — matching the table's `org_integrations_slack_org_provider_idx` unique index — with no client-supplied row id anywhere; `slackIntegrationSchema` accepts only provider/webhookUrl/channelName/eventTypes/isActive. `existing.id` in the update branch is the primary key of the row just selected under `eq(orgIntegrationsSlack.organizationId, org.id)`, and the insert branch writes `organizationId: org.id`. A caller in org A has no way to name org B's integration row, so the webhook URL of another tenant cannot be overwritten.",
    "server/routes-title-partners.ts::GET /api/founder/title-partners/billing-report": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Cross-org by design and actually founder-gated in the handler body, not just by URL prefix: the first statement is `if (!req.isFounder) return Errors.forbidden(...)`. `req.isFounder` is set ONLY inside `hydrateUser` in server/auth/clerkAuth.ts (line 278: `if (isFounderIdentity({ email: user.email, userId })) { req.isFounder = true; }`) and by `requireFounder`; it is never derived from body/query, so a customer session leaves it undefined and gets 403. The report is a platform-wide accrual aggregate over title_orders/title_partners, which cannot be org-scoped and still be a billing report.",
    "server/routes-title-partners.ts::GET /api/founder/title-partners::titlePartners": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). The full-table read is guarded by an in-handler `if (!req.isFounder)` 403. req.isFounder is not attributable to any request-controlled input: it is set in exactly two places, both in server/auth/clerkAuth.ts — hydrateUser (line 279) after isFounderIdentity({email: user.email, userId}) on the verified Clerk session, and requireFounder (line 353). Since this is the registry of title partners across all tenants (organizationId NULL = platform default, per shared/schema/accounting-ops.ts:765-766), a founder-only cross-org listing is by design. The route is really mounted: server/routes.ts:2058-2059 imports and calls registerTitlePartnerRoutes(app).",
    "server/routes-title-partners.ts::POST /api/webhooks/title-orders/:orderId/status": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). A signature-verified partner webhook with no session and therefore no caller org to scope by — the bare eq(titleOrders.id, orderId) read is followed by an explicit ownership check before any write. The caller is identified by hashed API key against an ACTIVE partner row, then the raw body must pass HMAC-SHA256 (timingSafeEqual) against that partner's own decrypted secret, failing closed if the secret is missing/undecryptable. An assigned order rejects any partner but its own; an UNASSIGNED order rejects any org-scoped partner whose organizationId differs from order.organizationId (the SEC 2026-09-04 fix). So an org-A-private partner can never touch an org-B order: routeTitleOrder only ever assigns partners with organizationId NULL or = the ordering org, so order.titlePartnerId can never equal a foreign org's private partner id. The residual reach — a platform-wide partner (organizationId NULL) claiming an unassigned order in any org — is the documented contract of that column (platform defaults are routable by every org) and such partners can only be created by the founder-gated POST /api/founder/title-partners (`if (!req.isFounder) return Errors.forbidden(...)`). Worth noting but not a defect: the claim path checks org eligibility but not matchesTerritory(), which routeTitleOrder does check, so a platform-default partner outside the territory can claim an order it would never have been routed; the response body leaks nothing (`{ ok: true, orderId, status }`).",
    "server/routes-title-partners.ts::authenticatePartnerByApiKey": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). This is the credential-resolution lookup itself — it takes an inbound `Authorization: Bearer <apiKey>`, hashes it, and resolves WHICH title partner (and therefore which org context) the request belongs to. There is no session and no organizationId available before it runs, so it cannot be org-scoped without circularity. Its predicate is the secret itself (apiKeyHash) plus isActive, so a caller can only ever resolve the partner row whose API key they already hold. Both call sites use the result as the authenticated principal: requirePartnerApiKey 401s when it returns null, and the webhook path additionally requires an HMAC-SHA256 signature over the raw body verified with that partner's own decrypted secret, failing closed when the secret is missing or undecryptable.",
    "server/routes-title-partners.ts::routeTitleOrder::titlePartners": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The lint saw the broadcast branch's `where(eq(titlePartners.isActive, true))` with no organization predicate. The tenant predicate for that branch is an exhaustive in-process filter immediately after: a row is only ever selected if p.organizationId === organizationId (the caller's own org) or p.organizationId === null (platform default, which shared/schema/accounting-ops.ts:765-766 documents as reachable by any org); everything else falls through to `return null`. No other-tenant partner row can be returned, so nothing about it (webhookUrl, name, id) reaches the caller or receives the order payload. The org id is not spoofable: the sole production call site is POST /api/title-orders (line 314) passing orgId from getOrganizationId(req), and grep finds no other caller outside this file. The explicit-titleCompanyId branch (line 234) does carry the predicate in the statement.",
    "server/routes.ts::GET /api/founder/executive-dashboard::churnRiskScores": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Platform-ops aggregate behind a verified middleware guard in the registration itself: requireFounder sits in the mount chain and 404s anyone whose Clerk session fails isFounderIdentity({email, userId}). The rows are reduced to per-band counts and an average across active orgs — no per-tenant row is echoed. A caller in org A gets 404 before the query runs.",
    "server/routes.ts::GET /api/founder/executive-dashboard::deals": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Same handler, same guard: requireFounder is in the registration chain (verified against server/auth/clerkAuth.ts:342, imported at server/routes.ts:9 from ./auth, which re-exports it from ./clerkAuth). The query is a bare COUNT(*) over all deals reported as platformUsage.totalDeals — a deliberate platform-wide total for the founder revenue dashboard, returning no row data and no org identifiers.",
    "server/routes.ts::GET /api/founder/executive-dashboard::leads": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Same handler and same requireFounder middleware in the mount (server/routes.ts:2075), whose implementation returns 404 unless isFounderIdentity passes on the authenticated Clerk identity. The leads query is a bare COUNT(*) surfaced as platformUsage.totalLeads — an intentional platform-wide aggregate with no per-tenant rows in the response.",
    "server/routes.ts::GET /api/founder/executive-dashboard::npsResponses": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Founder-only platform aggregate. The guard is in the registration itself — `requireFounder` imported at routes.ts:9 from ./auth, which re-exports server/auth/clerkAuth.ts's middleware; that middleware 404s unless `isFounderIdentity({email, userId})` passes, so no tenant user ever reaches the handler. The whole endpoint is a fleet-wide roll-up (all orgs, MRR, platform lead/property/deal counts); an org predicate on npsResponses would defeat its purpose. The NPS rows are reduced to aggregate score/promoters/detractors, not returned per-row.",
    "server/routes.ts::GET /api/founder/executive-dashboard::properties": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Same handler, same guard (`requireFounder` in the registration, verified against server/auth/clerkAuth.ts:342). The properties read is a bare COUNT(*) used only as `platformUsage.totalProperties` — no row content, no id, no request-controlled parameter — and platform-wide usage is exactly what the founder surface is for. Deliberately unscoped.",
    "server/routes/lob-webhooks.ts::POST /api/webhooks/lob": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Signature-verified provider webhook with no session and no caller org. Lob's HMAC over `${timestamp}.${rawBody}` is verified against LOB_WEBHOOK_SECRET before any DB access, fails closed when the secret or raw body is absent (`if (!secret) return \"webhook_secret_not_configured\"`, `missing_raw_body`), and carries a 5-minute replay window. The piece is resolved by Lob's own `providerPieceId` — that read is precisely the lookup that determines WHICH org owns the piece (it selects organizationId out), so it cannot itself be org-scoped; the subsequent update is keyed on the resolved `piece.id`. There is no caller-supplied org or piece row id, and no cross-tenant read is returned (the response is `{received, applied}` only).",
    "server/routes/lob-webhooks.ts::POST /api/webhooks/lob::mailShipmentPieces": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Signature-verified webhook with no session, and the unscoped query is the ownership resolver. Every request is rejected before any DB access unless `verifyLobSignature` returns null (HMAC-SHA256 over `${timestamp}.${rawBody}` with LOB_WEBHOOK_SECRET, timingSafeEqual, 5-minute replay window, and a missing secret rejects everything). The SELECT keys on `providerPieceId` — Lob's globally-unique piece id, which is precisely what determines WHICH org owns the piece — so it cannot itself be org-scoped; it reads `organizationId` back out. The UPDATE is then keyed on `piece.id` from that resolved row, and every value written comes from the Lob event, not from any caller-supplied org. There is no session, so there is no 'caller in org A' to begin with.",
    "server/routes/public-qr-redirect.ts::GET /r/:code": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). A deliberately public, unauthenticated surface (a stranger scanning a printed postcard) whose design is documented in the file header, and simultaneously a lookup that RESOLVES which org owns the piece — so it cannot be org-scoped itself. It is not an oracle: forged/expired/typo codes and valid codes all end in the identical constant 302 (landAnonymously -> qrLandingUrl()), and no org name, lead, shipment or piece id reaches the body, headers or redirect target. Reachability is arithmetic, not enumerable: parseQrCode HMAC-verifies the code with timingSafeEqual before any query, and the piece read additionally requires the stored qrCode column to match, so an attacker cannot walk piece ids. Every write derives its organizationId from the resolved piece row (piece.organizationId / piece.shipmentId), and the update is keyed on that same verified piece id — nothing cross-tenant can be written. Raw IP/UA are never stored, only a truncated non-reversible HMAC fingerprint.",
    "server/routes/public-qr-redirect.ts::GET /r/:code::mailQrScanEvents": "Deliberately unauthenticated postcard-scan redirect (the file's header documents the threat model). The piece is resolved first from an HMAC-signed code and its organizationId is read from that row; the scan-event query is keyed on the resolved pieceId, so there is no org context to scope it by and the response is a constant 302 that reveals nothing either way.",
    "server/utils/auditLogChain.ts::chainAndInsertAuditLog": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The bare-id UPDATE targets `created.id` — the primary key returned by the INSERT performed two statements earlier in the same function. The id is generated by the database for this call, never supplied by a caller or a request, so no request can steer it at another org's row. The only other read, getPrevHashForOrg, is org-scoped via `eq(auditLog.organizationId, organizationId)` using `created.organizationId` (the org of the row just written), which is what keeps the chain per-tenant.",
    "server/utils/auditLogChain.ts::verifyAllOrgAuditLogChains::auditLog": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Platform-ops sweep, and the flagged query is the org enumerator. `selectDistinct({ organizationId })` from audit_log exists to discover WHICH orgs have chains — scoping it to one org would make the fleet-wide weekly verification impossible. The only production call site is the scheduled `audit_chain_verify` job (server/jobs/runScheduledJobs.ts:5614-5615); it is not reachable from any route, so no tenant request enters this function. The actual chain walk it delegates to IS org-scoped (`eq(auditLog.organizationId, organizationId)` at line 207), and the returned summary carries only counts, org ids and failure metadata for the founder alert — never another tenant's audit rows.",
    "server/utils/auditLogPurge.ts::sealAndPurgeAuditLogs": "LINT FALSE POSITIVE (triaged 2026-09-04, adversarial read). The flagged bare-id write is `eq(auditLogPurges.id, ledger.id)`, where ledger.id is the id returned by the insert of that same ledger row eight lines above, inside the same transaction — not a caller-supplied id. Every other statement is org-scoped: the candidate snapshot filters `eq(auditLog.organizationId, organizationId)`, the ledger insert stamps organizationId, and the DELETE re-asserts `and(eq(auditLog.organizationId, organizationId), inArray(auditLog.id, safeIds))` even though safeIds already came from the org-scoped snapshot. The single caller, auditRepo.purgeOldAuditLogs (server/storage/auditRepo.ts:122-134), passes its own orgId and short-circuits on orgHasActiveHold.",
    "server/webhookHandlers.ts::processBorrowerPaymentRefund::notes": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Same signature-verified Connect webhook (no session, no caller org). The notes SELECT ... FOR UPDATE and the optimistic-locked UPDATE are both keyed on `original.noteId` — the FK of the payments row that resolved ownership one step earlier from the globally-unique Stripe checkout-session id, and whose org was cross-checked against the session metadata. That FK pair is written consistently by the sibling handler (processBorrowerPortalPayment resolves the note via `storage.getNoteByAccessToken`, verifies `note.pendingCheckoutSessionId === session.id`, then writes `organizationId: note.organizationId, noteId: note.id` together), so payments.noteId cannot address a foreign org's note. Adding eq(notes.organizationId, original.organizationId) would be defence in depth, not a fix for a reachable cross-tenant path — I could not construct a request that steers this at another org's note.",
    "server/webhookHandlers.ts::processBorrowerPaymentRefund::payments": "SAFE BY DESIGN (triaged 2026-09-04, adversarial read). Signature-verified Stripe Connect webhook with no session, and the unscoped SELECT is the org resolver. Entry is stripeConnect.handleWebhookEvent case 'charge.refunded' (services/stripeConnect.ts:492), reached only from POST /api/stripe/connect/webhook (routes-billing.ts:1094) after stripe.webhooks.constructEvent verifies the raw body against STRIPE_CONNECT_WEBHOOK_SECRET — no caller can forge it. The ledger row is found by `payments.transactionId = session.id`, a Stripe-generated checkout-session id that is globally unique and not attacker-choosable, and the session itself was listed on the LENDER'S OWN connected account (`customerMoneyReadOptions({ accountId: connectedAccountId })`), so a lender can only surface sessions belonging to their own account. There is then an explicit cross-tenant refusal comparing the session's metadata org to the ledger row's org. The second payments read is keyed on `original.noteId` from that same resolved row, and every write uses `original.organizationId` — never a request-supplied org. I could construct no request producing a cross-org effect: to touch org B's ledger a lender would need a session id on their account that collides with org B's session id.",
    "server/storage/growthConfigRepo.ts::getBorrowerMessages": "READ 2026-09-04 (org_id widening). VERIFIED-PARENT, not a leak. `borrower_messages` carries `org_id NOT NULL` referencing organizations.id, and these three repo methods take a bare noteId — but every caller establishes the note first. The three LENDER routes (routes-admin.ts:383, :398, :423) each call `storage.getNote(org.id, noteId)` and 404 before the message read, and the BORROWER portal route (routes-borrower.ts:2092) uses `session.noteId` from the borrower's own note-bound session rather than a request parameter. Threading org into the repo methods would be an improvement in defence-in-depth and is worth doing; it is not a live cross-tenant read today. These were invisible until `org_id` entered this gate's table population.",
    "server/storage/growthConfigRepo.ts::markBorrowerMessagesRead": "READ 2026-09-04 (org_id widening). VERIFIED-PARENT, not a leak. `borrower_messages` carries `org_id NOT NULL` referencing organizations.id, and these three repo methods take a bare noteId — but every caller establishes the note first. The three LENDER routes (routes-admin.ts:383, :398, :423) each call `storage.getNote(org.id, noteId)` and 404 before the message read, and the BORROWER portal route (routes-borrower.ts:2092) uses `session.noteId` from the borrower's own note-bound session rather than a request parameter. Threading org into the repo methods would be an improvement in defence-in-depth and is worth doing; it is not a live cross-tenant read today. These were invisible until `org_id` entered this gate's table population.",
    "server/storage/growthConfigRepo.ts::countUnreadBorrowerMessages": "READ 2026-09-04 (org_id widening). VERIFIED-PARENT, not a leak. `borrower_messages` carries `org_id NOT NULL` referencing organizations.id, and these three repo methods take a bare noteId — but every caller establishes the note first. The three LENDER routes (routes-admin.ts:383, :398, :423) each call `storage.getNote(org.id, noteId)` and 404 before the message read, and the BORROWER portal route (routes-borrower.ts:2092) uses `session.noteId` from the borrower's own note-bound session rather than a request parameter. Threading org into the repo methods would be an improvement in defence-in-depth and is worth doing; it is not a live cross-tenant read today. These were invisible until `org_id` entered this gate's table population.",
    "server/services/marketplace.ts::getListing": "READ 2026-09-04 (org-FK widening). CROSS-ORG BY DESIGN — this is a MARKETPLACE. marketplace_listings / _bids / _transactions key their tenant by ROLE (seller_organization_id, bidder_organization_id, buyer_organization_id), which is why they were outside this gate's population until the NOT NULL org-FK arm landed. A listing only its owner can see is not a listing; a bid you can only place on your own property is not a bid. The authority question for this surface is not 'does the read name my org' but 'may I ACT on this row', and that is enforced at the mutations, all of which were tightened in the same commit: completeTransaction now requires an ACCEPTED BID from the caller's org and takes the sale price from it, respondToBid resolves the bid through an inner join predicated on the seller's org, and deactivateListing / upgradeToPremium / the premium rollback all name the seller in the UPDATE rather than only in a guard above it. getListing additionally enforces the visibility rule (non-public listings are returned only to the owning org). The whole surface is also behind requireLadderFlag(\"feature_marketplace\"), which fails closed, per the standing founder decision 'no marketplace before ~25 customers'.",
    "server/services/marketplace.ts::placeBid": "READ 2026-09-04 (org-FK widening). CROSS-ORG BY DESIGN — this is a MARKETPLACE. marketplace_listings / _bids / _transactions key their tenant by ROLE (seller_organization_id, bidder_organization_id, buyer_organization_id), which is why they were outside this gate's population until the NOT NULL org-FK arm landed. A listing only its owner can see is not a listing; a bid you can only place on your own property is not a bid. The authority question for this surface is not 'does the read name my org' but 'may I ACT on this row', and that is enforced at the mutations, all of which were tightened in the same commit: completeTransaction now requires an ACCEPTED BID from the caller's org and takes the sale price from it, respondToBid resolves the bid through an inner join predicated on the seller's org, and deactivateListing / upgradeToPremium / the premium rollback all name the seller in the UPDATE rather than only in a guard above it. getListing additionally enforces the visibility rule (non-public listings are returned only to the owning org). The whole surface is also behind requireLadderFlag(\"feature_marketplace\"), which fails closed, per the standing founder decision 'no marketplace before ~25 customers'.",
    "server/services/marketplace.ts::createListing::marketplaceListings": "READ 2026-09-04 (org-FK widening). CROSS-ORG BY DESIGN — this is a MARKETPLACE. marketplace_listings / _bids / _transactions key their tenant by ROLE (seller_organization_id, bidder_organization_id, buyer_organization_id), which is why they were outside this gate's population until the NOT NULL org-FK arm landed. A listing only its owner can see is not a listing; a bid you can only place on your own property is not a bid. The authority question for this surface is not 'does the read name my org' but 'may I ACT on this row', and that is enforced at the mutations, all of which were tightened in the same commit: completeTransaction now requires an ACCEPTED BID from the caller's org and takes the sale price from it, respondToBid resolves the bid through an inner join predicated on the seller's org, and deactivateListing / upgradeToPremium / the premium rollback all name the seller in the UPDATE rather than only in a guard above it. getListing additionally enforces the visibility rule (non-public listings are returned only to the owning org). The whole surface is also behind requireLadderFlag(\"feature_marketplace\"), which fails closed, per the standing founder decision 'no marketplace before ~25 customers'.",
    "server/services/marketplace.ts::placeBid::marketplaceBids": "READ 2026-09-04 (org-FK widening). CROSS-ORG BY DESIGN — this is a MARKETPLACE. marketplace_listings / _bids / _transactions key their tenant by ROLE (seller_organization_id, bidder_organization_id, buyer_organization_id), which is why they were outside this gate's population until the NOT NULL org-FK arm landed. A listing only its owner can see is not a listing; a bid you can only place on your own property is not a bid. The authority question for this surface is not 'does the read name my org' but 'may I ACT on this row', and that is enforced at the mutations, all of which were tightened in the same commit: completeTransaction now requires an ACCEPTED BID from the caller's org and takes the sale price from it, respondToBid resolves the bid through an inner join predicated on the seller's org, and deactivateListing / upgradeToPremium / the premium rollback all name the seller in the UPDATE rather than only in a guard above it. getListing additionally enforces the visibility rule (non-public listings are returned only to the owning org). The whole surface is also behind requireLadderFlag(\"feature_marketplace\"), which fails closed, per the standing founder decision 'no marketplace before ~25 customers'.",
    "server/services/marketplace.ts::getMarketplaceStats::marketplaceListings": "READ 2026-09-04 (org-FK widening). CROSS-ORG BY DESIGN — this is a MARKETPLACE. marketplace_listings / _bids / _transactions key their tenant by ROLE (seller_organization_id, bidder_organization_id, buyer_organization_id), which is why they were outside this gate's population until the NOT NULL org-FK arm landed. A listing only its owner can see is not a listing; a bid you can only place on your own property is not a bid. The authority question for this surface is not 'does the read name my org' but 'may I ACT on this row', and that is enforced at the mutations, all of which were tightened in the same commit: completeTransaction now requires an ACCEPTED BID from the caller's org and takes the sale price from it, respondToBid resolves the bid through an inner join predicated on the seller's org, and deactivateListing / upgradeToPremium / the premium rollback all name the seller in the UPDATE rather than only in a guard above it. getListing additionally enforces the visibility rule (non-public listings are returned only to the owning org). The whole surface is also behind requireLadderFlag(\"feature_marketplace\"), which fails closed, per the standing founder decision 'no marketplace before ~25 customers'.",
    "server/services/marketplace.ts::getMarketplaceStats::marketplaceTransactions": "READ 2026-09-04 (org-FK widening). CROSS-ORG BY DESIGN — this is a MARKETPLACE. marketplace_listings / _bids / _transactions key their tenant by ROLE (seller_organization_id, bidder_organization_id, buyer_organization_id), which is why they were outside this gate's population until the NOT NULL org-FK arm landed. A listing only its owner can see is not a listing; a bid you can only place on your own property is not a bid. The authority question for this surface is not 'does the read name my org' but 'may I ACT on this row', and that is enforced at the mutations, all of which were tightened in the same commit: completeTransaction now requires an ACCEPTED BID from the caller's org and takes the sale price from it, respondToBid resolves the bid through an inner join predicated on the seller's org, and deactivateListing / upgradeToPremium / the premium rollback all name the seller in the UPDATE rather than only in a guard above it. getListing additionally enforces the visibility rule (non-public listings are returned only to the owning org). The whole surface is also behind requireLadderFlag(\"feature_marketplace\"), which fails closed, per the standing founder decision 'no marketplace before ~25 customers'.",
    "server/services/matchmaking.ts::findMatchesForInvestor::marketplaceListings": "READ 2026-09-04 (org-FK widening). CROSS-ORG BY DESIGN — this is a MARKETPLACE. marketplace_listings / _bids / _transactions key their tenant by ROLE (seller_organization_id, bidder_organization_id, buyer_organization_id), which is why they were outside this gate's population until the NOT NULL org-FK arm landed. A listing only its owner can see is not a listing; a bid you can only place on your own property is not a bid. The authority question for this surface is not 'does the read name my org' but 'may I ACT on this row', and that is enforced at the mutations, all of which were tightened in the same commit: completeTransaction now requires an ACCEPTED BID from the caller's org and takes the sale price from it, respondToBid resolves the bid through an inner join predicated on the seller's org, and deactivateListing / upgradeToPremium / the premium rollback all name the seller in the UPDATE rather than only in a guard above it. getListing additionally enforces the visibility rule (non-public listings are returned only to the owning org). The whole surface is also behind requireLadderFlag(\"feature_marketplace\"), which fails closed, per the standing founder decision 'no marketplace before ~25 customers'.",
    "server/services/matchmaking.ts::getRecommendations::marketplaceListings": "READ 2026-09-04 (org-FK widening). CROSS-ORG BY DESIGN — this is a MARKETPLACE. marketplace_listings / _bids / _transactions key their tenant by ROLE (seller_organization_id, bidder_organization_id, buyer_organization_id), which is why they were outside this gate's population until the NOT NULL org-FK arm landed. A listing only its owner can see is not a listing; a bid you can only place on your own property is not a bid. The authority question for this surface is not 'does the read name my org' but 'may I ACT on this row', and that is enforced at the mutations, all of which were tightened in the same commit: completeTransaction now requires an ACCEPTED BID from the caller's org and takes the sale price from it, respondToBid resolves the bid through an inner join predicated on the seller's org, and deactivateListing / upgradeToPremium / the premium rollback all name the seller in the UPDATE rather than only in a guard above it. getListing additionally enforces the visibility rule (non-public listings are returned only to the owning org). The whole surface is also behind requireLadderFlag(\"feature_marketplace\"), which fails closed, per the standing founder decision 'no marketplace before ~25 customers'.",
    "server/services/matchmaking.ts::findBuyersForListing::investorProfiles": "READ AND FIXED 2026-09-04. The UNIT is now scoped — findBuyersForListing takes the seller's organizationId and resolves the listing with `and(eq(id), eq(sellerOrganizationId, org))`, so a listing that is not yours reads as absent, and the seller's own org is skipped as a buyer. The remaining unscoped chain is `db.query.investorProfiles.findMany({})`, and it is CORRECT: the buyers for your listing are by definition in other organizations, so a per-org predicate would make the feature return nothing. What was wrong was not the breadth of the read but what came back from it — the WHOLE profile row, `verificationDocuments` included: the counterparty's identity and accreditation paperwork, returned to whoever asked which buyers matched a listing. Matches now carry `publicInvestorFields()`, an explicit PICK (not a delete-list, so a column added later is private by default) of the columns the schema itself groups as public, with `isVerified` standing in for the documents behind it.",
    "server/services/autonomousDecisionExecutor.ts::buildSupportEscalationContext": "UNREAD as of 2026-09-04, surfaced by widening rule 2 to Drizzle's relational `where:` spelling. These four resolve supportTickets / systemAlerts / sequencePerformance by primary key inside a unit that has an organization. decisionsInbox and autonomousDecisionExecutor are the FOUNDER's cross-org decision machinery — two sibling methods in decisionsInbox were read earlier today and routed through unscopedForPlatformOps(reason) for exactly that reason — so the likely correct fix here is the hatch rather than an org predicate, but likely is not read. sequenceOptimizer.determineABTestWinner is the one to take first: sequence_performance carries no foreign key on sequence_id, and a sibling method in that same file was DELETED on 2026-09-04 because two orgs holding the same sequence_id would have read each other's A/B performance and cleared each other's winner.",
    "server/services/communityEnhancements.ts::gatherCaseStudyData": "READ 2026-09-04 (relational by-id widening). VERIFIED PARENT. The DEAL is resolved first with `and(eq(deals.id, dealId), eq(deals.organizationId, orgId))` and the function returns null when it is not the org's; the property is then read via `deal.propertyId`, so it is reachable only through a row already proved to belong to the caller.",
    "server/services/decisionsInbox.ts::createFromEscalation": "UNREAD as of 2026-09-04, surfaced by widening rule 2 to Drizzle's relational `where:` spelling. These four resolve supportTickets / systemAlerts / sequencePerformance by primary key inside a unit that has an organization. decisionsInbox and autonomousDecisionExecutor are the FOUNDER's cross-org decision machinery — two sibling methods in decisionsInbox were read earlier today and routed through unscopedForPlatformOps(reason) for exactly that reason — so the likely correct fix here is the hatch rather than an org predicate, but likely is not read. sequenceOptimizer.determineABTestWinner is the one to take first: sequence_performance carries no foreign key on sequence_id, and a sibling method in that same file was DELETED on 2026-09-04 because two orgs holding the same sequence_id would have read each other's A/B performance and cleared each other's winner.",
    "server/services/decisionsInbox.ts::createFromAlert": "UNREAD as of 2026-09-04, surfaced by widening rule 2 to Drizzle's relational `where:` spelling. These four resolve supportTickets / systemAlerts / sequencePerformance by primary key inside a unit that has an organization. decisionsInbox and autonomousDecisionExecutor are the FOUNDER's cross-org decision machinery — two sibling methods in decisionsInbox were read earlier today and routed through unscopedForPlatformOps(reason) for exactly that reason — so the likely correct fix here is the hatch rather than an org predicate, but likely is not read. sequenceOptimizer.determineABTestWinner is the one to take first: sequence_performance carries no foreign key on sequence_id, and a sibling method in that same file was DELETED on 2026-09-04 because two orgs holding the same sequence_id would have read each other's A/B performance and cleared each other's winner.",
    "server/services/financialOSService.ts::generateTaxReport": "READ 2026-09-04 (relational by-id widening). VERIFIED PARENT. The closed deals are selected with `eq(deals.organizationId, organizationId)`, and each property is read via that deal's own `propertyId` — the tax report can only describe transactions the organization owns.",
    "server/services/sequenceOptimizer.ts::determineABTestWinner": "UNREAD as of 2026-09-04, surfaced by widening rule 2 to Drizzle's relational `where:` spelling. These four resolve supportTickets / systemAlerts / sequencePerformance by primary key inside a unit that has an organization. decisionsInbox and autonomousDecisionExecutor are the FOUNDER's cross-org decision machinery — two sibling methods in decisionsInbox were read earlier today and routed through unscopedForPlatformOps(reason) for exactly that reason — so the likely correct fix here is the hatch rather than an org predicate, but likely is not read. sequenceOptimizer.determineABTestWinner is the one to take first: sequence_performance carries no foreign key on sequence_id, and a sibling method in that same file was DELETED on 2026-09-04 because two orgs holding the same sequence_id would have read each other's A/B performance and cleared each other's winner.",
    "server/services/eventMeshV12.ts::getRecentEvents": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). eventMeshV12.ts:291-297 — `async getRecentEvents(limit: number = 100): Promise<EventMeshEvent[]> {` / `return db` / `.select()` / `.from(eventMeshEvents)` / `.orderBy(desc(eventMeshEvents.createdAt))` / `.limit(Math.min(Math.max(limit, 1), 500));`. There is NO `.where(...)` at all: this is a bare `select()` returning WHOLE ROWS (payload jsonb + orgId) for every tenant, up to 500. The lint is right about the predicate — the unit touches an org-scoped table and never mentions orgId. TABLE SHAPE: shared/schema.ts:16280 `orgId: integer(\"org_id\"),` — NULLABLE, no `.references(() => organizations.id)`; migrations/0017_v12_real_runtime.sql:41 `org_id INTEGER, -- tenant scope`. A nullable tag, not a NOT NULL FK. IT DOES HOLD CUSTOMER DATA: …",
    "server/services/eventMeshV12.ts::getUnprocessedEvents": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). eventMeshV12.ts:168-179 — `const events = await db` / `.select()` / `.from(eventMeshEvents)` / `.where(` / `and(` / `like(eventMeshEvents.channel, pattern),` / `eq(eventMeshEvents.deadLettered, false),` / ``sql`NOT (${eventMeshEvents.ackedBy} @> ${JSON.stringify([subscriber])}::jsonb)`,`` / `),`. No org term; whole rows for every tenant. The `pattern` is not narrowing either — line 167 `const pattern = sub.channelPattern.replace(/\\*/g, \"%\");` and eventMeshDrain.ts:98 and :103 register `\"ws-bridge\"` and `\"notification-router\"` on the pattern `\"*\"`, which becomes the SQL wildcard `%`. (The first query, lines 151-159, reads eventMeshSubscriptions, which has NO org column at all — schema.ts:16302-16316 — so only the second statement is the flagged one.) …",
    "server/services/eventMeshV12.ts::getDeadLetterEvents": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). eventMeshV12.ts:244-251 — `async getDeadLetterEvents(limit: number = 50): Promise<EventMeshEvent[]> {` / `return db` / `.select()` / `.from(eventMeshEvents)` / `.where(eq(eventMeshEvents.deadLettered, true))` / `.orderBy(desc(eventMeshEvents.createdAt))` / `.limit(limit);`. The only predicate is `deadLettered = true` — no org term, whole rows (payload + orgId) across all tenants. REACHABILITY — founder only: sole caller is routes-founder-real-runtime.ts:105 `res.json(await eventMeshService.getDeadLetterEvents());` on `app.get(\"/api/founder/v12/events/dead-letter\", ...)` (line 104), behind routes-founder-real-runtime.ts:30 and routes.ts:2021. The handler takes `_req` and passes NO argument, so even `limit` is not caller-supplied here; it is the …",
    "server/services/eventMeshV12.ts::getEventsByChannel": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). eventMeshV12.ts:277-284 — `async getEventsByChannel(channel: string, limit: number = 50): Promise<EventMeshEvent[]> {` / `return db` / `.select()` / `.from(eventMeshEvents)` / `.where(eq(eventMeshEvents.channel, channel))` / `.orderBy(desc(eventMeshEvents.createdAt))` / `.limit(limit);`. `channel` IS caller-supplied — routes-founder-real-runtime.ts:110 `res.json(await eventMeshService.getEventsByChannel(req.params.channel));` on `app.get(\"/api/founder/v12/events/channel/:channel\", ...)` (line 109). It is the only caller. But `channel` is not a tenant selector and cannot be turned into one: every channel in the codebase is a hardcoded platform-wide constant — `\"deal:lifecycle\"`, `\"agent:decisions\"`, `\"agent:escalations\"`, `\"agent:conflicts\"`, …",
    "server/services/eventMeshV12.ts::getStats": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). eventMeshV12.ts:331-387. Seven queries, ALL aggregates — no row content leaves the method: line 340-342 ``.select({ count: sql<number>`count(*)::int` }).from(eventMeshEvents)``; 344-347 the same filtered by `eq(eventMeshEvents.deadLettered, true)`; 349-351 ``count(distinct ${eventMeshEvents.channel})::int``; 353-355 and 357-360 counts over eventMeshSubscriptions (a table with NO org column — schema.ts:16302-16316, so not the flagged one); 363-366 a count filtered `gte(eventMeshEvents.createdAt, fiveMinAgo)`; and 368-376 `topChannels`, `.groupBy(eventMeshEvents.channel).orderBy(sql\\`count(*) desc\\`).limit(10)`. The only non-numeric value returned is `channel`, and every channel is a hardcoded platform constant (see the getEventsByChannel entry) — no …",
    "server/services/eventMeshV12.ts::acknowledge": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). eventMeshV12.ts:200-224, three statements. READ, 201-205: `const [event] = await db` / `.select()` / `.from(eventMeshEvents)` / `.where(eq(eventMeshEvents.eventId, eventId))` / `.limit(1);` — resolves any tenant's event by bare id, no org term. WRITE, 211-214: `await db` / `.update(eventMeshEvents)` / `.set({ ackedBy })` / `.where(eq(eventMeshEvents.eventId, eventId));` — same bare id. Third statement, 217-223, updates eventMeshSubscriptions, which has no org column (schema.ts:16302-16316). `eventId` IS caller-supplied at the route: routes-founder-real-runtime.ts:95 `await eventMeshService.acknowledge(req.params.eventId, req.body.subscriber);` on `app.post(\"/api/founder/v12/events/:eventId/ack\", ...)` (line 94) — but founder-only …",
    "server/services/eventMeshV12.ts::deadLetter": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). eventMeshV12.ts:229-239 — `async deadLetter(eventId: string, reason: string): Promise<void> {` / `await db` / `.update(eventMeshEvents)` / `.set({` / `deadLettered: true,` / `deadLetterReason: reason,` / `})` / `.where(eq(eventMeshEvents.eventId, eventId));`. A write against a bare id with no org term — the lint reads it correctly. REACHABILITY — NO ROUTE AT ALL, unlike the other five platform ops. `grep -rn \"eventMeshService\" server/ client/ tests/ shared/ scripts/` excluding its own file returns exactly three files (eventMeshDrain.ts, eventMeshPublisher.ts, routes-founder-real-runtime.ts), and `.deadLetter` appears in only one of them: eventMeshDrain.ts:155 `await eventMeshService.deadLetter(event.eventId, eventErr.message);`. …",
    "server/services/eventMeshV12.ts::getEventsByType": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). eventMeshV12.ts:302-309 — `async getEventsByType(type: string, limit: number = 50): Promise<EventMeshEvent[]> {` / `return db` / `.select()` / `.from(eventMeshEvents)` / `.where(eq(eventMeshEvents.eventType, type))` / `.orderBy(desc(eventMeshEvents.createdAt))` / `.limit(limit);`. Whole rows (payload + orgId), one caller-supplied string predicate, no org term. PROOF OF NO CALLERS — repo-wide grep for the literal `getEventsByType` across *.ts/*.tsx/*.js/*.mjs/*.json/*.md, excluding node_modules and .git, returns FOUR hits and no more: (1) `./server/services/eventMeshV12.ts:302` — this definition; (2) `./scripts/org-scope-route-widening.json:32` — the register entry I am ruling on; (3) `./server/services/realtimeNervousSystemV10.ts:239: async …",
    "server/services/eventMeshV12.ts::replayEvents": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). eventMeshV12.ts:256-272 — `async replayEvents(` / `channel: string,` / `fromTimestamp: Date,` / `toTimestamp: Date,` / `): Promise<EventMeshEvent[]> {` / `return db` / `.select()` / `.from(eventMeshEvents)` / `.where(` / `and(` / `eq(eventMeshEvents.channel, channel),` / `gte(eventMeshEvents.createdAt, fromTimestamp),` / `lte(eventMeshEvents.createdAt, toTimestamp),` / `),` / `)` / `.orderBy(eventMeshEvents.createdAt);`. Note there is no `.limit(...)` on this one — a channel plus a wide time range returns every matching row for every tenant, whole rows including payload. PROOF OF NO CALLERS — repo-wide grep for `replayEvents` across *.ts/*.tsx/*.js/*.mjs/*.json/*.md excluding node_modules and .git returns exactly TWO hits: …",
    "server/services/outcomeVerificationV12.ts::verifyApiResponse": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). THE STATEMENT (lines 266-270): `const [execution] = await db.select()` / `.from(integrationExecutionLog)` / `.where(sql`${integrationExecutionLog.endpoint} = ${endpoint}`)` / `.orderBy(desc(integrationExecutionLog.createdAt))` / `.limit(1);` — there is NO lane term of any kind. CALLER-SUPPLIED KEY: `const endpoint = config.endpoint ?? \"unknown\";` (line 260), where `config` is the stored `verificationConfig` JSONB blob. This file's own doc comment (lines 140-143) states it \"arrives unvalidated from the request body of POST /api/founder/v12/verification/contracts\" — so yes, this id comes from a STORED JSON PAYLOAD. COLUMN SHAPE: shared/schema.ts:16422 `orgId: integer(\"org_id\"),` — NULLABLE TAG, no .notNull(), no .references(organizations.id). Non-null …",
    "server/services/outcomeVerificationV12.ts::verify": "LINT_FALSE_POSITIVE (triaged 2026-09-05, fan-out read + adversarial refutation). This is the rule-2 entry ('has an org and resolves by id anyway'). WHAT RULE 2 MATCHED: line 404 `.where(eq(outcomeVerificationContracts.id, contractId))` against LONE_ID_WHERE (`/where\\s*[(:]\\s*eq\\(\\s*([A-Za-z0-9_]+)\\s*\\.\\s*id\\s*,[^)]*\\)\\s*,?\\s*\\)?/g`, scripts/check-org-scoped-fetch.mjs:1795), plus an ORG_CONTEXT_RE hit somewhere in the unit. THE MISREAD: the only org token in this unit is `contract.orgId` on line 417 — `const result = await handler(contract.verificationConfig, contract.orgId ?? null);` — which is an OUTPUT OF THE VERY QUERY at 401-405, not an input available to constrain it. Rule 2's stated premise (mjs lines 1752-1758) is 'a method that ACCEPTS an organizationId and then resolves an org-scoped table by primary key anyway'. This …",
    "server/services/outcomeVerificationV12.ts::processVerifications": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). THE STATEMENT (lines 361-370): `const dueContracts = await db` / `.select()` / `.from(outcomeVerificationContracts)` / `.where(` / `and(` / `lte(outcomeVerificationContracts.nextVerificationAt, now),` / `isNull(outcomeVerificationContracts.completedAt),` / `),` / `)` / `.orderBy(outcomeVerificationContracts.nextVerificationAt);` — the predicate is due-time + not-completed, with no lane term, so it sweeps every lane (org-tagged rows and platform rows alike). It then calls `await this.verify(contract.id)` at line 379 for each. COLUMN SHAPE: shared/schema.ts:16338 `orgId: integer(\"org_id\"),` — NULLABLE TAG, no FK to organizations.id. REACHABILITY: exactly one caller in the repo — routes-founder-real-runtime.ts:143 `try { res.json(await …",
    "server/services/outcomeVerificationV12.ts::recordVerification": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). TWO STATEMENTS. Read, lines 429-433: `const [contract] = await db` / `.select()` / `.from(outcomeVerificationContracts)` / `.where(eq(outcomeVerificationContracts.id, contractId))` / `.limit(1);`. Write, lines 466-470: `const [updated] = await db` / `.update(outcomeVerificationContracts)` / `.set(updates)` / `.where(eq(outcomeVerificationContracts.id, contractId))` / `.returning();`. Both resolve by bare primary key with no lane term. REACHABILITY — SHOWN, NOT ASSERTED: the only call site of this METHOD in the repo is line 418, `return this.recordVerification(contractId, result.outcome, result.verified);`, inside verify(). A repo-wide grep for `recordVerification` outside this file returns only an unrelated pure function of the same name — `export …",
    "server/services/outcomeVerificationV12.ts::getDiscrepancies": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). THE STATEMENT (lines 484-488): `return db` / `.select()` / `.from(outcomeVerificationContracts)` / `.where(eq(outcomeVerificationContracts.discrepancyDetected, true))` / `.orderBy(desc(outcomeVerificationContracts.createdAt));` — the sole predicate is a boolean flag; no lane term, every tenant lane included. It is an UNPROJECTED `select()`, so WHOLE ROWS leave: `verificationConfig` (arbitrary JSONB written straight from `req.body` at line 138 via `createContract`), `claimedOutcome`, `actionDescription`, `discrepancyDetails`, `verifiedOutcome` and `orgId`. `verifiedOutcome` is not inert — for payment_status contracts it is the string built at line 224, `Deal/payment ${paymentId} status: ${actualStatus}`, so a customer's deal status is persisted there …",
    "server/services/outcomeVerificationV12.ts::getPendingVerifications": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). THE STATEMENT (lines 585-589): `return db` / `.select()` / `.from(outcomeVerificationContracts)` / `.where(isNull(outcomeVerificationContracts.completedAt))` / `.orderBy(outcomeVerificationContracts.nextVerificationAt);` — predicate is completion state only, no lane term, every tenant lane returned, and unprojected so WHOLE ROWS leave (same payload as getDiscrepancies: `verificationConfig`, `claimedOutcome`, `actionDescription`, `verifiedOutcome`, `orgId`). COLUMN SHAPE: shared/schema.ts:16338 `orgId: integer(\"org_id\"),` — NULLABLE TAG, no FK. REACHABILITY: one caller — routes-founder-real-runtime.ts:168 `try { res.json(await outcomeVerificationService.getPendingVerifications()); }` on GET /api/founder/v12/verification/pending (line 167). No client …",
    "server/services/outcomeVerificationV12.ts::getRecent": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). THE STATEMENT (lines 626-630): `return db` / `.select()` / `.from(outcomeVerificationContracts)` / `.orderBy(desc(outcomeVerificationContracts.createdAt))` / `.limit(limit);` — there is NO `.where(` at all. This is the newest N rows of the whole table across every lane, unprojected, so whole rows leave including `verificationConfig`, `claimedOutcome`, `actionDescription`, `verifiedOutcome` and `orgId`. Signature line 625: `async getRecent(limit: number = 50): Promise<OutcomeVerificationContract[]> {`. COLUMN SHAPE: shared/schema.ts:16338 `orgId: integer(\"org_id\"),` — NULLABLE TAG, no FK. CALLER-SUPPLIED INPUT: the only argument is `limit`, taken from the query string — routes-founder-real-runtime.ts:173 `try { res.json(await …",
    "server/services/outcomeVerificationV12.ts::getAgentVerificationStats": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). THE STATEMENT (lines 500-503): `const contracts = await db` / `.select()` / `.from(outcomeVerificationContracts)` / `.where(eq(outcomeVerificationContracts.agentCodename, agentCodename));` — filtered on agent codename only, no lane term, so it reads that agent's contracts in every tenant lane. WHAT LEAVES: nothing row-shaped. Lines 505-519 reduce the rows to four integers and return `{ totalActions, verified, discrepancies, accuracyRate }` — `const total = contracts.length;`, `const verified = contracts.filter((c) => c.verifiedOutcome !== null).length;`, `const discrepancies = contracts.filter((c) => c.discrepancyDetected).length;`. No `verificationConfig`, no `verifiedOutcome` text, no orgId crosses the boundary. COLUMN SHAPE: …",
    "server/services/outcomeVerificationV12.ts::getAllVerificationStats": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). THE STATEMENT (lines 531-534): `const allContracts = await db` / `.select()` / `.from(outcomeVerificationContracts)` / `.orderBy(desc(outcomeVerificationContracts.createdAt));` — no `.where(` and no `.limit(`; the entire table, every lane. The docstring at line 520 is 'Get system-wide verification stats.' WHAT LEAVES: aggregates only. Lines 536-560 produce `{ totalContracts, pending, completed, verified, discrepancies, overallAccuracyRate, byAgent, byMethod }`, where `byAgent` is keyed on `c.agentCodename` (a global platform identity, per lines 596-598) and `byMethod` on `c.verificationMethod` (one of the five literals in the VerificationMethod union, lines 20-26). No row text, no `verificationConfig`, no `orgId` reaches the response. COLUMN SHAPE: …",
    "server/services/integrationFrameworkV12.ts::execute": "DEFECT (triaged 2026-09-05, fan-out read + adversarial refutation). The two statements the lint flagged are fine, and the defect is one the register entry does NOT point at. Rule 2 fires on line 224 `.where(eq(integrationCredentials.id, cred.id))` and line 231 `.where(eq(integrationCredentials.id, cred.id))`; both write the row bound at 102-110, whose predicate is lane-scoped: `105 .where(and(` / `106 eq(integrationCredentials.serviceName, serviceName),` / `107 this.credentialLane(params?.orgId),` / `109 .orderBy(IntegrationFrameworkService.LANE_ORDER)`. Those are verified-parent. Line 143 is not. Inside the breaker branch: 132 if (cred.circuitBreakerOpen) { 134 if (cred.circuitBreakerResetAt && new Date() < cred.circuitBreakerResetAt) { ...return } 142 // Cooldown expired — auto-reset 143 await …",
    "server/services/integrationFrameworkV12.ts::rollbackExecution": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). Two bare-PK statements on an org-tagged table, with a caller-supplied id: 239 const [entry] = await db 240 .select() 241 .from(integrationExecutionLog) 242 .where(eq(integrationExecutionLog.id, executionId)); 249 const [updated] = await db.update(integrationExecutionLog) 250 .set({ rollbackExecuted: true }) 251 .where(eq(integrationExecutionLog.id, executionId)) 252 .returning(); The id IS caller-supplied — `server/routes-founder-real-runtime.ts:242 res.json(await integrationFrameworkService.rollbackExecution(parseInt(req.params.id)))` — and the route returns the whole `updated` row, i.e. `requestSummary` / `responseSummary`, which carry up to 500 chars of whatever the integration sent and received (logExecution 500-501). Why it is a rule-2 and not …",
    "server/services/integrationFrameworkV12.ts::getCredentials": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). Rule 1 is right that there is no organization anywhere in the unit: 269 const conditions = []; 270 if (serviceName) conditions.push(eq(integrationCredentials.serviceName, serviceName)); 272 const creds = await db 273 .select() 274 .from(integrationCredentials) 275 .where(conditions.length > 0 ? and(...conditions) : undefined) 276 .orderBy(desc(integrationCredentials.createdAt)); Called with no `serviceName` the predicate is literally `undefined` — every credential row in every lane. The secret does not leave: `278 return creds.map(({ encryptedValue, ...safe }) => safe);` strips it at runtime, so what ships is serviceName, credentialType, allowedAgents, the rate-limit and breaker counters, lastUsedAt and orgId. REACHABILITY: founder only. `GET …",
    "server/services/integrationFrameworkV12.ts::getCircuitBreakerStatus": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). 323 const creds = await db 324 .select() 325 .from(integrationCredentials) 326 .where(eq(integrationCredentials.circuitBreakerOpen, true)); One predicate, on a boolean; no org term, so rule 1 is reading it correctly. The response is a hand-built projection — `328-334 { serviceName, open, failures, threshold, resetsAt }` — carrying no secret and not even the orgId, so nothing that identifies a tenant crosses. REACHABILITY: founder only. `GET /api/founder/v12/integrations/circuit-breakers` — routes-founder-real-runtime.ts:251-252 `res.json(await integrationFrameworkService.getCircuitBreakerStatus())` — behind routes-founder-real-runtime.ts:29 + server/routes.ts:2021. Grep for `getCircuitBreakerStatus` across server/, client/, tests/, shared/, scripts/ …",
    "server/services/integrationFrameworkV12.ts::getCostReport": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). 399 const since = new Date(Date.now() - days * 24 * 60 * 60 * 1000); 401 const entries = await db 402 .select() 403 .from(integrationExecutionLog) 404 .where(gte(integrationExecutionLog.createdAt, since)); A time window and nothing else — rule 1 is correct that the unit names no organization. Whole rows are selected, but only `e.costCents`, `e.serviceName` and `e.agentCodename` are read (410-419), and the return value is aggregate-only: `byService` and `byAgent` totals plus `totalCostCents` (422-430). No row content — no requestSummary, no responseSummary, no endpoint — leaves the function. REACHABILITY: founder only. `GET /api/founder/v12/integrations/costs` — routes-founder-real-runtime.ts:261-262 `res.json(await …",
    "server/services/integrationFrameworkV12.ts::getStats": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). The most naked read in the file — no predicate at all: 446 const all = await db.select().from(integrationExecutionLog); 447 const today = all.filter(e => e.createdAt >= todayStart); 457 const openCircuits = await db 458 .select() 459 .from(integrationCredentials) 460 .where(eq(integrationCredentials.circuitBreakerOpen, true)); Rule 1 is right: no org token anywhere in 435-470. What is RETURNED is six scalars — totalCalls, callsToday, successRate, avgLatencyMs, totalCostCents, openCircuits (462-469). No row content escapes. REACHABILITY: founder only. `GET /api/founder/v12/integrations/stats` — routes-founder-real-runtime.ts:266-267 `res.json(await integrationFrameworkService.getStats())` — behind routes-founder-real-runtime.ts:29 + …",
    "server/services/integrationFrameworkV12.ts::consumeRateLimit": "VERIFIED_PARENT (triaged 2026-09-05, fan-out read + adversarial refutation). PARENT CHECK: the row is selected under the lane predicate in `execute`, at lines 105-108 — `105 .where(and(` / `106 eq(integrationCredentials.serviceName, serviceName),` / `107 this.credentialLane(params?.orgId),` / `108 ))` — with `109 .orderBy(IntegrationFrameworkService.LANE_ORDER)` making which lane wins deterministic. `credentialLane` is 74-81: `75 return orgId == null` / `76 ? isNull(integrationCredentials.orgId)` / `77 : or(eq(integrationCredentials.orgId, orgId), isNull(integrationCredentials.orgId))`. The method takes the ROW, not an id — `510 private async consumeRateLimit(cred: IntegrationCredential): Promise<boolean>` — so no caller-supplied identifier can reach either WHERE: 515 await db.update(integrationCredentials) 516 .set({ …",
    "server/services/integrationFrameworkV12.ts::incrementCircuitBreaker": "VERIFIED_PARENT (triaged 2026-09-05, fan-out read + adversarial refutation). Same parent as consumeRateLimit: the lane-scoped select in `execute` at 105-108 (`eq(integrationCredentials.serviceName, serviceName)` AND `this.credentialLane(params?.orgId)`) ordered by `LANE_ORDER` (84). Signature takes the ROW, not an id — `537 private async incrementCircuitBreaker(cred: IntegrationCredential): Promise<void>` — and the write is: 541 await db.update(integrationCredentials) 542 .set({ 543 circuitBreakerFailures: newFailures, 544 circuitBreakerOpen: shouldOpen, 545 circuitBreakerResetAt: shouldOpen ? new Date(Date.now() + 5 * 60 * 1000) : cred.circuitBreakerResetAt, 546 }) 547 .where(eq(integrationCredentials.id, cred.id)); One row, the one just used. No caller-supplied id reaches it. UNREACHABLE EXCEPT THROUGH THAT PARENT — shown: …",
    "server/services/cognitiveMemoryV13.ts::recallEpisodes": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). THE LINT IS RIGHT ABOUT THE CODE. Line 95: `const conditions = [eq(agentEpisodicMemory.agentCodename, agentCodename)];` and line 98-99: `let results = await db.select().from(agentEpisodicMemory)` / `.where(and(...conditions))`. The only predicates are agent codename and, optionally, `gte(agentEpisodicMemory.relevanceScore, query.minRelevance)` (96). Agent codenames are GLOBAL (commit c0427688's own message: \"agent codenames are global\"), so this reads every tenant's episodes for that codename, and the tag/entity filters at 104-115 are applied IN MEMORY, after the rows have already been fetched. It then WRITES across whatever it matched: line 119-123 `await db.update(agentEpisodicMemory).set({ relevanceScore: sql`...+ 2`, accessCount: ... …",
    "server/services/cognitiveMemoryV13.ts::consolidateMemories": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). THE LINT IS RIGHT ABOUT THE CODE, and this is the worst-shaped of the seven. Line 317-320: `const recentEpisodes = await db.select().from(agentEpisodicMemory).where(and(` / `eq(agentEpisodicMemory.agentCodename, agentCodename),` / `gte(agentEpisodicMemory.createdAt, dayAgo),` — codename + time only, no org term anywhere in the unit (310-360), so it groups EVERY tenant's episodes for that global codename into one tagGroups map (322-328). It then MERGES them into one semantic fact: line 336 `await this.extractFact(agentCodename, {` with `fact:` built from the group's tag, count, success rate and actions (337) and `sourceEpisodes: episodes.map((e) => e.episodeId)` (339) — episode ids drawn from multiple orgs — and NO `orgId` field, so after c0427688 it …",
    "server/services/cognitiveMemoryV13.ts::getMemoryStats": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). THE LINT IS RIGHT ABOUT THE CODE: the unit (363-409) contains no org marker at all. Lines 364-366 build the only filters from an OPTIONAL codename — `const epFilter = agentCodename ? eq(agentEpisodicMemory.agentCodename, agentCodename) : undefined;` — and `undefined` passed to `.where()` in Drizzle is no predicate, so calling it with no argument counts every row of all three tables for every tenant: 368-373 three `count(*)::int` selects, 374-375 the average relevance, 377-380 `topCategories` grouped by `agentSemanticMemory.category`, and 389-394 the per-agent breakdown (`groupBy(agentEpisodicMemory.agentCodename)` etc.) with no filter on any of the three. What would escape is aggregate — counts, an average, category labels, codenames — not row …",
    "server/services/cognitiveMemoryV13.ts::getWorkingMemory": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). THE LINT IS RIGHT ABOUT THE CODE. Lines 290-297: `const conditions = [` / `eq(agentWorkingMemoryV13.agentCodename, agentCodename),` / `eq(agentWorkingMemoryV13.key, key),` / `];` / `if (sagaId) conditions.push(eq(agentWorkingMemoryV13.sagaId, sagaId));` / `const [entry] = await db.select().from(agentWorkingMemoryV13)` / `.where(and(...conditions)).limit(1);` — no org term in the unit at all, and the row (including its whole `value` jsonb and its `orgId`) is returned at 299. Codename is global and `key` is a plain string, so a (codename, key) pair colliding across tenants returns whichever row `limit(1)` picks. `agent_working_memory_v13.orgId` is `orgId: integer(\"org_id\")` (shared/schema.ts:16496): NULLABLE, no FK, and NOT in the table's index list …",
    "server/services/cognitiveMemoryV13.ts::setWorkingMemory": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). RULE 2 (\"has an org and resolves by id anyway\") FIRED CORRECTLY — this is not a lint misread. The signature does carry an org at line 262: `ttlSeconds: number = 3600, sagaId?: string, orgId?: number,`, which is why rule 1 passed it; but the org is used at exactly one place, the INSERT at line 283 (`sagaId: sagaId ?? null, orgId: orgId ?? null,`), and never in a predicate. The update rule 2 names is line 275-277: `const [updated] = await db.update(agentWorkingMemoryV13)` / `.set({ value, ttlSeconds, expiresAt })` / `.where(eq(agentWorkingMemoryV13.id, existing.id)).returning();` — a bare serial primary key with no lane term. `existing.id` is NOT a verified parent: it comes from the unscoped lookup at 271-272 (see the sibling rule-3 entry), so an org …",
    "server/services/cognitiveMemoryV13.ts::setWorkingMemory::agentWorkingMemoryV13": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). RULE 3 (\"the UNIT is scoped, this QUERY is not\") FIRED CORRECTLY, and it names a DIFFERENT statement from the rule-2 entry above: the upsert probe at lines 271-272, `const [existing] = await db.select().from(agentWorkingMemoryV13)` / `.where(and(...conditions)).limit(1);`, where `conditions` (265-269) is `[eq(agentWorkingMemoryV13.agentCodename, agentCodename), eq(agentWorkingMemoryV13.key, key)]` plus `eq(agentWorkingMemoryV13.sagaId, sagaId)` only `if (sagaId)`. The unit has org context (`orgId?: number`, line 262) and this chain does not name it and does not resolve by primary key — precisely rule 3's shape, and the chain is the ROOT of the rule-2 defect: whatever row this returns is what line 277 then overwrites by bare id. Because `orgId` is …",
    "server/services/cognitiveMemoryV13.ts::clearExpiredWorkingMemory": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). THE LINT IS RIGHT ABOUT THE TEXT: the whole unit is lines 303-307 and contains no org marker — `const deleted = await db.delete(agentWorkingMemoryV13)` (304) / `.where(lte(agentWorkingMemoryV13.expiresAt, new Date())).returning();` (305). It deletes every organization's expired working-memory rows in one statement. PROOF OF NO CALLERS: `grep -rn clearExpiredWorkingMemory` across the repo excluding node_modules/.git/dist returns exactly ONE hit — the definition at cognitiveMemoryV13.ts:303 (plus register line :17). No job registers it: server/jobs/jobRegistry.ts:296's `agent_memory_consolidation_weekly` is the only memory job and it runs `runWeeklyMemoryConsolidation` from services/agentMemoryConsolidation (runScheduledJobs.ts:1839), a different …",
    "server/services/selfHealingMeshV13.ts::performRootCauseAnalysis": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). THE MOST SUBSTANTIVE OF THE SIX — a genuine cross-tenant aggregation, but with zero callers. server/services/selfHealingMeshV13.ts:185-256. What the code says (quoted, not paraphrased): - 186-189 parent read by caller-supplied id, no org term: `.from(anomalyDetections)` / `.where(eq(anomalyDetections.anomalyId, anomalyId))`. - 199-207 UNPROJECTED, UNSCOPED read of every tenant's mesh events in a +/-5min window: `const correlatedEvents = await db` / `.select()` / `.from(eventMeshEvents)` / `.where(and(` / `gte(eventMeshEvents.createdAt, windowStart),` / `lte(eventMeshEvents.createdAt, windowEnd),` / `))`. The only predicates are the two timestamps. `.select()` with no projection materialises the whole row including `payload` jsonb. - 209-216 same …",
    "server/services/selfHealingMeshV13.ts::resolveAnomaly": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). A WRITE by caller-supplied id with no org predicate — latent, not live. server/services/selfHealingMeshV13.ts:483-497: `const [resolved] = await db` / 488 `.update(anomalyDetections)` / 489 `.set({ resolvedAt: new Date(), autoResolved })` / 490 `.where(eq(anomalyDetections.anomalyId, anomalyId))` / 491 `.returning();`. The sole predicate is the string id, and 496 `return resolved;` hands the whole row back. ORG COLUMN — NULLABLE TAG. shared/schema.ts:16727 `orgId: integer(\"org_id\"),`; migrations/0018_v13_sentient_enterprise.sql:264 `org_id INTEGER,` — not NOT NULL, no REFERENCES organizations(id). Under the repo's own \"null matches only null\" rule the missing lane would still matter if rows were ever tagged. BUT NO ROW CAN EVER CARRY A TENANT TAG …",
    "server/services/selfHealingMeshV13.ts::detectAnomalies": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). server/services/selfHealingMeshV13.ts:166-182. The gate read the code correctly — there is no org predicate: 167 `const conditions = [isNull(anomalyDetections.resolvedAt)];` 169 `conditions.push(eq(anomalyDetections.agentCodename, agentCodename));` (only when the optional arg is passed) 172-175 `return db` / `.select()` / `.from(anomalyDetections)` / `.where(and(...conditions))` — an unprojected select of every unresolved anomaly row, ordered by severity then createdAt (176-181). ORG COLUMN — NULLABLE TAG, NOT AN FK: shared/schema.ts:16727 `orgId: integer(\"org_id\"),`; migrations/0018_v13_sentient_enterprise.sql:264 `org_id INTEGER,`, no NOT NULL and no REFERENCES. The table is also unwritten: its only INSERT is recordMetric:145-158, which omits …",
    "server/services/selfHealingMeshV13.ts::evaluatePlaybooks": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). server/services/selfHealingMeshV13.ts:341-396. Three unscoped statements: 345-349 parent read by caller-supplied id, no org term: `const [anomaly] = await db` / `.select()` / `.from(anomalyDetections)` / `.where(eq(anomalyDetections.anomalyId, anomalyId))` / `.limit(1);` — and 351-353 throws if absent, so it is not a verified parent, it IS the id it was handed. 355-358 `const enabledPlaybooks = await db` / `.select()` / `.from(incidentPlaybooks)` / `.where(eq(incidentPlaybooks.isEnabled, true));` — every enabled playbook, platform-wide. 386-389 a WRITE: `.update(incidentPlaybooks)` / `.set({ lastTriggeredAt: now, triggerCount: playbook.triggerCount + 1 })` / `.where(eq(incidentPlaybooks.id, playbook.id));` TABLE FACTS: incident_playbooks has NO org …",
    "server/services/selfHealingMeshV13.ts::getHealthDashboard": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). server/services/selfHealingMeshV13.ts:441-480. Five reads, none carrying an org term; only one touches a table the gate considers org-scoped: 447-450 `const activeAnomalies = await db` / `.select()` / `.from(anomalyDetections)` / `.where(isNull(anomalyDetections.resolvedAt));` — the org-tagged table, read platform-wide. The other four are on tables with NO org column anywhere: 442-445 `.from(agentHealthBaselines)` (shared/schema.ts:16694-16708 and migrations/0018_v13_sentient_enterprise.sql:236-247 declare none), 457-460 `.from(degradationModes)` `.where(eq(degradationModes.isCurrentMode, true))` (schema 16734-16748 / DDL 272-282, none), 462-465 `.from(incidentPlaybooks)` (schema 16750-16764 / DDL 286-297, none), 467-471 `.from(chaosExperiments)` …",
    "server/services/selfHealingMeshV13.ts::getStats": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). server/services/selfHealingMeshV13.ts:500-545. Seven aggregate reads; the two that touch the gate's org-scoped population are: 505-508 `const [unresolvedCount] = await db` / `.select({ value: count() })` / `.from(anomalyDetections)` / `.where(isNull(anomalyDetections.resolvedAt));` 510-513 `const [resolvedCount] = await db` / `.select({ value: count() })` / `.from(anomalyDetections)` / `.where(sql`${anomalyDetections.resolvedAt} IS NOT NULL`);` Neither carries an org term. The remaining five (501-503 agentHealthBaselines, 515-517 degradationModes, 519-521 incidentPlaybooks, 523-526 and 528-534 chaosExperiments) are on tables with no org column in schema or DDL, and they return only COUNT/AVG scalars — no rows and no free text cross anything. …",
    "server/services/adaptiveStrategyV13.ts::deactivateStrategy": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). REGISTER ENTRY: rule1.method (\"no organization anywhere in the unit\"), scripts/org-scope-route-widening.json:14. Not present in _TRIAGED. THE CODE (/home/user/AcreOS/server/services/adaptiveStrategyV13.ts:425-440) — the predicate is exactly one term and it is not an org term: 425 async deactivateStrategy(strategyId: string): Promise<AgentStrategyEntry> { 426 const [strategy] = await db 427 .update(agentStrategies) 428 .set({ 429 isActive: false, 430 updatedAt: new Date(), 431 }) 432 .where(eq(agentStrategies.strategyId, strategyId)) 433 .returning(); The unit contains no `orgId`, no `organizationId`, no `forOrg(`, no `unscopedForPlatformOps(`. Rule 1's predicate reads the code correctly — this is NOT a lint false positive. It is a caller-supplied …",
    "server/services/adaptiveStrategyV13.ts::getStrategyLineage": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). REGISTER ENTRY: rule1.method (\"no organization anywhere in the unit\"), scripts/org-scope-route-widening.json:15. Not present in _TRIAGED. THE CODE (/home/user/AcreOS/server/services/adaptiveStrategyV13.ts:395-418) — a LOOP of unscoped reads, not one: 402 while (currentId) { 403 if (visited.has(currentId)) break; // prevent cycles 404 visited.add(currentId); 405 406 const [strategy] = await db 407 .select() 408 .from(agentStrategies) 409 .where(eq(agentStrategies.strategyId, currentId)); 410 411 if (!strategy) break; 412 413 lineage.unshift(strategy); // prepend — ancestors first 414 currentId = strategy.parentStrategyId; Signature is `getStrategyLineage(strategyId: string)` (line 395-397) — no org parameter exists. The unit contains no …",
    "server/services/adaptiveStrategyV13.ts::getStats::agentStrategies": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). REGISTER ENTRY: rule3 (\"the UNIT is scoped, this QUERY is not\"), scripts/org-scope-route-widening.json:304. Not present in _TRIAGED. WHY THE LINT FIRED, and it read the text correctly: the unit `getStats(orgId?: number)` (line 447) has org context, but the org predicate is HOISTED out of the chain at lines 455-457 — 455 const stratConditions = orgId !== undefined 456 ? [eq(agentStrategies.orgId, orgId)] 457 : []; — so the chain span itself (from `.from(` to the statement's `;` at depth 0, lines 470-499) contains no `orgId`/`OrgId` token and fails ORG_CONTEXT_RE. This is rule 3's declared discriminator (d) limitation ('chains whose predicate is a hoisted variable are not reachable by text and are not guessed at') firing as designed, not a misread. …",
    "server/services/adaptiveStrategyV13.ts::getStats::strategyAssignments": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). REGISTER ENTRY: rule3, scripts/org-scope-route-widening.json:305. Not present in _TRIAGED. THE CHAIN (/home/user/AcreOS/server/services/adaptiveStrategyV13.ts:487-494): 487 db 488 .select({ count: sql<number>`count(*)` }) 489 .from(strategyAssignments) 490 .where( 491 assignConditions.length > 0 492 ? and(...assignConditions) 493 : undefined, 494 ), with the hoisted predicate at 459-461: 459 const assignConditions = orgId !== undefined 460 ? [eq(strategyAssignments.orgId, orgId)] 461 : []; Same structure as the agentStrategies chain: scoped when `orgId !== undefined`, `.where(undefined)` — i.e. no predicate, all lanes — when it is omitted. Rule 3 fired because the chain text carries no org token (the predicate is hoisted); the lint read the code …",
    "server/services/adaptiveStrategyV13.ts::getStats::strategyProposals": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). REGISTER ENTRY: rule3, scripts/org-scope-route-widening.json:306. Not present in _TRIAGED. THE CHAIN (/home/user/AcreOS/server/services/adaptiveStrategyV13.ts:495-498): 495 db 496 .select({ count: sql<number>`count(*)` }) 497 .from(strategyProposals) 498 .where(and(...proposalConditions)), with the hoisted predicate at 463-465: 463 const proposalConditions = orgId !== undefined 464 ? [eq(strategyProposals.orgId, orgId), eq(strategyProposals.status, \"pending\")] 465 : [eq(strategyProposals.status, \"pending\")]; This chain differs from its two siblings: it ALWAYS has a predicate (`status = 'pending'`), so it never degrades to `.where(undefined)`. When `orgId` is omitted the surviving predicate is a status filter with no tenant term, and …",
    "server/services/collaborationProtocolV13.ts::addMessage": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). PROOF OF NO CALLERS. The singleton is `export const collaborationProtocolService = new CollaborationProtocolService();` (collaborationProtocolV13.ts:659) and the class is not exported (line 89 `class CollaborationProtocolService {`), so every call must go through that name. Repo-wide grep for `collaborationProtocolService` across ./server, ./client, ./tests, ./scripts, ./shared, ./docs (excluding node_modules, .git and its own file) returns exactly five lines: server/services/autonomyFinalMile.ts:128 (`const { collaborationProtocolService } = await import(\"./collaborationProtocolV13\");`), autonomyFinalMile.ts:152 (`.updateDelegation(...)`), server/services/confidenceCascadeV14.ts:16 (import), confidenceCascadeV14.ts:416 (`.delegate({`), and …",
    "server/services/collaborationProtocolV13.ts::checkConsensus": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). PROOF OF NO CALLERS — this is the strongest of the five. Grep for the bare identifier `checkConsensus` across the entire repo (no extension filter; excluding node_modules, .git, dist and its own file) returns exactly three lines, and all three are the register entry itself: scripts/org-scope-route-widening.json:23. Zero source hits in server/, client/, tests/, shared/, scripts/*.mjs or docs/. It is not called internally either — the only internal consensus call inside this file is `this.evaluateConsensus(...)` (lines 179 and 312), a different private method (declared line 211). The built bundle corroborates: `grep -o '\\.checkConsensus(' dist/index.cjs` returns zero matches, while `.resolveDialogue(` and `.updateDelegation(` each return one. As with …",
    "server/services/collaborationProtocolV13.ts::getAgentReputation": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). PROOF OF NO CALLERS. Grep for `getAgentReputation` across the whole repo (no extension filter; excluding node_modules, .git, dist and its own file) returns exactly one line — the register entry, scripts/org-scope-route-widening.json:24. Zero hits in server/, client/, tests/, shared/ or scripts/. The compiled bundle agrees: `grep -o '\\.getAgentReputation(' dist/index.cjs` returns zero. The only two production member accesses on `collaborationProtocolService` anywhere are autonomyFinalMile.ts:152 (`.updateDelegation`) and confidenceCascadeV14.ts:416 (`.delegate`); no route file imports collaborationProtocolV13 (repo-wide grep for the module name returns only autonomyFinalMile.ts:128, confidenceCascadeV14.ts:16, the register, one test mock and two docs …",
    "server/services/collaborationProtocolV13.ts::resolveDialogue": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). PROOF OF NO CALLERS — transitively unreachable, and I am stating the transitivity rather than hiding it. Grep for `resolveDialogue` repo-wide (no extension filter; excluding node_modules, .git, dist) returns exactly two source occurrences, BOTH inside collaborationProtocolV13.ts: the declaration at line 322 (`async resolveDialogue(dialogueId: string, resolution: string)`) and one internal call at line 186 (`await this.resolveDialogue(dialogueId, consensus.resolution);`), which sits inside `addMessage`. The only other hit anywhere is the register entry scripts/org-scope-route-widening.json:25. So the sole path into it is `addMessage` → and addMessage itself has zero callers (see that finding: the singleton's only two production member accesses …",
    "server/services/collaborationProtocolV13.ts::updateDelegation": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). THIS IS THE ONE OF THE FIVE THAT ACTUALLY RUNS, and it is a scheduled platform sweep. Sole caller: server/services/autonomyFinalMile.ts:152 `await collaborationProtocolService.updateDelegation(delegation.delegationId, { status: \"completed\", qualityScore: 0.8, result: {...} });`, inside `checkDelegationCompletions` (declared autonomyFinalMile.ts:123). That function is registered as a cron at server/jobs/runScheduledJobs.ts:4592 `withJobLock(\"check_delegation_completions\", 13 * 60, checkDelegationCompletions)` on a 15-minute `trackInterval` (line 4594). Grep for `checkDelegationCompletions` repo-wide returns only that declaration, that import (runScheduledJobs.ts:4576) and that registration — there is no route, founder or customer, that invokes it, …",
    "server/services/feedbackLoopV14.ts::getOverrideDetails": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). THE QUERY (rule 1 — the unit names no org at all). server/services/feedbackLoopV14.ts:201-202: 201: async getOverrideDetails(overrideId: string): Promise<FounderOverrideEntry | null> { 202: const [entry] = await db.select().from(founderOverrides).where(eq(founderOverrides.overrideId, overrideId)).limit(1); The whole method body contains no `orgId` token (confirmed: `grep -n orgId server/services/feedbackLoopV14.ts` returns nothing between 188 and 213), so rule 1 fired correctly — this is NOT a lint false positive. The predicate is on `overrideId`, which is `text(\"override_id\").notNull().unique()` (shared/schema.ts:17009), not the serial PK, which is also why rule 2's `eq(t.id, …)` shape never covered it. COLUMN NATURE. shared/schema.ts:17010 — …",
    "server/services/feedbackLoopV14.ts::reinforceLearning": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). THE STATEMENTS (rule 1 — unit names no org). server/services/feedbackLoopV14.ts:257-266. A READ and a WRITE, both unscoped: 257: async reinforceLearning(learningId: string): Promise<FeedbackLearningEntry | null> { 258: const [existing] = await db.select().from(feedbackLearnings).where(eq(feedbackLearnings.learningId, learningId)).limit(1); 261: const [updated] = await db.update(feedbackLearnings).set({ 262: confidence: Math.min(newConfidence, 1.0), 263: reinforcementCount: existing.reinforcementCount + 1, 266: }).where(eq(feedbackLearnings.learningId, learningId)).returning(); The body contains no `orgId` (the orgId grep skips from line 239 to 286), so rule 1 is right. `learningId` is `text(\"learning_id\").notNull().unique()` …",
    "server/services/feedbackLoopV14.ts::retractLearning": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). THE STATEMENTS (rule 1 — unit names no org). server/services/feedbackLoopV14.ts:404-413, read then write, both unscoped: 404: async retractLearning(learningId: string, reason?: string): Promise<FeedbackLearningEntry | null> { 405: const [existing] = await db.select().from(feedbackLearnings).where(eq(feedbackLearnings.learningId, learningId)).limit(1); 408: const [updated] = await db.update(feedbackLearnings).set({ 409: status: \"retracted\", 410: ruleConfig: { ...cfg, retracted: true, retractedAt: new Date().toISOString(), retractedReason: reason ?? \"Founder disagreed with extracted rule\" }, 412: }).where(eq(feedbackLearnings.learningId, learningId)).returning(); No `orgId` anywhere in the body (orgId grep jumps 393 → 425), so rule 1 is correct. …",
    "server/services/feedbackLoopV14.ts::getLearningImpact::feedbackLearnings": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). WHICH CHAIN THE KEY POINTS AT. Rule 3 = 'the unit is scoped, this query is not'. getLearningImpact qualifies as a scoped unit because ORG_CONTEXT_RE matches `learning.orgId` at lines 425 and 429. The offending chain is server/services/feedbackLoopV14.ts:418: 418: const [learning] = await db.select().from(feedbackLearnings).where(eq(feedbackLearnings.learningId, learningId)).limit(1); Chain text is `.from(feedbackLearnings).where(eq(feedbackLearnings.learningId, learningId)).limit(1)` — no org token, and not excluded by discriminator (b) (`eq(feedbackLearnings.id,` is absent; the predicate is on the unique text column `learning_id`, shared/schema.ts:17030). So the flag is accurate, not a false positive. WHY ONLY feedbackLearnings IS IN THE KEY, AND …",
    "server/services/feedbackLoopV14.ts::propagateLearning::feedbackLearnings": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). WHICH CHAIN THE KEY POINTS AT. The unit is scoped for rule-3 purposes via `orgId: learning.orgId` at lines 286 and 296. The offending chain is server/services/feedbackLoopV14.ts:271: 270: async propagateLearning(learningId: string): Promise<PropagationSummary> { 271: const [learning] = await db.select().from(feedbackLearnings).where(eq(feedbackLearnings.learningId, learningId)).limit(1); No org token in the chain; not a PK lookup (`learning_id` is the unique text column, shared/schema.ts:17030) so discriminator (b) does not exempt it. Accurate flag. The trailing write at 369-370 `db.update(feedbackLearnings).set({appliedToMemory,…}).where(eq(feedbackLearnings.learningId, learningId))` is equally unscoped but is structurally invisible to rule 3 …",
    "server/services/governanceBrainV13.ts::getExplanation": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). THE FLAGGED CODE (rule 1 — unit touches an org-scoped table and never names an org), server/services/governanceBrainV13.ts:257-265: 257 async getExplanation(actionId: string) { 258 const [explanation] = await db 259 .select() 260 .from(auditExplanations) 261 .where(eq(auditExplanations.actionId, actionId)) 262 .limit(1); 263 264 return explanation ?? null; The lint read it correctly — this is NOT a false positive. The unit text contains none of ORG_CONTEXT_RE's tokens (organizationId / orgId / forOrg( / unscopedForPlatformOps(), and `audit_explanations` IS org-scoped: shared/schema.ts:16837 `orgId: integer(\"org_id\"),` — NULLABLE, and with NO `.references(() => organizations.id)` anywhere in the block (schema.ts:16828-16844). A nullable untethered …",
    "server/services/governanceBrainV13.ts::getStats::policyEvaluations": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). THE FLAGGED CHAIN (rule 3 — 'the UNIT is scoped, this QUERY is not'), server/services/governanceBrainV13.ts:529-540: 529 async getStats(orgId?: number) { ... 536 const evalConditions = orgId ? eq(policyEvaluations.orgId, orgId) : undefined; 537 const evaluations = await db 538 .select() 539 .from(policyEvaluations) 540 .where(evalConditions); NOT a false positive, and the gate is right for the right reason. `orgId` is OPTIONAL (`orgId?: number`, line 529), and the predicate is the exact ternary-collapsing-to-undefined shape the lint was hardened against: scripts/check-org-scoped-fetch.mjs:2237-2249 says \"A predicate that can be `undefined` is not a predicate… An initializer with an `undefined` branch is not credited; the query answers to rule 3 as …",
    "server/services/governanceBrainV13.ts::getStats::complianceSnapshots": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). TWO flagged chains, both in getStats, both keyed on the same variable — server/services/governanceBrainV13.ts:550-556 and 568-571: 550 const snapshotConditions = orgId ? eq(complianceSnapshots.orgId, orgId) : undefined; 551 const [latestSnapshot] = await db 552 .select() 553 .from(complianceSnapshots) 554 .where(snapshotConditions) 555 .orderBy(desc(complianceSnapshots.period)) 556 .limit(1); ... 568 const allSnapshots = await db 569 .select() 570 .from(complianceSnapshots) 571 .where(snapshotConditions); Same `undefined`-branch ternary as the policyEvaluations chain, correctly refused credit by scripts/check-org-scoped-fetch.mjs:2249 (`if (!/\\bundefined\\b/.test(initializer)) text += \"\\n\" + initializer;`). Not a false positive. CORRECTION TO THE …",
    "server/services/governanceBrainV13.ts::getStats::regulatorySandboxRuns": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). THE FLAGGED CHAIN, server/services/governanceBrainV13.ts:561-565: 561 const sandboxConditions = orgId ? eq(regulatorySandboxRuns.orgId, orgId) : undefined; 562 const sandboxRuns = await db 563 .select() 564 .from(regulatorySandboxRuns) 565 .where(sandboxConditions); Third instance of the same `undefined`-branch ternary in one method; the lint correctly declines to credit it (scripts/check-org-scoped-fetch.mjs:2237-2249). Not a false positive. Only `.length` is used from the result (line 586 `sandboxRuns: sandboxRuns.length`), but `db.select()` with no projection reads every column of every matching row, so the read itself is full-row and cross-org whenever orgId is absent or 0. TABLE: shared/schema.ts:16871 `orgId: integer(\"org_id\"),` — NULLABLE, no …",
    "server/services/decisionsInbox.ts::createFromAlert::decisionsInboxItems": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). THE FLAGGED STATEMENT — server/services/decisionsInbox.ts:224-229, the open-card dedupe, no org term anywhere in the chain: 224 const existing = await db.query.decisionsInboxItems.findFirst({ 225 where: and( 226 eq(decisionsInboxItems.sourceAlertId, alertId), 227 eq(decisionsInboxItems.status, \"pending\"), 228 ), 229 }); 230 if (existing) return existing.id; Rule 3 fires because the unit DOES name an org — but only as a literal null at 246, `organizationId: null,` inside the attachPrediction({...}) call. The insert at 251-267 sets no organizationId at all, so every row this function can ever write is org-NULL. NO PRODUCTION CALLERS. Grep of the identifier across the repo excluding its own file: $ grep -rn \"createFromAlert\" --include=*.ts …",
    "server/services/decisionsInbox.ts::createOutcomeCheckIn::decisionsInboxItems": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). THE FLAGGED STATEMENT — server/services/decisionsInbox.ts:524-533: 524 const existing = await db.query.decisionsInboxItems.findFirst({ 525 where: and( 526 eq(decisionsInboxItems.itemType, \"outcome_check_in\"), 527 or( 528 eq(decisionsInboxItems.status, \"pending\"), 529 eq(decisionsInboxItems.status, \"deferred\"), 530 ), 531 sql`${decisionsInboxItems.contextBundle}->>'outcomeCheckInFor' = ${String(original.id)}`, 532 ), 533 }); This is the one of the four whose INSERT is genuinely tenant-tagged — line 566, `organizationId: original.organizationId,` — so the flag is not noise: the table's org column is a NULLABLE FK, `organizationId: integer(\"organization_id\").references(() => organizations.id),` (shared/schema.ts:13103), i.e. a mixed table holding both …",
    "server/services/decisionsInbox.ts::createShadowPromotionRequest::decisionsInboxItems": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). THE FLAGGED STATEMENT — server/services/decisionsInbox.ts:617-627: 617 const existing = await db.query.decisionsInboxItems.findFirst({ 618 where: and( 619 eq(decisionsInboxItems.itemType, SHADOW_PROMOTION_ITEM_TYPE), 620 or( 621 eq(decisionsInboxItems.status, \"pending\"), 622 eq(decisionsInboxItems.status, \"deferred\"), 623 ), 624 sql`${decisionsInboxItems.contextBundle}->'shadowPromotion'->>'domain' = ${input.domain}`, 625 sql`${decisionsInboxItems.contextBundle}->'shadowPromotion'->>'toLevel' = ${input.toLevel}`, 626 ), 627 }); The insert this dedupes writes `organizationId: null,` (line 665) with `ownerAgentCodename: \"solene\"` (666), so every matchable row is org-NULL by construction. `shadowPromotion` has exactly one writer — grep across server/ …",
    "server/services/decisionsInbox.ts::createDirectDecisionCard::decisionsInboxItems": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). THE FLAGGED STATEMENT — server/services/decisionsInbox.ts:476-484: 476 const existing = await db.query.decisionsInboxItems.findFirst({ 477 where: and( 478 or( 479 eq(decisionsInboxItems.status, \"pending\"), 480 eq(decisionsInboxItems.status, \"deferred\"), 481 ), 482 sql`${decisionsInboxItems.contextBundle}->>'directCardSubject' = ${card.subject}`, 483 ), 484 }); 485 if (existing) return { itemId: existing.id, created: false }; The matchable population is closed and org-null. `directCardSubject` has EXACTLY ONE writer in the repo — grep across server/, client/ and shared/ excluding this file returns zero hits; the only write is this same function's insert at :504-507, and that insert hard-codes `organizationId: null,` (line 502) beside `actionPayload: …",
    "server/services/autonomousDecisionExecutor.ts::buildAlertContext": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). THE STATEMENT (autonomousDecisionExecutor.ts:394-399): `async function buildAlertContext(item: any): Promise<string> {` / 395 ` const alert = item.sourceAlertId` / 396 ` ? await db.query.systemAlerts.findFirst({` / 397 ` where: eq(systemAlerts.id, item.sourceAlertId),` / 398 ` })` / 399 ` : null;` — one relational read of system_alerts by primary key, no org predicate. NOT A LINT FALSE POSITIVE: the whole unit (394-412) never spells organizationId, orgId, forOrg( or unscopedForPlatformOps(, so rule 1 fires correctly. (Its sibling buildSupportEscalationContext is in rule 2 instead only because its template string interpolates `item.organizationId`.) I re-ran `node scripts/check-org-scoped-fetch.mjs`: PASS with `0 stale` register entries, so this key …",
    "server/services/autonomousDecisionExecutor.ts::buildFeatureRequestContext": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). THE STATEMENT (autonomousDecisionExecutor.ts:414-419): `async function buildFeatureRequestContext(item: any): Promise<string> {` / 415 ` const request = item.sourceFeatureRequestId` / 416 ` ? await db.query.featureRequests.findFirst({` / 417 ` where: eq(featureRequests.id, item.sourceFeatureRequestId),` / 418 ` })` / 419 ` : null;` — a by-primary-key relational read of feature_requests with no org predicate. Rule 1 fires correctly: the unit (414-433) contains no organizationId/orgId/forOrg(/unscopedForPlatformOps( token. Not a lint false positive. THIS ONE IS A REAL TENANT KEY AND REAL CUSTOMER TEXT — which is why it was worth reading: shared/schema.ts:4731 `export const featureRequests = pgTable(\"feature_requests\", {` and 4733 ` organizationId: …",
    "server/services/scpGoldenSuite.ts::getAllGoldenCases": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). THE STATEMENT. /home/user/AcreOS/server/services/scpGoldenSuite.ts:150-157: 150: export async function getAllGoldenCases(): Promise<{ 155: const allCases = await db.query.scpGoldenCases.findMany({ 156: orderBy: [sql`created_at DESC`], 157: }); No predicate of any kind — every row of scp_golden_cases in the database, aggregated into byAgent counts plus the 10 most recent full rows (line 167 `recentCases: allCases.slice(0, 10)`). THE GATE IS NOT WRONG. scripts/check-org-scoped-fetch.mjs:1749 — `const ORG_CONTEXT_RE = /[Oo]rganizationId|[Oo]rgId|forOrg\\s*\\(|unscopedForPlatformOps\\s*\\(/;`. The function's signature+body contain none of those four, and it reads an org-scoped-by-population table, so rule 1 fires correctly. This is not a …",
    "server/services/scpGoldenSuite.ts::getGoldenCases": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). THE STATEMENT. /home/user/AcreOS/server/services/scpGoldenSuite.ts:140-145: 140: export async function getGoldenCases(agent: string): Promise<typeof scpGoldenCases.$inferSelect[]> { 141: return db.query.scpGoldenCases.findMany({ 142: where: eq(scpGoldenCases.agentCodename, agent), 143: orderBy: [sql`created_at DESC`], 144: }); 145: } The sole predicate is on agent_codename. There is no org term. THE ARGUMENT IS CALLER-SUPPLIED, AND THAT IS THE PART TO BE PRECISE ABOUT. server/routes-scp-v2.ts:188-194 — line 188 `app.get(\"/api/scp/v2/golden-suite/:agent\", async (req: Request, res: Response) => {`, line 191 `const cases = await getGoldenCases(req.params.agent);`. So `agent` comes straight off the URL, unvalidated. But it is an AGENT CODENAME, not an …",
    "server/services/autonomyScoreV14.ts::resolveDependencyEvent": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). WORK LIST: scripts/org-scope-route-widening.json holds exactly one key beginning \"server/services/autonomyScoreV14.ts::\" — line 16, inside rule1.method: \"server/services/autonomyScoreV14.ts::resolveDependencyEvent\". It is NOT present under _TRIAGED (226 entries, checked programmatically). So this one key is the whole work list. THE CODE IS AS THE GATE SAYS — not a false positive. server/services/autonomyScoreV14.ts:100-113: 100: async resolveDependencyEvent(eventId: string): Promise<FounderDependencyEventEntry> { 101: const [event] = await db.select().from(founderDependencyEvents) 102: .where(eq(founderDependencyEvents.eventId, eventId)); 104: if (!event) throw new Error(`Dependency event ${eventId} not found`); 110: const [updated] = await …",
    "server/services/confidenceCascadeV14.ts::getResolutionDetails": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). WORK LIST: scripts/org-scope-route-widening.json contains exactly one key for this file — \"server/services/confidenceCascadeV14.ts::getResolutionDetails\" at rule1.method (line 27 of the register). It is absent from _TRIAGED (which holds no key for this file at all), so it is the single unread entry. THE STATEMENT (server/services/confidenceCascadeV14.ts:661-669, verbatim with line numbers): 661: async getResolutionDetails(resolutionId: string): Promise<CascadeResolutionEntry | null> { 662: const [row] = await db 663: .select() 664: .from(cascadeResolutions) 665: .where(eq(cascadeResolutions.resolutionId, resolutionId)) 666: .limit(1); 667: 668: return row ?? null; 669: } The predicate is `eq(cascadeResolutions.resolutionId, resolutionId)` and …",
    "server/services/ceoReminders.ts::getContextForReminder": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). WORK LIST: the register (scripts/org-scope-route-widening.json) contains exactly ONE key for this file, at line 90, under rule1.function: \"server/services/ceoReminders.ts::getContextForReminder\". It is absent from _TRIAGED (226 keys, none matching 'ceoReminders'), so it is the whole work list. THE FLAG IS NOT A FALSE POSITIVE. server/services/ceoReminders.ts:316 `export async function getContextForReminder(reminder: CEOReminder): Promise<string | null> {` — its body resolves org-scoped rows by bare primary key with no org term anywhere in signature or body: L325-327: `const org = await db.query.organizations.findFirst({ where: eq(organizations.id, reminder.entityId), });` L349-351: `const deal = await db.query.deals.findFirst({ where: eq(deals.id, …",
    "server/services/matchmaking.ts::calculateSimilarity": "UNREACHABLE (triaged 2026-09-05, fan-out read + adversarial refutation). WHAT THE CODE SAYS. server/services/matchmaking.ts:347-358: 347: async calculateSimilarity( 348: propertyId1: number, 349: propertyId2: number 350: ): Promise<number> { 352: const prop1 = await db.query.properties.findFirst({ 353: where: eq(properties.id, propertyId1), 354: }); 356: const prop2 = await db.query.properties.findFirst({ 357: where: eq(properties.id, propertyId2), 358: }); Both statements are bare-id relational reads of `properties` with no org term; the string `organizationId` does not appear anywhere between lines 347 and 395. That is exactly rule 1's shape (scripts/check-org-scoped-fetch.mjs:123 — \"RULE 1 (this register) — no organization anywhere\"), so the gate did NOT misread it: the register entry describes the code accurately. …",
    "server/routes-founder-intelligence.ts::GET /sophie-activity": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). WORK LIST DERIVATION: scripts/org-scope-route-widening.json holds 16 keys prefixed \"server/routes-founder-intelligence.ts::\" across rule1.route and rule2.route; 15 appear under _TRIAGED. The single unread one is scripts/org-scope-route-widening.json:136 -> \"server/routes-founder-intelligence.ts::GET /sophie-activity\" (rule1.route). Rule 1 = the unit touches an org-scoped table and mentions organizationId nowhere. That is a correct reading here, not a false positive. THE STATEMENT (server/routes-founder-intelligence.ts:2743-2751), quoted verbatim: 2743: const autoResolved = await db.query.supportTickets.findMany({ 2744: where: and( 2745: sql`${supportTickets.resolvedAt} IS NOT NULL`, 2746: gte(supportTickets.resolvedAt, since), 2747: …",
    "server/services/founderBriefing.ts::getTopActions": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). WORK LIST: scripts/org-scope-route-widening.json holds exactly one key for this file — line 91, inside `rule1.function`: \"server/services/founderBriefing.ts::getTopActions\". It is absent from `_TRIAGED` (verified programmatically: no key under _TRIAGED contains \"founderBriefing\"). rule 1 = \"no organization anywhere in the unit\" (scripts/check-org-scoped-fetch.mjs:123). The gate still fires on it: a full run reports \"0 stale\" for the register, so this is a live, unread finding, not residue. THE STATEMENT (server/services/founderBriefing.ts:113-122): 113 async function getTopActions(since: Date, limit = 8): Promise<string[]> { 114 const rows = await db 115 .select({ summary: systemActivity.summary, job: systemActivity.jobName }) 116 …",
    "server/routes-onboarding-funnel.ts::GET /api/founder/onboarding-funnel/orgs": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). WORK LIST: scripts/org-scope-route-widening.json holds exactly one key for this file — \"server/routes-onboarding-funnel.ts::GET /api/founder/onboarding-funnel/orgs\", at line 149 of the register, inside rule1.route (the rule-1 class: the unit touches an org-scoped table and mentions no organization scoping anywhere). It is NOT present under _TRIAGED (I enumerated _TRIAGED keys and no key containing \"onboarding-funnel\" appears). The other three routes in the file are not in the register. THE STATEMENT. /home/user/AcreOS/server/routes-onboarding-funnel.ts lines 126-130: 126 const rows = await db 127 .select() 128 .from(onboardingFunnelMetrics) 129 .orderBy(orderByExpr) 130 .limit(limit); There is no .where() at all. The gate did not misread anything — …",
    "server/services/autonomyFinalMile.ts::checkDelegationCompletions::agentDelegations": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). THE STATEMENT (server/services/autonomyFinalMile.ts:132-136): 132: const pendingDelegations = await db 133: .select() 134: .from(agentDelegations) 135: .where(eq(agentDelegations.status, \"pending\")) 136: .limit(50); No organization predicate; the table is imported dynamically at :129 (`const { agentDelegations } = await import(\"@shared/schema\");`), which is why rule 3 sees the chain and no org term. WHY PER-ORG SCOPING WOULD BE WRONG, NOT MERELY INCONVENIENT — there is no caller org to scope to. `checkDelegationCompletions` is declared at autonomyFinalMile.ts:123 and grep for the name across server/, client/ and tests/ returns exactly five hits: its own definition (:123), server/jobs/runScheduledJobs.ts:4576 (destructured from the dynamic import) …",
    "server/services/onboarding/firstValueInstrumentation.ts::getFunnelSummary::onboardingFunnelMetrics": "PLATFORM_OP (triaged 2026-09-05, fan-out read + adversarial refutation). WORK LIST: scripts/org-scope-route-widening.json has exactly ONE key for this file, and it is untriaged. It lives in the rule3 array (rule1/rule2 are objects, rule3 is a list): \"server/services/onboarding/firstValueInstrumentation.ts::getFunnelSummary::onboardingFunnelMetrics\". It is absent from _TRIAGED. No other key in the register begins with this file's prefix. THE FLAGGED STATEMENT (server/services/onboarding/firstValueInstrumentation.ts:387-397): 387 const rows = await db 388 .select({ 389 orgId: onboardingFunnelMetrics.orgId, 390 signupAt: onboardingFunnelMetrics.signupAt, 391 timeToFirstValueSeconds: 392 onboardingFunnelMetrics.timeToFirstValueSeconds, 393 abandonedAtStep: onboardingFunnelMetrics.abandonedAtStep, 394 measuredDate: …"
  },
  "_TRIAGE_PROGRESS": [
    "Read: 300 of 300. Unread: 0.",
    "",
    "Every key in this file carries an individual, hand-checked reason under",
    "_TRIAGED. The 75 that were still unread on 2026-09-05 — the relational-API",
    "and org_id widening batches — were read that day by a fan-out of 21 agents,",
    "one per file, each told to open the CODE and rule on its file's entries, with",
    "every DEFECT claim then put to two independent skeptics briefed to refute it.",
    "",
    "Verdicts: 40 PLATFORM_OP, 31 UNREACHABLE, 2 VERIFIED_PARENT,",
    "1 LINT_FALSE_POSITIVE, 1 DEFECT.",
    "",
    "THE ONE DEFECT WAS NOT THE ONE THE ENTRY POINTED AT, and that is the part",
    "worth keeping. `integrationFrameworkV12.ts::execute` is registered for its",
    "two by-id writes at 224/231, and those are genuinely verified-parent — they",
    "write the row bound by the lane-scoped read above them. The defect was four",
    "lines away at 143: the cooldown auto-reset called `resetCircuitBreaker`",
    "WITHOUT a lane, and that method's `orgId: number | null = null` default sent",
    "the write to the platform lane. Since `credentialLane` admits an org's own",
    "credential and LANE_ORDER ranks it FIRST, an org-lane breaker cleared the",
    "SHARED platform row every other tenant falls back to, while its own breaker",
    "stayed open forever. Fixed in the same commit, the default removed so the",
    "type system demands the lane at every call site, and",
    "integrationCredentialLane.test.ts extended to drive the auto-reset path it",
    "had never reached (its `execute` cases all returned early on a null",
    "credential — the third law, on that suite's own population).",
    "",
    "An entry being READ is not an entry being FIXED. These 300 remain debt; what",
    "changed is that none of them is now an unopened claim."
  ],
  "_FIXED_2026_09_04": [
    "Four keys left this register by being FIXED, not excused. Recorded here",
    "because a register that only ever shrinks by deletion teaches nothing about",
    "what deletion meant:",
    "",
    "  GET /api/maintenance-tickets/:id — read a contractor by a bare id that the",
    "  DISPATCH route had written straight from a request body, with no ownership",
    "  check and a soft FK the database would not catch. Dispatching to another",
    "  org's contractor id returned that contractor's name, business name, email,",
    "  phone and trades. Both halves fixed.",
    "",
    "  GET /api/investor-profiles/directory — an unprojected select() shipped the",
    "  whole investor_profiles row, including verificationDocuments (the identity",
    "  document URLs), organizationId and lastActiveAt, to every authenticated",
    "  user of every org. A cross-org directory is the feature; the verification",
    "  block is not. Now projected column by column.",
    "",
    "  POST /api/webhooks/title-orders/:orderId/status — the assignment guard read",
    "  `if (order.titlePartnerId && …)`, vacuous in exactly the state the system",
    "  creates whenever routeTitleOrder() finds no territory match, and the handler",
    "  then auto-claimed the order for whichever partner called. Now bounded by the",
    "  same eligibility rule routeTitleOrder uses.",
    "",
    "  GET /api/borrower/session — resolved its note through",
    "  getNoteByAccessToken(session.noteId.toString()): a numeric id passed to a",
    "  lookup keyed on a client-settable, globally unique secret column, with no",
    "  organization predicate, on a Reg-Z consumer surface. Now loaded by id and",
    "  the session's own organization snapshot.",
    "",
    "All four are pinned by tests/unit/crossTenantPathsClosed2026_09_04.test.ts,",
    "and each pin was falsified by reverting its fix."
  ],
  "_UNTRIAGED_2026_09_04": "The 22 entries added on 2026-09-04 are UNTRIAGED, and that is different from every other entry in this file. The 208 that came before were each READ and carry an individual _TRIAGED reason. These came from widening the gate to Drizzle's RELATIONAL query API — db.query.<table>.findMany / .findFirst, 280 call sites under server/ that `touchedOrgScopedTables` and the rule-3 chain walker, both keyed on `.from(`, had never read. A unit whose only org-table access was relational was skipped before ANY of the three rules ran. They are frozen so the widened gate can fail on anything NEW while these are worked down; an entry here is not a judgement that the query is safe. Two were read on the way in and are worth the next reader's attention first: campaignEnhancements.cloneCampaign reads a campaign by id with no org check and inserts a copy, and matchmaking.findBuyersForListing reads EVERY investor profile in the platform. decisionsInbox.* is the founder's inbox and is probably legitimately cross-org — which means it wants unscopedForPlatformOps(reason), not a register line. BURN-DOWN: complianceAI.checkPropertyCompliance, complianceAI.generateDisclosure and landCredit.backtestScore were read and FIXED the same day — all three were live customer routes resolving a property by id with no ownership check. Four more the same day: decisionsInbox.getPendingItems and .getSuppressedDefects are the founder's own platform-wide queue and now go through unscopedForPlatformOps(reason) so the cross-org read is logged and greppable rather than indistinguishable from a forgotten one; campaignEnhancements.cloneCampaign and propertyIntelligenceEnhancements.compareProperties had no callers at all and were deleted. THREE more the same day, taking the untriaged block from 15 to 12: capitalMarkets.listSecurities and .getCapitalRaises both took `organizationId?: number` and built the predicate as `organizationId && status ? … : status ? eq(status) : undefined` — so a missing org, or the literal 0, produced a status-only or entirely empty predicate over every tenant's securities and raises. Both callers already passed org.id, so this was latent rather than live; the parameter is now REQUIRED and the org term UNCONDITIONAL, with status only narrowing it. complianceAI.getAlertsForOrganization was never an offender at all — it was a FALSE POSITIVE of the gate, which could not follow a relational `where:` held in a variable and so could not see the org predicate that was right there. That is fixed in `chainScopeText` in the same commit, and the fix was falsified by reintroducing a `: undefined` arm and confirming rule 3 still fires. AND matchmaking.findBuyersForListing — named above as the one to read first — was FIXED 2026-09-04: it now requires the seller's organizationId and resolves the listing against it, and returns a public projection of each matched investor rather than the whole row. Its remaining cross-org profile read carries its own _TRIAGED reason. Untriaged block 12 → 11.",
  "_UNTRIAGED_2026_09_04_ORGID": "The 88 entries added on 2026-09-04 by the org_id widening are UNTRIAGED, on the same terms as the relational-API batch above: frozen so the gate can fail on anything NEW while they are worked down, and an entry here is NOT a judgement that the query is safe. WHAT CHANGED: `collectOrgScopedTableIdents` — the function that decides which pgTables are org-scoped at all, the gate's front door — required the literal spelling `organizationId: <col>(\"organization_id\")`. This schema keys 39 of its tables on `orgId: integer(\"org_id\")` instead, so those 40 tables (404 vs 364) were outside the population before ANY of the three rules ran. Same failure as the `.from(`-only keying fixed on 2026-09-04 earlier the same day, one layer further out: not a rule that was wrong, a POPULATION smaller than the claim made about it. `check-org-leading-index.mjs` carried the identical assumption and its own header listed `orgId` under 'Limitations … raise to the team if they become real' — it was real; both are widened in the same commit. WHAT IS IN HERE: the great majority are the V12/V13/V14 agent-infrastructure services (cognitiveMemory, eventMesh, integrationFramework, outcomeVerification, selfHealingMesh, governanceBrain, adaptiveStrategy, collaborationProtocol, feedbackLoop, confidenceCascade, autonomyScore, scpGoldenSuite), whose tables declare `org_id` NULLABLE and with no FK to organizations.id — a tag rather than a tenant key. That is a REASON TO READ THEM, not a reason to assume they are fine: a nullable untethered org column is how a platform table and a tenant table become indistinguishable. Read these first, because they are the ones whose tables hold customer-shaped data: leadingIndicators.computeLeadingIndicators (user_sessions, user_feedback, user_activation_events), governanceBrainV13.getStats (compliance_snapshots), and the eventMeshEvents reads in leaseExpiryDetector and notePaymentDueDetector. READ ON THE WAY IN, so the next reader does not repeat it: the whole integrationFrameworkV12 family (execute, getCredentials, checkRateLimit, the circuit breaker, the execution log) looked like the worst of the batch — integration_credentials holds ENCRYPTED CREDENTIALS and `execute` resolves one by serviceName with no org — but it is founder-only platform machinery, not tenant data: its single caller is routes-founder-real-runtime.ts, mounted under `app.use(\"/api/founder/v12\", isAuthenticated, requireFounder)`, and `createCredential` has NO caller anywhere, so the orgId column it would populate is vestigial and null on every row. The right fix is unscopedForPlatformOps(reason) plus a decision about whether org_id belongs on these tables at all — not an org predicate. ALREADY BURNED DOWN in the same commit: autopilot/senses.getDealActivitySignal was a genuine platform-wide read — Solene is the founder's brain and 'deal motion in the last 24h' is a company number — and now says so through unscopedForPlatformOps(reason) rather than looking like a forgotten predicate; and the three growthConfigRepo borrower-message methods were read and carry individual _TRIAGED reasons (verified-parent: every caller establishes the note against the org, or holds a note-bound borrower session, before the message read). BURNED DOWN 2026-09-04: leadingIndicators.computeLeadingIndicators — named above as one to read first, because user_sessions / user_feedback / user_activation_events hold customer-shaped data. Read, and the tenancy question turned out to be the smaller half. Every read there IS legitimately platform-wide (these are the FOUNDER's company-level indicators, served by GET /api/founder/leading-indicators behind isFounderAdmin), and each now says so through unscopedForPlatformOps(reason) AT ITS OWN CHAIN ROOT — a shared handle would not do, because the gate's hatch recognition is statement-scoped by design. The larger half: FOUR of the six indicators were FABRICATED — five hardcoded 'daily return rates', three support categories manufactured by arithmetic from one user_feedback count while support_cases.category sat imported and unread, `usagePercent: 75 + i * 8` attached to real organization names, and referral figures derived from an org count. All four are now measured or absent; user_feedback is no longer read at all, which is why its entry went stale. Untriaged, register-wide: 96 → 93 NOT read. BURNED DOWN 2026-09-04, and the reading CORRECTED THE GUESS THIS NOTE MADE. leaseExpiryDetector and notePaymentDueDetector were named above as ones to read first because event_mesh_events sits beside customer-shaped data. They are daily SCHEDULED PLATFORM JOBS (server/jobs/expiryDetectorJobs.ts) that sweep EVERY organization's active leases and notes in one pass and publish a per-org mesh event per finding — organizationId is selected precisely so each event is attributed correctly — so a per-org predicate would make them scan nothing. All four sweeps now say so through unscopedForPlatformOps(reason). The dedupe ledger reads looked worse and are the more interesting half: each asks the mesh channel which dedupeKeys already exist, with no org predicate. They are SAFE, and the reason is the KEY, not the table: every key is `lease-expiry:<lease.id>:<endDate>` or `note-payment:<note.id>:<dueDate>:<classification>`, and both ids are serial PRIMARY KEYS — globally unique, never per-org. So the query asks 'which of MY OWN keys are already on the channel', a cross-org row can only match by carrying an id that cannot collide, and the SELECT returns nothing but keys the caller already holds. It cannot leak and it cannot false-dedupe. That is written into both files beside the query so the next reader does not repeat the investigation. Untriaged, register-wide: 92 → 90 NOT read. COUNTING CORRECTION, 2026-09-04. Two burn-down lines in this note read \"Untriaged block 11 → 8\" and \"8 → 4\". Both were wrong, and wrong in the most misleading direction: they carried the RELATIONAL batch's numbering (22 entries, worked down to 12 and then 11) into this note, which covers the 88-entry ORG_ID batch — so a register whose whole purpose is making debt countable was reporting a block of ninety as a block of four. Corrected to the register-wide NOT-read figure, which is the number the gate itself prints on every run and therefore the only one that cannot drift from the truth: 90 of 310 entries are still unread. Read them from this note's priority list; the count to watch is the verdict line's, not a number typed into prose. BURNED DOWN 2026-09-04, the two entries that read `properties` — the only customer table left in this block, which is why they were taken first. They turned out to be opposites. complianceAI.identifyAffectedProperties is CORRECT and now says so: a regulatory change in a state and county affects every organization holding land there, so the sweep is jurisdiction-wide on purpose and each alert is written against that property's OWN organizationId — a per-org predicate would mean one customer heard about a rule change and the rest did not. It routes through unscopedForPlatformOps(reason) now. negotiationOrchestrator.runNegotiationAssistant was a REAL cross-tenant read, in the worst place: three AI TOOL handlers, every argument supplied by a language model from its own reasoning or from text a counterparty wrote into the thread it was reading. get_property_valuation resolved a property by that id with no org and returned market value, asking price, acreage and location; get_comparable_sales had NO org predicate at all and handed the model every tenant's private sold price as a 'comparable' — a cross-tenant comps plane assembled by accident, against the standing no-comps-data-plane decision; get_negotiation_thread returned a whole thread and its last ten moves, which is counterparty correspondence and the org's own strategy. All three are scoped to the caller now, and negotiation_moves — which has no org column — is scoped through its VERIFIED parent thread rather than the model's argument. LATENT, not live: runNegotiationAssistant has no callers outside its own file. That is the reason it is scoped rather than deleted — an unreachable function with this shape is precisely what someone wires up later believing it safe."
}
