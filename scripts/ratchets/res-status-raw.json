{
  "name": "res-status-raw",
  "description": "raw res.status(...) responses bypass the Errors.* helpers (server/utils/errors.ts) and drift from the { error, message, details?, statusCode } contract. Use Errors.notFound/badRequest/validationFailed/unauthorized/forbidden/limitExceeded/internal instead.",
  "pattern": "res\\.status\\(",
  "globs": [
    "server/**/*.ts"
  ],
  "exclude": [
    "**/*.test.ts",
    "**/*.spec.ts"
  ],
  "baselineCount": 500,
  "direction": "down",
  "lastBumpNote": "504 -> 500 on 2026-08-15: unit 120's metrics consolidation deleted the hand-rolled exposition renderers in middleware/metrics.ts and routes-metrics.ts (four raw res.status sites lived in them). A deletion, not an error-handling improvement. Locked in per this ratchet's strictly-down rule. PRIOR NOTE — 505 -> 504 on 2026-08-12: expansion-ladder gate. requireLadderFlag needed two 404s and the ratchet correctly refused them, so rather than raise the baseline the THREE raw `res.status(404).json({ message: \"Feature not available\" })` calls in server/middleware/featureGate.ts (one pre-existing in requireFlag, two new) became Errors.featureUnavailable — a helper added because a gated feature is not a missing entity: notFound's copy (\"may have been deleted, archived, or moved between organizations\") is three false suggestions about a route that is simply off. Net -1. Locked in per this ratchet's strictly-down rule. PRIOR NOTE — 506 -> 505 on 2026-08-08: Wave-2 agent_investor commission wedge deleted the duplicate server/routes-commissions.ts standalone router (its endpoints are served by the canonical routes-organization.ts mount), removing one raw res.status occurrence. Locked in per this ratchet's down-only rule. PRIOR NOTE — 518 -> 507 on 2026-08-01: reduction earned by the founder-authorized deletion wave (voice + satellite/vision subsystems, five SCP modules, unreached ML registry) — the deleted files carried these occurrences. Locked in the same commit per ratchet rule. PRIOR NOTE — 528 -> 519 on 2026-07-29: platform money-custody purge (founder ruling \"be the rail, not the provider\"). Seven came from deleting server/routes-transaction-fees.ts — a fully stubbed platform escrow/payout router whose handlers hand-rolled 400/404/201/202 responses, including the POST /fees/payouts/trigger placebo that returned 202 \"processing\" for an operation that did nothing. The remainder landed with the borrower/billing card-rail rework in the same wave. Locked in per this ratchet's down-only rule. PRIOR NOTE — 541 -> 528 on 2026-07-29: reduction landed by founder ruling #12(c) Wave A ('Nothing lies'). Dead-surface purge (routes-tax-optimization 501 router, /automation twin, Field Scout) plus the skip-trace and rentals rewrites removed these. Locked in per this ratchet's down-only rule."
}
