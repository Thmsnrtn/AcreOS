# Launch Go/No-Go — 2026-07-08

*The one-page verdict the launch-week doc promised: green/amber/red per
area, with the evidence, and the first campaign recipe. Maintained in
place; re-verdict after each material change.*

## Verdict: **AMBER-GO — conditions met**

PR #112 merged (founder-authorized) and **deployed: acreos.io is on
`603522f6`, verified 2026-07-08 ~02:15Z**. The witnessed, mail-first
$5/day campaign is CLEARED per the recipe below. Remaining ambers are
founder-input-gated or post-launch hardening. Post-deploy verification
found ONE regression (public transparency JSON behind auth — the /api
catch-all trap), fixed + prod-build-verified in PR #113 (docs+hotfix).

## The board

| Area | State | Evidence |
|---|---|---|
| Public/ad landing surfaces | 🟢 | Blank-page env bug fixed + verified LIVE on acreos.io (`5433a1d0`); 140-route render sweep clean |
| Core customer flows | 🟢 | All 5 found bugs fixed; deployed `603522f6` |
| Payments defense | 🟢 | Drilled: Stripe replay = proven no-op; dunning ladder end-to-end every rung incl. scheduled_retry; webhook bad-signature → P0 |
| Kill switches | 🟢 | Drilled: panic stop froze every loop in one POST (dispatch untouched 2.5 min, resume 11s after unflip); hard-stops can never auto-execute (test-pinned) |
| Cost ceilings | 🟢 | $15/day platform backstop (fail-closed), $1/turn chat cap, $50/mo Solene envelope, dispatch kill-cap now ALERTS when it trips; cost attribution corrected (was 3–5× wrong in three modules) |
| Crash recovery | 🟢 | Drill gaps fixed (lease skip, orphan reaper + migration 0199); deployed `603522f6` |
| Alerting | 🟢 | 24 real pages captured across 8 alarm classes: deadman, error-rate spike, AI budget, SLO fast-burn (auto SEV-1), Stripe failure, vendor credential death, panic stop |
| Founder cockpit | 🟢 | Honest tiles + wedge/error/deploy wiring; deployed `603522f6` |
| Brain (Solene) | 🟢 | Grounded + eval'd; deployed `603522f6` |
| Self-birthing roadmap | 🟢 | 14 gates live; deployed `603522f6` |
| PR #112 | 🟢 | MERGED + DEPLOYED as `603522f6`; `/api/version` verified |
| Public transparency JSON | 🟡 | Post-deploy check found anon `/api/transparency` 401'd (registered after the `/api` auth catch-all); fixed + prod-build-verified, ships in PR #113 |
| Credentialed signup E2E in CI | 🟡 | Needs founder: Clerk test creds as GitHub Actions secrets. Manual onboarding-v2 pass was clean end-to-end |
| Alert email + push legs | 🟡 | ntfy leg fully drilled; SES email + VAPID push legs blocked locally by missing creds — verify once in prod post-deploy (creds live in Fly secrets) |
| Mobile cockpit eyeball pass | 🟡 | Layout primitives verified responsive in code; needs one on-device look |
| Wedge E2E extensions | 🟢 | All three automated + green: SMS journey, email-reply leg (real HMAC webhook, fail-closed proven), billing-upgrade journey (Stripe flow self-activates on test keys). Email leg also caught+fixed: inbound emails were invisible in the Inbox door (no inbox_messages writer) — ships in PR #113 |
| DNC scrub vendor | 🟡 | Seam built + tested, inert until vendor pick — **irrelevant to mail-first** (no DNC obligation for mail; TCPA is phone-channel) |
| Meta ad account | 🟡 | Needed only for the separate $5/day paid-ads test, not for mail-first |
| Anything red | — | none |

## First campaign recipe — $5/day, witnessed, mail-first

1. **Prerequisite: MET** — #112 deployed + verified (`/api/version` =
   `603522f6`). Optional final sanity: create one lead in prod and see it
   appear in the list immediately.
2. **Universe:** one county the platform already has data depth in; pull
   ~50 absentee/tax-delinquent land leads (existing import path).
3. **Channel: direct mail only.** No DNC/TCPA exposure; the scrub seam can
   stay dormant. SMS/voice stay off until the vendor pick.
4. **Witnessed mode on:** every send requires the founder's tap; the
   autopilot drafts, the founder releases. Keep `simulationMode` OFF for
   this org but leave global spend switches at current dials.
5. **Volume:** ~20 letters over week one (≈ $2–3/day at current print+post
   rates — under the $5/day line with headroom).
6. **The watch loop:** replies land in Inbox (inbound-SMS fix is live if
   sellers text the tracking number); lead flips `responded`; cockpit
   wedge tiles (outreach/replies/offers, 7d) show the funnel move —
   that's the scoreboard for the week.
7. **Abort handles (drilled):** panic stop freezes everything in one tap;
   dispatch kill-cap and daily AI backstop are armed and now audible.
8. **Success bar for week one:** the machine survives contact — zero
   customer-visible errors, every reply captured, at least one offer
   drafted. Response-rate economics come later; launch week proves the
   plumbing.

*Re-verdict when: PR #113 deploys (flip the transparency amber), founder
inputs land (flip those ambers), or anything above changes state.*
