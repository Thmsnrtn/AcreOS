# Turns 12–13 Cutover Map — lane-3 trust retirement

**Status:** READ-ONLY DISCOVERY, generated 2026-09-01 by workflow run
`wf_b43e6bcf-7b4` (5 mappers + 1 adversarial completeness auditor; 122
touchpoints; per-agent transcripts in the session workflow directory).
Execution stays gated on the durable trust-seam shadow evidence maturing
(~2026-09-07: `GET /api/admin/trust-seam-shadow` must show `seamLooser === 0`
with non-vacuous comparisons over ≥1 week). This map exists so the cutover
commits are mechanical when the evidence licenses them — it authorizes
nothing by itself.

**Program doc:** `docs/autonomous/BRAIN_CONSOLIDATION_STAGE4.md` (turns 12–13,
Decision C). **Discoveries beyond the program doc** (the reason this map was
worth a workflow):

1. **A third caller the shadow never measured** —
   `server/services/agentInitiativeEngine.ts:211` pre-filters proposals via
   `trustAuthorityEscalation.isActionAllowed` with NO shadowCompare, so the
   divergence evidence does not directly cover it. It must flip (or be
   deliberately removed) in the same commit that deletes the service.
2. **Deadman coupling** — deleting the job without its
   `jobRegistry.ts` roster row leaves `deadmanCheck.ts` paging P2
   "trust_evolution dark" forever; the roster row, the `withJobLock` literal,
   and the job must go in ONE commit (`jobRosterCoverage.test.ts` enforces the
   pairing).
3. **Dead telemetry already** — `trustAuthorityEscalation`'s
   `eventMeshPublisher.publish('trust:lifecycle', …)` has ZERO subscribers
   since turn 16 retired its reaction-chain consumer.
4. **Orphan cascade** — flipping both gates orphans
   `companyAgents.effectiveTrustScore` and (post-routes) `updateTrustScore`,
   which in turn orphans `ceoAbsenceService.activeTrustBoosts` — the
   Decision-C clause. Zero-caller canonicals violate the second law; each
   must be deleted or explicitly adjudicated in the flip commit.


## trustEvolution weekly job + trustAuthorityEscalation service (Stage-4 turns 12-13 retirement; flip licensed by durable trustSeam shadow evidence maturing ~2026-09-07 with seamLooser=0 and non-vacuous comparisons per BRAIN_CONSOLIDATION_STAGE4.md turn 11)

- **[delete]** `/home/user/AcreOS/server/jobs/runScheduledJobs.ts:2301` — startTrustEvolutionJob() definition (:2301-2321): hourly tick, Sunday-00:00-UTC guard, dynamic import('../services/trustEvolution'), withJobLock('trust_evolution', 1800, runTrustEvolution). Log strings 'Registering trust evolution job', 'Trust evolution failed', 'Trust evolution import failed' (lane 'sovereign') die with it. This is the ONLY production caller of runTrustEvolution.
  - Also delete the call at :4152 (startTrustEvolutionJob();). runScheduledJobs.ts is under a strictly-down line-count ratchet, so shrinking is compliant.
- **[delete]** `/home/user/AcreOS/server/jobs/jobRegistry.ts:319` — JOB_ROSTER entry { name: 'trust_evolution', intervalMs: WEEK, critical: false }.
  - MUST go in the SAME commit as the withJobLock literal: (a) jobRosterCoverage.test.ts 'no phantom rows' case fails on a roster entry with no withJobLock literal; (b) deadmanCheck.ts walks this roster and would page P2 'trust_evolution dark' forever once the job stops writing jobHealthLogs rows.
- **[verify-only]** `/home/user/AcreOS/tests/unit/jobRosterCoverage.test.ts:73` — Roster <-> withJobLock parity pin. Passes automatically iff both the literal (runScheduledJobs.ts:2315) and the roster entry (jobRegistry.ts:319) are removed together; fails on either alone.
- **[verify-only]** `/home/user/AcreOS/server/utils/jobRuntime.ts:42` — withJobLock writes jobHealthLogs rows keyed jobName='trust_evolution' (skipped_lock always, success sampled 1/hr, failed always). NO dashboard reads that name specifically: routes-job-health.ts SURFACED_JOBS (:47-56) excludes it; autonomousHealthMonitor/burnRate group by jobName generically. The only reader that notices absence is the roster-driven deadman (handled above). Historical rows stay; no migration.
- **[delete]** `/home/user/AcreOS/server/services/trustEvolution.ts:42` — Whole module retires. Internals dying with it: trustEvolutionLog inserts (:193,:223,:240 — the table's ONLY writers), companyAgentService.updateTrustScore (:181), updateMetrics (:185), agentComms broadcast from:'trust_evolution' channel 'incidents' (:266-273), dynamic import of trustAuthorityEscalation.onThresholdCrossed (:211-212), outcomeVerificationQueue read (:122-136), getSetting('trust.*') reads (:53-60).
  - Zero other importers repo-wide (grep 'services/trustEvolution' = runScheduledJobs.ts:2314 only).
- **[delete]** `/home/user/AcreOS/server/services/trustAuthorityEscalation.ts:185` — Whole module retires. Side effects dying with it: agentEvents insert eventType 'authority_promotion' / eventSource 'trust_evolution' (:130-144); eventMeshPublisher.publish('trust:lifecycle','trust:threshold_crossed', publisher 'trust-evolution') (:147-161) — grep shows ZERO remaining subscribers (its reaction-chain consumer died in turn 16, so this is already dead telemetry); wsServer.broadcastFounderEvent('trust_promotion') (:164-170); cognitiveMemoryV13.extractFact dynamic import (:174-180).
- **[edit]** `/home/user/AcreOS/server/services/executionEngine.ts:408` — Caller 1 (turn-12 flip): validateSafetyGates trust check (:408-433) — dynamic import of trustAuthorityEscalation (:410), effectiveTrustScore (:415), isActionAllowed (:416), getTier (:419), plus the fire-and-forget shadowCompare (:428-432).
  - Replace legacy check with direct seamVerdict() consumption, fail-closed (unknown agent/unmapped/seam-error => block-escalate); delete the shadowCompare call — this gate stops shadowing the moment the seam is live here. Mutation-test per turn-12 spec (domain->observe => refusal; ->execute_gated => pass; deleted mapping => escalate).
- **[edit]** `/home/user/AcreOS/server/services/agentAuthorityGate.ts:113` — Caller 2 (turn-13 flip): checkAuthority wrapper (:113-123) fires shadowCompare; checkAuthorityInner's trust-based promotion reads agent.trustScore against TRUST_THRESHOLDS (:269-317). Note it does NOT import trustAuthorityEscalation — its legacy verdict is its own threshold math.
  - Flip trust logic to the seam (doc: behavior-preserving — every live proactive/reaction action is unclassified => level-2 escalate; assert exactly that survives); remove the shadowCompare wrapper. CONSTRAINT: isNeverPromote (:104-106) and NEVER_PROMOTE_ACTIONS derivation MUST SURVIVE — trustSeam.ts:36 imports isNeverPromote from this module, and agentAuthorityCeiling + hardStopLaneCoverage pin it.
- **[edit]** `/home/user/AcreOS/server/services/agentInitiativeEngine.ts:211` — Caller 3 — NOT in the doc's turn-13 list: top-level import (:20) and trustAuthorityEscalation.isActionAllowed(trustScore, proposal.action) pre-filter (:211) with effectiveTrustScore (:207), deciding auto-execute vs escalate BEFORE executionEngine.execute. This call site has NO shadowCompare — the shadow evidence never directly measured it.
  - Flip to seamVerdict (or delete the pre-filter and rely on executionEngine's flipped gate — but that changes the escalate path shape). Must be handled in the same commit that deletes the service or the build breaks; absenceGrantExpires.test.ts pins this file's check shape (see below).
- **[delete]** `/home/user/AcreOS/server/routes-scp-v2.ts:204` — Decision-C surface: GET /api/scp/v2/trust/promotions (:204-212, reads scpGoldenSuite.checkTrustPromotions — file-metrics, not the ledger), POST /api/scp/v2/trust/promote (:218-245) and POST /api/scp/v2/trust/demote (:251-278), both calling companyAgentService.updateTrustScore. Also :405-441: a status endpoint calls checkTrustPromotions (:409) and returns trust_promotions (:441) — decide whether Decision C takes that field too.
  - PATH-STRING grep done (lesson 1): client/src has ZERO 'scp/v2' fetches — no client 404 risk. Server prose mentions of the path: routes.ts:1990 and :2032-2033 (comments — update); the /api/scp/v2 mount at routes.ts:1992 survives for the other sections. Follow the turn-14/15 tombstone pattern.
- **[edit]** `/home/user/AcreOS/server/services/companyAgents.ts:367` — updateTrustScore (:367-377): after the job + scp promote/demote routes retire, production callers = ZERO. effectiveTrustScore (:348-365) folds ceoAbsenceService.activeTrustBoosts (ceoAbsenceMode.ts:131-149); its only consumers are the two flipped call sites (executionEngine:415, agentInitiativeEngine:207), so the flip orphans it — this is exactly the doc's 'ceoAbsence boosts lose their only consumer' clause (Decision C: re-express as witness grants or retire).
  - Delete or explicitly adjudicate both methods in the flip commit — the reachability ratchet is down-only and a canonical function with zero production callers is the repo's second law violation.
- **[edit]** `/home/user/AcreOS/server/services/autopilot/trustSeam.ts:104` — WHICH SIDE OF THE SEAM DIES: the verdict half (seamVerdict + ACTION_DOMAIN_MAP, :40-102) SURVIVES and becomes the live gate in both flipped callers. The shadow half (:104-234 — shadowCompare, in-memory ShadowCounters, persistShadowRow, FLUSH_EVERY, getShadowCounters, the jobHealthLogs writers for 'trustSeamShadow:flush'/'trustSeamShadow:divergence' at :179/:196) DIES once BOTH gates are flipped — its purpose (divergence evidence) is complete.
  - Order matters: turn 12 removes only executionEngine's shadowCompare call (agentAuthorityGate still shadows until turn 13); the shadow half deletes in the turn-13 commit. seamVerdict's isNeverPromote import (:36) keeps agentAuthorityGate alive as a module.
- **[edit]** `/home/user/AcreOS/server/routes-admin.ts:771` — GET /api/admin/trust-seam-shadow (:759-844): the flip's evidence reader — live counters + durable aggregation over jobHealthLogs jobName IN ('trustSeamShadow:flush','trustSeamShadow:divergence'), computing seamLooser/comparisons flipConditions.
  - MUST SURVIVE until the flip is licensed and executed (it is how the ~2026-09-07 evidence is read). After both flips it retires with the shadow half — or is trimmed to durable-rows-only for a post-flip observation window; the getShadowCounters import (:773) breaks if the shadow half is deleted first.
- **[lower-baseline]** `/home/user/AcreOS/scripts/ratchets/reachability.json:118` — Two allowlist entries for trustSeam.ts::seamVerdict and ::ACTION_DOMAIN_MAP (:118-127) with an explicit REVOCATION clause: 'the turn-12 flip makes executionEngine consume seamVerdict directly — these entries then go STALE and the gate FAILS until removed'.
  - Remove both entries in the turn-12 commit (the recorded loud-adoption pattern).
- **[lower-baseline]** `/home/user/AcreOS/scripts/check-org-scoped-fetch.mjs:531` — BASELINE_FUNCTION_OFFENDERS entry 'server/services/trustEvolution.ts::runTrustEvolution' (down-only register; also cited in comments at :125/:517 and tests/unit/orgScopedFetchCoverage.test.ts:91 prose).
  - Drop the entry in the deletion commit — a register entry pointing at deleted code is exactly the stale-exemption defect the three-laws section warns about.
- **[repin-test]** `/home/user/AcreOS/tests/unit/trustFromEvidence.test.ts:148` — Reads trustEvolution.ts source (:134) and asserts runTrustEvolution exists + calls trustDeltaFrom; its own message says 'runTrustEvolution is gone — re-adjudicate this check' (:148). Also unit-tests trustDeltaFrom directly (:41-121).
  - server/services/trustDelta.ts loses its ONLY production importer when trustEvolution.ts deletes (agentPerformanceReviews.ts:104 is an unrelated local property) — delete trustDelta.ts with it and retire/repin this suite; do NOT leave a pure rule tested only against itself (second law).
- **[repin-test]** `/home/user/AcreOS/tests/unit/absenceGrantExpires.test.ts:178` — Scans executionEngine.ts + agentInitiativeEngine.ts (CALL_SITES :178-181) asserting both contain 'isActionAllowed' and 'effectiveTrustScore' and never read '.trustScore' (:191-207). The flip makes ALL of these false in both files.
  - Repin to the new truth: the invariant 'temporary elevations expire by derivation, and gates consume the derived value' either re-expresses against the seam/witness-grant mechanism (Decision C) or retires WITH ceoAbsence — never silently deleted. The :149-161 case (ceoAbsenceMode does not call updateTrustScore) survives as long as ceoAbsenceMode.ts exists.
- **[repin-test]** `/home/user/AcreOS/tests/unit/agentAuthorityCeiling.test.ts:204` — The :204-251 describe drives trustAuthorityEscalation directly (escalate_to_founder allowed at every tier; 4-band vacuity; Observer cannot act). The file's NEVER_PROMOTE half (via real checkAuthority) survives untouched.
  - Repin the tier block's invariants onto seamVerdict: 'the recommended remedy is permitted' and 'lowest state cannot act outwardly'. See risk 2 — the seam currently CANNOT express the first invariant.
- **[repin-test]** `/home/user/AcreOS/tests/unit/safetyGateFailClosed.test.ts:101` — doMocks trustAuthorityEscalation (:101-109) incl. a trustThrows path proving the trust gate fails CLOSED when unevaluable; also mocks effectiveTrustScore (:115).
  - Repin mocks to trustSeam/domainAutonomy; keep the exact invariant — seamVerdict's ledger-read-throws => escalate (trustSeam.ts:87-94) is the new implementation of 'a check that cannot run has not passed'.
- **[repin-test]** `/home/user/AcreOS/tests/unit/executionEngineHonesty.test.ts:55` — vi.mock of trustAuthorityEscalation (:55-60) + companyAgents.effectiveTrustScore (:61-70) to pass the gates so executor honesty is isolated.
  - Swap for a trustSeam/domainAutonomy mock that passes the flipped gate.
- **[repin-test]** `/home/user/AcreOS/tests/unit/trustSeamShadow.test.ts:32` — Drives seamVerdict fail-closed edges + shadowCompare direction accounting; trustSeamShadowPersistence.test.ts pins the jobHealthLogs flush/divergence rows.
  - seamVerdict cases SURVIVE (they now pin a live gate — add turn-12 mutation cases). The shadowCompare/persistence cases retire with the shadow half in turn 13 — rewrite, don't orphan.
- **[edit]** `/home/user/AcreOS/client/src/hooks/use-websocket-channel.ts:86` — 'trust_promotion' WS handler (:86-87); notification-center.tsx maps the type at :48/:68. The only emitter (trustAuthorityEscalation.ts:164) dies.
  - Remove the dead event handling in the same turn (stale-UI rule) or knowingly leave as inert tolerance — but the doc's turn-13 rule is same-turn UI truth.
- **[edit]** `/home/user/AcreOS/server/services/systemTrends.ts:239` — Reads trustEvolutionLog (:23, :239-245) into /api/founder/intelligence/system-trends (routes-founder-intelligence.ts:2533), rendered by founder-trends.tsx (:50, :250; empty-state prose :146-147 promises 'the first trust-evolution cron run' — a cron that will no longer exist).
  - Turn-13 same-turn repoint: either drop the per-agent trust trajectory panel or relabel it as historical/frozen; fix the founder-trends empty-state copy (i18n-candidates.csv:5137/5140 regenerates).
- **[edit]** `/home/user/AcreOS/server/routes-founder-intelligence.ts:3479` — trustEvolutionLog readers that go stale-frozen: daily-briefing trust changes (:118-125), GET /agent-trust-history/:codename (:3479-3491), agent-detail trustHistory (:3519-3523). Client renderers of trustScore per the doc's turn-13 list: agent-performance.tsx:78-90, agent-detail.tsx:122/:282, founder/inspector.tsx:47/:190 (fed by routes-founder-inspector.ts:66-68), command-palette.tsx:406/:1250.
  - Same-turn repoint/remove so no surface renders a frozen score as live (fabrication-adjacent per the doc). The trust_evolution_log TABLE is NOT dropped now — it joins the founder OD-8 drop list with last-write evidence (shared/schema.ts:15063-15080; scripts/migrate.mjs:10478-10494).
- **[migration]** `/home/user/AcreOS/server/services/cognitiveMemoryV13.ts:1` — Still exists; its LAST TWO importers are trustAuthorityEscalation.ts:174 (dynamic) and feedbackLoopV14.ts:12 (STATIC). The doc claims it 'deletes at turn 13 with its last two importers'.
  - The turn-13 commit must also sever feedbackLoopV14's static import (that service was slated to die with its v14 surfaces, but routes-founder-self-running-company is still mounted at routes.ts:2010) — otherwise cognitiveMemoryV13 cannot delete and the doc's claim goes false. Do the zero-caller grep in-commit.
- **[verify-only]** `/home/user/AcreOS/server/services/scpGoldenSuite.ts:355` — checkTrustPromotions (:355) — consumed only by the two routes-scp-v2 call sites (:206, :409). If Decision C removes the whole /trust surface, this export loses its route consumers.
  - Adjudicate with Decision C; note it reads SCP file metrics, not trustEvolutionLog, so it is not evidence-coupled to the job.
- **[verify-only]** `/home/user/AcreOS/server/services/outcomeVerifiers.ts:1` — outcomeVerificationQueue population: trustEvolution.ts was a READER (dimension 3, :122-136). Remaining references: trustDelta.ts (dies too) and outcomeVerifiers.ts itself.
  - Verify a real reader remains after the deletion, else the queue becomes write-only (tablesNoReader deletion-revealed pattern — record it, don't hide it).

  **Risks:**
  - UNSHADOWED THIRD CALLER: agentInitiativeEngine.ts:211 consults trustAuthorityEscalation.isActionAllowed as an auto-execute/escalate pre-filter with NO shadowCompare — the durable evidence window never directly measured this gate's action population (its auto-executes do pass through executionEngine's shadowed gate afterwards, but the escalate-path decision itself is unmeasured). The doc's turn-13 list omits this call site entirely; the cutover must flip it deliberately, not discover it via a compile error.
  - SEMANTIC DIVERGENCE ON THE REMEDY ACTION: legacy makes escalate_to_founder universal at every tier (agentAuthorityCeiling pins 'the remedy the gates recommend is actually permitted'); ACTION_DOMAIN_MAP maps escalate_to_founder -> 'ops', so with ops at observe/draft the seam returns block/escalate for the act of asking permission. This reads as seam-stricter (not flip-blocking under the seamLooser=0 rule) but breaks the pinned invariant post-flip — check the byAction shadow counters for escalate_to_founder before flipping and consider a structural allow in seamVerdict.
  - ORDERING: deleting trustSeam's shadow half before routes-admin.ts:773's getShadowCounters import is trimmed breaks the admin endpoint; deleting the endpoint before the flip destroys the evidence reader that licenses the flip. Sequence: turn 12 (flip executionEngine, drop its shadowCompare, revoke the two reachability allowlist entries) -> turn 13 (flip agentAuthorityGate + initiative pre-filter, retire job/service/routes, delete shadow half + persistence test, trim or retire the admin endpoint).
  - SAME-COMMIT PAIRS or CI/paging breaks: (a) withJobLock literal + JOB_ROSTER entry (jobRosterCoverage parity + deadman P2 phantom-dark page); (b) trustEvolution.ts deletion + check-org-scoped-fetch.mjs:531 register entry; (c) service deletion + the five test files that import/mock it (absenceGrantExpires, agentAuthorityCeiling, safetyGateFailClosed, executionEngineHonesty, trustFromEvidence) — all REPIN, never delete.
  - agentAuthorityGate.ts cannot be retired wholesale even though its trust math dies: trustSeam.ts:36 imports isNeverPromote from it, and the NEVER_PROMOTE derivation from hardStops is pinned by agentAuthorityCeiling + hardStopLaneCoverage. Only the score-threshold logic flips.
  - Post-flip orphans the reachability ratchet will count: companyAgents.updateTrustScore + effectiveTrustScore, ceoAbsenceMode.activeTrustBoosts, trustDelta.ts, scpGoldenSuite.checkTrustPromotions (if Decision C takes the /trust GET). Adjudicate each in the flip commits — ratchets are down-only and a stale-high baseline fails.
  - Frozen-score fabrication: companyAgents.trustScore and trust_evolution_log stop updating at the flip but five client surfaces and three founder API readers keep rendering them as live; the doc mandates the UI repoint in the SAME turn as the job retirement.
  - cognitiveMemoryV13 deletion is blocked by feedbackLoopV14.ts:12's static import unless that is severed in the same turn — the doc's 'deletes with its last two importers' claim is currently only half-satisfiable.

  **Notes:** Evidence gate confirmed from the program doc: turns 12-13 execute only after >=1 week of DURABLE shadow evidence (jobHealthLogs rows 'trustSeamShadow:flush'/'trustSeamShadow:divergence', aggregated at GET /api/admin/trust-seam-shadow ?sinceDays) with seamLooser=0 and comparisons>0; any seam-LOOSER divergence blocks and reopens ACTION_DOMAIN_MAP. Clock restarted at the 2026-08-31 persistence fix; matures ~2026-09-07.

Seam verdict (asked directly): server/services/autopilot/trustSeam.ts SPLITS — seamVerdict + ACTION_DOMAIN_MAP (:40-102) are the SURVIVING side and become the live gate in executionEngine.validateSafetyGates (turn 12), agentAuthorityGate.checkAuthorityInner (turn 13), and the unlisted agentInitiativeEngine pre-filter; the shadow-comparison side (:104-234: shadowCompare, in-memory counters, persistShadowRow/jobHealthLogs writers, getShadowCounters) DIES with the second flip, together with trustSeamShadowPersistence.test.ts and (eventually) the admin evidence endpoint.

Job-name-string inventory ('trust_evolution'): runScheduledJobs.ts:2315 (withJobLock -> jobHealthLogs rows), jobRegistry.ts:319 (roster/deadman), trustEvolution.ts:267 (agentComms broadcast 'from'), trustAuthorityEscalation.ts:133 (agentEvents eventSource) and :159 ('trust-evolution' eventMesh publisher tag). No dashboard reads jobHealthLogs by the name 'trust_evolution' (routes-job-health SURFACED_JOBS excludes it); only the roster-driven deadman notices absence. 'trustSeamShadow:*' rows: written trustSeam.ts:179/:196, read routes-admin.ts:784-787 and the persistence test.

Path-string audit (hard lesson 1): zero client fetches of 'scp/v2' — the /api/scp/v2/trust removal cannot 404 any client; the only path mentions outside routes-scp-v2.ts are comments in routes.ts:1990/:2032. All line numbers verified against the working tree at HEAD on 2026-09-01.

## /api/scp/v2/trust route surface (3 routes in server/routes-scp-v2.ts) — stage-4 turn 13, founder Decision C; executes only after the durable shadow window matures (~2026-09-07, seamLooser=0, non-vacuous comparison count) and after turn 12

- **[delete]** `server/routes-scp-v2.ts:204` — Route registration: GET /api/scp/v2/trust/promotions (handler 204-212, doc comment 200-203). Calls scpGoldenSuite.checkTrustPromotions().
  - Delete the whole '─── Trust Promotion/Demotion ───' section, lines 198-278. NOTE: the program doc cites :208-269 — that range has DRIFTED; cut by the section boundaries, not the doc's stale line numbers.
- **[delete]** `server/routes-scp-v2.ts:218` — Route registration: POST /api/scp/v2/trust/promote (handler 218-245). Body {agent, new_trust_score}; calls companyAgentService.getByCodename + updateTrustScore. Contains 'currentAgent.trustScore ?? 50' at :231 (measurement-defaults register key).
  - Dies inside the 198-278 section cut.
- **[delete]** `server/routes-scp-v2.ts:251` — Route registration: POST /api/scp/v2/trust/demote (handler 251-278). Identical shape to promote; second 'currentAgent.trustScore ?? 50' at :264.
  - Dies inside the 198-278 section cut.
- **[edit]** `server/routes-scp-v2.ts:409` — GET /api/scp/v2/briefing (route at :402) imports checkTrustPromotions at :405, computes at :409, and renders a 'trust_promotions' field at :441 — a second consumer of the trust read-facade OUTSIDE the trust subtree.
  - SAME COMMIT: remove the checkTrustPromotions import/call and the trust_promotions response field. Left in place it would render promotion suggestions computed from a frozen trustScore as live — exactly the fabrication-adjacent 'frozen score rendered as live' hazard turn 13 exists to prevent — and it becomes a dead import once checkTrustPromotions deletes.
- **[edit]** `server/routes-scp-v2.ts:6` — File header doc comment advertises 'trust promotion/demotion' as a capability of this file.
  - Trim the header; also the perimeter-guard comment at :47 ('including trust promote/demote'). The guard itself at :51 (app.use('/api/scp/v2', isAuthenticated, requireFounder)) STAYS — 12 other /api/scp/v2 routes survive turn 13.
- **[edit]** `server/routes.ts:1988` — Mount-layer guard comment (F-D32, 1988-1991) cites '/trust/demote, or /trust/promote' as the attack examples; the guard app.use('/api/scp/v2', ...) at :1992 stays.
  - Comment-only edit so the security note doesn't cite routes that no longer exist. Do NOT remove the :1992 guard — the rest of the scp/v2 surface still needs it.
- **[delete]** `server/services/scpGoldenSuite.ts:355` — checkTrustPromotions() (355-~415) — reads companyAgents.trustScore from DB (:367-373, keyed `${agent}_cto`) and suggests promotions; plus TrustPromotionSuggestion interface (343-349) and the export at :459.
  - After the trust route and the briefing field go, production call sites = ZERO (verified: only routes-scp-v2.ts:206-207 and :405/:409; no test covers it — scpGoldenSuite.test.ts never mentions it). A canonical function with zero production callers is not canonical; leaving it also nudges the reachability unreachedExports count.
- **[delete]** `server/services/companyAgents.ts:367` — updateTrustScore(codename, delta) (367-377) — the trust-score mutation primitive. Callers today: routes-scp-v2.ts:232 + :265 (this surface) and trustEvolution.ts:181 (weekly job retired the same turn). Zero callers after turn 13.
  - Retire per the program doc's lane-3 list ('companyAgents.updateTrustScore'). CAREFUL: effectiveTrustScore (:348) is a DIFFERENT function — it feeds executionEngine.ts:415 and agentInitiativeEngine.ts:207 and is what carries ceoAbsenceMode boosts; its fate is the turn-12/13 gate flips + Decision C, not this deletion.
- **[lower-baseline]** `scripts/check-measurement-defaults.mjs:128` — BASELINE register entry "server/routes-scp-v2.ts::currentAgent.trustScore ?? 50" — matched only by the promote (:231) and demote (:264) handlers being deleted.
  - The script exits 1 on STALE baseline entries (:345-355: 'A stale-high baseline is free headroom for the next invented number'). Deleting the handlers without removing this line turns CI red — remove the entry in the SAME commit. This is the designed loud-adoption behavior, not an accident.
- **[lower-baseline]** `scripts/check-org-scoped-fetch.mjs:531` — Baseline entry "server/services/trustEvolution.ts::runTrustEvolution" (rule-1 no-org allowlist; also cited in comments at :125 and :517, and in orgScopedFetchCoverage.test.ts:91).
  - Script exits 1 on stale allowlist entries (:88-89). When trustEvolution.ts deletes in the same turn, remove this entry (and the two explanatory comment mentions) in the SAME commit.
- **[delete]** `server/jobs/runScheduledJobs.ts:2300` — startTrustEvolutionJob() (~2300-2321): hourly interval that fires runTrustEvolution Sunday 00:00 UTC under withJobLock('trust_evolution', ...) at :2315. Doc's cited range 2301-2321 still approximately holds.
  - Unregister the job AND its call site in the same file's startup sequence. This is the writer whose retirement makes every remaining trustScore rendering 'frozen' — which is why the doc mandates the UI repoint in the SAME turn.
- **[delete]** `server/jobs/jobRegistry.ts:319` — JOB_ROSTER entry { name: "trust_evolution", intervalMs: WEEK, critical: false } — a job-name string table (dynamic dispatch). Consumed by routes-job-health.ts:31 (founder job-health panel) and jobs/deadmanCheck.ts:59 (activeRosterEntries — dead-man alarm).
  - SAME COMMIT as the runScheduledJobs unregistration. Left behind, deadmanCheck alarms weekly on a job that never runs and job-health advertises a phantom job. tests/unit/jobRosterCoverage.test.ts pins withJobLock-literal ↔ JOB_ROSTER parity, so removing either side alone is a red build — the pin forces the pair.
- **[delete]** `server/services/trustEvolution.ts:181` — runTrustEvolution — the weekly mutation engine (updateTrustScore call at :181; writes trust_evolution_log, eventSource 'trust_evolution').
  - Whole-file retirement per turn 13 lane-3 list. Its deletion cascades into check-org-scoped-fetch baseline (:531) and trustFromEvidence.test.ts (below).
- **[delete]** `server/services/trustAuthorityEscalation.ts:1` — Tier/ceiling module (getTier, isActionAllowed) — lane-3 machinery and one of the last two cognitiveMemoryV13 importers (per turn-17 note).
  - Retires at turn 13 with the agentAuthorityGate flip. Its mocks/imports in three tests must move to the seam (see repin entries). agentInitiativeEngine.ts:20,:211 also imports it — that call site flips to the seam in the same turn.
- **[repin-test]** `tests/unit/trustFromEvidence.test.ts:134` — fs.readFileSync of server/services/trustEvolution.ts source; its own assertion message at ~:147 says 'runTrustEvolution is gone — re-adjudicate this check'. Pins: delta computed through trustDeltaFrom; no self-reported success count in the authority path.
  - The invariant ('authority derives from verified evidence, never self-reported success') survives the engine — repin it against the seam (trustSeam ACTION_DOMAIN_MAP / domain-ledger verdict path), never delete. The test even anticipates this.
- **[repin-test]** `tests/unit/agentAuthorityCeiling.test.ts:216` — Imports trustAuthorityEscalation directly (216-247): pins that no trust score can unlock escalate-class actions and the tier ladder labels.
  - The program doc keeps agentAuthorityCeiling.test.ts as a load-bearing ratchet (hard-stop classes structurally unmappable). Rewrite the assertions to the seam's ceiling (hardStops.ts derivation over ACTION_DOMAIN_MAP) in the same commit that deletes trustAuthorityEscalation.ts.
- **[repin-test]** `tests/unit/safetyGateFailClosed.test.ts:101` — vi.doMock of ../../server/services/trustAuthorityEscalation (101-104) inside the fail-closed suite.
  - Mock target vanishes when the module deletes; rewire the mock/fixture to the seam so the fail-closed pins (unknown agent / unmapped action / seam error ⇒ block-escalate) keep running. Same for executionEngineHonesty.test.ts:55-56 (vi.mock of the same module).
- **[repin-test]** `tests/unit/absenceGrantExpires.test.ts:149` — Pins (by source scan, 149-161) that ceoAbsenceMode.activate does NOT call updateTrustScore — the boost is derived via activeTrustBoosts/effectiveTrustScore instead.
  - Green through the updateTrustScore deletion (absence of a symbol stays absent). BUT it reads ceoAbsenceMode.ts source: if Decision C retires ceoAbsenceMode outright, the readFileSync throws — repin to whatever replaces it (witness-grant issuance) or retire with an explicit tombstone note, per the founder's Decision C ruling.
- **[verify-only]** `tests/unit/agents/scpRoutes.test.ts:68` — Only test importing routes-scp-v2 — it exercises handleTalkQuery exclusively; NO test anywhere exercises any /api/scp/v2/trust path (repo-wide URL-path grep over tests/: zero hits).
  - Untouched by the trust-section deletion; run it to confirm. There is no supertest/e2e coverage of the trust routes to remove or repin.
- **[verify-only]** `tests/unit/__snapshots__/routeManifest.test.ts.snap:233` — 'routes-scp-v2.ts' in the sorted manifest-files snapshot; server/routeManifest.ts:275 carries the file-level entry (mountPath null, kind register).
  - Both are FILE-level, not path-level — removing three routes inside the file changes neither. They become edits only if/when the whole routes-scp-v2.ts file retires (a later stage; sovereign-protocol/agents/ARCHIVED.md:17 ties the archived agent-dir removal to that full retirement, not to turn 13).
- **[verify-only]** `client/src:1` — CLIENT CONSUMERS: ZERO. Case-insensitive grep for 'scp' across all of client/src returns no matches (no fetch/axios/queryKey/hook string, no dynamic '/api/scp/' + suffix construction possible without the substring). Repo-wide grep for '/api/scp/v2/trust' hits only server/routes-scp-v2.ts, the program doc, and routes.ts comments.
  - Nothing in the UI calls this surface today, so retirement 404s no client fetch — there is no component-removal-vs-empty-state decision FOR THIS SURFACE. Re-run both greps in the cutover commit as the in-commit proof (the turn-15 lesson: path-string grep, not symbol grep).
- **[edit]** `client/src/pages/agent-performance.tsx:78` — SAME-TURN COUPLED (not consumers of this surface): the five trustScore renderings the doc names — agent-performance.tsx:78-90, agent-detail.tsx:122 + :282, founder/inspector.tsx:47 + :190, founder-trends.tsx:50 + :250, command-palette.tsx:406 + :1250. All are fed by OTHER endpoints (founder inspector/intelligence/systemTrends reading companyAgents.trustScore and trust_evolution_log), not /api/scp/v2/trust.
  - Once trustEvolution stops mutating scores, these show a frozen number as live — the doc mandates repoint-or-remove in the SAME turn. Honest post-cutover state: either render the seam's domain authority level (the new truth) or drop the trust chip entirely; a frozen '73%' is fabrication-adjacent, an empty-state is honest. (marketplace.tsx:944-946 seller trustScore and achievement-progress.tsx:154 /1000 score are DIFFERENT concepts — do not touch.)
- **[migration]** `shared/schema.ts:15063` — trust_evolution_log table (+ indexes :15078-15079; created in scripts/migrate.mjs:10478-10494). Writer: trustEvolution.ts (dies turn 13). Surviving readers: routes-founder-inspector.ts:66-68, routes-founder-intelligence.ts:119-124/:3483-3485/:3520-3522, systemTrends.ts:239-245 (feeds founder-trends.tsx).
  - NOT on the doc's Decision-G drop list — flag it. After turn 13 it is read-only history; the founder-intelligence 'promotionSuggested' query (:122) will return an ever-aging window presented as recent. Cutover should either label these reads as historical or queue the table for the OD-8 drop batch WITH last-write evidence, after the reader panels repoint. Nothing drops inside the code turn itself.
- **[edit]** `server/services/ceoAbsenceMode.ts:131` — activeTrustBoosts() (:131) → companyAgents.effectiveTrustScore (:348, boost merge :358) → consumed at executionEngine.ts:415 and agentInitiativeEngine.ts:207 — the boost's ONLY consumers, and both flip to the seam at turns 12-13.
  - Decision C's second half: after the flips, an active absence confers nothing while the feature still reports success — a silent no-op (fabrication-adjacent). The founder must rule: re-express as witness-grant issuance, or retire the absence concept. Do not leave the dead lever wired.
- **[verify-only]** `server/openapi-spec.ts:1` — OpenAPI surfaces: openapi-spec.ts and openapi-reflector.ts contain zero scp/trust entries (grepped).
  - No API-doc entry names these paths; nothing to remove.
- **[edit]** `docs/autonomous/BRAIN_CONSOLIDATION_STAGE4.md:184` — Program-doc turn-13 spec (:184) and Decision C (:266); also deletion-ledger.md:21/:438, BLOCKERS.md:1248, EXECUTION_LEDGER.md:6946, solene-kernel-restructure-verification.md:22, sovereign-protocol/agents/ARCHIVED.md:6/:17 — prose mentions of the surface.
  - Cutover commit records turn 13 as DONE in the program doc (matching the DONE pattern of turns 14-18) and notes Decision C's ruling. Other docs are historical ledger prose — annotate only where they claim the surface is live (ARCHIVED.md's removal trigger is the FULL /api/scp/v2 retirement, which turn 13 is not — leave its trigger armed).

  **Risks:**
  - EVIDENCE GATE: nothing here executes before the durable shadow window matures (~2026-09-07) with seamLooser = 0 and a non-vacuous durable comparison count from GET /api/admin/trust-seam-shadow (durable.* over jobHealthLogs, not the process-memory counters), and turn 12 (executionEngine flip) must land first. Any seam-LOOSER divergence blocks the flip and reopens ACTION_DOMAIN_MAP.
  - Doc line drift: the program doc cites routes-scp-v2.ts:208-269 for the trust section; the real section is :198-278 (routes at :204/:218/:251). Cutting the doc's range would leave the GET /trust/promotions registration (:204) alive — a partial retirement. Cut by section boundaries verified in-commit.
  - Briefing coupling is the easiest miss: GET /api/scp/v2/briefing (routes-scp-v2.ts:405/:409/:441) renders trust_promotions from the same machinery but lives outside the trust subtree. Deleting only the /trust/* routes leaves a founder surface presenting frozen-score promotion suggestions as live — the exact fabrication class turn 13 exists to prevent.
  - Two gates are DESIGNED to go red on this deletion: check-measurement-defaults.mjs (stale BASELINE entry :128 → exit 1) and check-org-scoped-fetch.mjs (stale allowlist :531 → exit 1). The cutover commit must remove both entries (baselines move down in the same commit) or CI blocks — do not 'fix' this by re-adding the entries.
  - Job-registry parity: unregistering startTrustEvolutionJob without deleting jobRegistry.ts:319 leaves deadmanCheck alarming on a phantom weekly job and routes-job-health advertising it; jobRosterCoverage.test.ts pins the parity so a one-sided removal is a red build — treat that red as the forcing function, not an obstacle.
  - Four tests import or mock the dying modules (trustFromEvidence:134 reads trustEvolution.ts source; agentAuthorityCeiling:216-247, executionEngineHonesty:55, safetyGateFailClosed:101 import/mock trustAuthorityEscalation). Per program law, each is REPINNED to the seam's equivalent invariant in the same commit — never deleted. agentAuthorityCeiling is explicitly load-bearing for R2 (authority widening).
  - trust_evolution_log is absent from the Decision-G drop list yet loses its only writer at turn 13 while three founder read-paths (inspector, intelligence, systemTrends→founder-trends) keep querying it — an aging window silently presented as recent activity. Either queue it for OD-8 with last-write evidence after repointing the readers, or label those panels historical in the same turn.
  - ceoAbsenceMode becomes a silent no-op after the flips (its boosts reach authority only via effectiveTrustScore at executionEngine:415 and agentInitiativeEngine:207, both moving to the seam): an 'I'm away' activation would report success and confer nothing. Decision C must rule witness-grant re-expression vs retirement before or in the same turn.
  - Client-side is clean TODAY (zero scp strings in client/src) — but the five coupled trustScore renderings are fed by surviving endpoints, so they will not error-card; they will keep rendering confidently-stale numbers, which is worse. The turn-15 lesson (useDelegationTokens error-carding for a deploy cycle) does not repeat here in 404 form; it repeats in frozen-data form unless the UI repoint lands in the same commit.
  - Partial-retirement ambiguity: several artifacts (sovereign-protocol/agents/ARCHIVED.md, routeManifest entry, the :1992 auth mount, the file-level snapshot) key off the FULL /api/scp/v2 retirement, which turn 13 is not. Touching them now would misrecord scope; leaving them is correct — record that explicitly so a later audit doesn't read them as residue.

  **Notes:** Surface inventory (server/routes-scp-v2.ts, mounted via server/routes.ts:2036-2037 with founder-only guards at routes.ts:1992 and routes-scp-v2.ts:51): GET /api/scp/v2/trust/promotions (:204), POST /api/scp/v2/trust/promote (:218), POST /api/scp/v2/trust/demote (:251) — the '─── Trust Promotion/Demotion ───' section spans :198-278. Method used: URL-path-string grep repo-wide ('/api/scp/v2/trust', '/api/scp', 'scp' case-insensitive over client/src), then symbol grep (checkTrustPromotions, updateTrustScore, trustEvolution, trustAuthorityEscalation, trust_evolution), then dynamic-dispatch hunt (jobRegistry job-name table, JOB_ROSTER consumers routes-job-health.ts:31 + deadmanCheck.ts:59, routeManifest, openapi-spec/reflector, measurement-defaults and org-scoped-fetch string registers, reachability.json). Client consumers: NONE — the only 'scp' hits outside server/docs are sovereign-protocol/agents/ARCHIVED.md prose. The reachability.json routes-scp-v2 mention (:129) is inside lastBumpNote prose (no action); its trustSeam SHADOW exemptions (~:120-127) go stale at the TURN-12 flip, not turn 13. checkTrustPromotions has exactly two call sites, both in routes-scp-v2.ts (trust route :206-207 and briefing :405/:409); updateTrustScore has exactly three (promote :232, demote :265, trustEvolution.ts:181) — all die in this turn, so both functions retire with zero-caller in-commit greps. The cutover commit shape: delete section :198-278 + briefing field + the two service functions + trustEvolution.ts + trustAuthorityEscalation.ts + runScheduledJobs :~2300-2321 + jobRegistry.ts:319; lower two script baselines; repin four tests to the seam; repoint/remove five trustScore UI renderings; record Decision C and mark turn 13 DONE in the program doc; prove with re-run path greps + npm run check/test/build.

## Client trustScore surfaces — Stage-4 turn-13 same-turn UI repoint (retirement of lane-3 trust mutation machinery: trustEvolution weekly job, trustAuthorityEscalation, /api/scp/v2/trust, companyAgents.updateTrustScore)

- **[edit]** `client/src/pages/agent-detail.tsx:122` — Trust level card: trustPct = agent.trustScore || 50 (:122), trustBadgeColor/trustLabel badge (:172), progressbar (:174-186), describeTrust prose (:188). Data: companyAgents.trustScore via GET /api/founder/intelligence/company-agents/:codename/detail (routes-founder-intelligence.ts:3497-3543). Route /founder/agents/:codename (App.tsx:1778); only inbound link is command-palette (:1243). HIGHEST fabrication risk: describeTrust renders present-tense authority claims ('They act on their own for most things') over a score frozen at the flip.
  - Repoint the card to the surviving domain-autonomy ledger (trustSeam verdict / domain levels) or remove it. Line 282 trustLabel(action.trustScoreAtTime) is a historical audit value (agent_action_log.trust_score_at_time) — honest as a record, but trustLabel phrasing has no past-tense marker; relabel or render raw. Also remove the :22 import once the card goes.
- **[edit]** `client/src/pages/founder/inspector.tsx:190` — AgentInspector header badge 'trust {agent.trustScore}' (:190, type :47) — live claim from companyAgents.trustScore; plus 'Trust curve' section (:53-60, :204-229) rendering trust_evolution_log entries (server routes-founder-inspector.ts:64-69,140,146). The log goes append-dead when runScheduledJobs.ts:2314 unregisters the weekly job.
  - Repoint badge to domain-autonomy level; either remove the Trust curve section or relabel it explicitly historical ('evolution log retired <date>'). Its 'No trust events yet' empty state (:206) becomes a permanent forever-state otherwise.
- **[edit]** `client/src/pages/founder-trends.tsx:143` — 'Per-agent trust trajectory' card (:138-155) + AgentTrustChart (:246-288) + AgentTrustPoint/agentTrustTrajectories types (:47-51, :63). Data: systemTrends.agentTrustSeries reading trustEvolutionLog (server/services/systemTrends.ts:235-252) via GET /api/founder/intelligence/system-trends. EmptyState copy (:146-148) says 'Come back after the first trust-evolution cron run' — a permanent lie once the cron is gone. Page description (:89) sells 'per-agent trust'.
  - Remove the card + AgentTrustChart (cascade: chart's only parent is this card) + the agentTrustTrajectories field, and edit :89 copy — or repoint to domainAutonomy level-change history. MUST land with a matching server edit to systemTrends.ts:235-252 in the same commit so client/server shapes stay in sync.
- **[edit]** `client/src/components/command-palette.tsx:1250` — Founder agents search group subtitle '{a.wing} wing · trust {a.trustScore}' (:1250), FounderAgentHit type (:406). Data: GET /api/founder/intelligence/search selecting companyAgents.trustScore (routes-founder-intelligence.ts:2571-2583).
  - Drop trustScore from the subtitle and the FounderAgentHit type; strip the column from the server search select in the same commit so the payload stops shipping a frozen number.
- **[edit]** `client/src/pages/agent-performance.tsx:78` — Trust tab (route /agent-performance, App.tsx:1837): badge (:78-86), TrustScoreBar score={agent.trustScore ?? agent.trust ?? 0} (:90). Data: useAgentRuntimeStates → GET /api/founder/v12/runtime → raw agent_runtime_state rows (schema.ts:16250-16276) which have NO trustScore/trust/decisionAccuracy/taskCompletionRate columns — the tab renders 'N/A' badges and a 0% 'Overall trust' bar TODAY. Never-fed surface, fabrication-adjacent independent of the flip.
  - There is nothing to repoint FROM — decide: remove the trust renderings (TrustScoreBar :18-39 cascades, all its uses are in this tab) or feed the tab from the domain-autonomy ledger. Also update sidebar copy that promises trust: layout-sidebar.tsx:769 ('Per-agent trust + decision quality metrics') and nav-items.ts:250.
- **[delete]** `client/src/components/founder/MorningBriefing.tsx:130` — ORPHANED component — zero importers anywhere in client/src (only near-miss is the unrelated UserMorningBriefing). Renders trustUpdates (:34-37, :46, :199-219) from GET /api/founder/intelligence/morning-briefing, whose trustChanges block reads trustEvolutionLog promotionSuggested rows (routes-founder-intelligence.ts:117-125). A trust surface the program doc's five-file list misses.
  - Delete the whole file (no parent dies — none exists). The morning-briefing endpoint survives for other fields; the two invalidateQueries of its key (founder-decisions.tsx:913, SwipeDecisionCard.tsx:124) are harmless and can stay. The server trustChanges block can be removed with lane 3.
- **[edit]** `client/src/lib/trust-language.ts:74` — Trust-score language: describeTrust (:74), trustLabel (:87), trustBadgeColor (:98) — sole consumer is agent-detail.tsx:22. describeTrustChange (:109) and describePromotion (:120) have ZERO consumers already (dead exports today).
  - Delete the five trust-score functions once agent-detail's card is repointed. DO NOT delete the file: AGENT_* maps + naturalConfidence/naturalUrgency/naturalRisk/naturalItemType have live consumers (founder-decisions, SwipeDecisionCard, DecisionsInbox, AgentTeamChat, MorningBriefing-user side), and tests/unit/decisionsDoorPlainLanguage.test.ts:29,57-64 reads the file and pins naturalConfidence.
- **[delete]** `server/routes-founder-intelligence.ts:3479` — GET /agent-trust-history/:codename (:3479-3491) — ZERO client callers (URL-path grep of client/src: clean). Also the company-agents detail payload ships trustHistory (:3518-3523, :3537) that the client never destructures (agent-detail reads only {agent, liveData, recentActions, goals}).
  - Delete the orphan endpoint and drop the unused trustHistory field with lane 3 — client-lens residue of the same class as the turn-15 useDelegationTokens miss, inverted (server route with no client consumer instead of client hook with no server route).
- **[delete]** `server/routes-scp-v2.ts:204` — The /api/scp/v2/trust surface is THREE routes, not two: GET /trust/promotions (:204-212, calls scpGoldenSuite.checkTrustPromotions) plus POST /trust/promote (:218-245) and /trust/demote (:251-276) — the doc's ':208-269' names only promote/demote. Client URL-string grep for 'scp' (case-insensitive): zero matches — no client 404 risk on deletion.
  - Retire all three together (Decision C); tombstone per house pattern. Doc's cited line range is stale — use these.
- **[repin-test]** `tests/simulation/sim-founder-journey.spec.ts:203` — Test '5. Agent detail page shows trust score and authority levels' (:203-253) asserts /trust level|trust score/i text on /founder/agents/:codename — but via an OR with authority/audit/chat (:253), so removing the trust card leaves it GREEN while it silently stops testing anything about trust. Also :336-344 visits /agent-performance with a /trust|performance|score/ OR-regex.
  - Rewrite the assertions to the new truth (domain-autonomy rendering) in the same commit — an OR-pin that cannot go red is exactly the decoration class the three laws forbid. Do not delete the test.
- **[repin-test]** `tests/production-smoke.spec.ts:140` — Smoke route list contains '/agent-performance' (:140) and '/founder/agents' (:131); tests/e2e/route-sweep.spec.ts has the same at :148 and '/founder/trends' at :154.
  - If the cutover repoints page content, lists stay green untouched. If any route is retired/redirected, keep the path listed to pin the redirect (the file's own recorded convention, :133-135) — never silently drop the entry.
- **[repin-test]** `tests/unit/trustFromEvidence.test.ts:134` — Reads server/services/trustEvolution.ts SOURCE at :134 and imports trustDelta (:41) — goes red the moment turn 13 deletes trustEvolution.ts.
  - Repin the invariant ('trust may only be granted by evidence the agent did not author') onto whatever survives — if the entire evidence→score pipeline dies, the invariant re-expresses on the trustSeam/domain-ledger promotion path (promotionRequest founder tap). Related same-commit rewrites the doc already schedules: agentAuthorityCeiling.test.ts:39, safetyGateFailClosed.test.ts:112, absenceGrantExpires.test.ts:198-204 (its .trustScore reader-scan population changes when agentAuthorityGate flips to the seam).
- **[verify-only]** `client/src/components/achievement-progress.tsx:59` — Renders 'Trust Score: N/1000 — X tier' (:154-157) from GET /api/investor-network/trust-score (routes-epic-services.ts:298 → computeInvestorTrustScore, deal-based investor-network domain).
  - DIFFERENT trust domain — survives turn 13 untouched. Listed so the cutover does not overreach on a grep hit.
- **[verify-only]** `client/src/pages/marketplace.tsx:942` — Seller TrustBadge branch (:942-950) is DEAD CODE today: the browse endpoint returns seller:{id,name} only (services/marketplace.ts:86-96) and no trust_tier column exists anywhere in shared/schema.ts, so seller.trustTier is always undefined and the else-branch hardcodes '— Verified Investor' for EVERY seller. components/trust-badge.tsx's only consumer is this dead branch.
  - Different (investor-network) trust domain — NOT turn-13 scope. But flag separately: the unconditional 'Verified Investor' label is a fabricated claim (lint:no-fabrication territory) and the dead TrustBadge branch + trust-badge.tsx are removable in their own cleanup.
- **[verify-only]** `client/src/pages/founder/trust-graduation.tsx:58` — Reads /api/founder/graduation — Pillar R per-category graduation rail (agent_trust_graduation, manual→notify_only→silent), a separate mechanism from the companyAgents.trustScore ledger. Likewise data-sources.tsx:93 (/api/trust/data-sources) and landing/Positioning.tsx:97 (/api/trust/verticals) are the public maturity registry.
  - All survive turn 13. They are the reason 'grep trust' overmatches — the cutover must distinguish four trust domains: agent ledger (dying mutation rail), Pillar-R graduation, investor-network, public maturity registry.
- **[edit]** `client/src/lib/nav-items.ts:250` — Nav entries pointing at trust-selling surfaces: 'Agent performance' :250 (href /agent-performance) and 'System trends' :278; layout-sidebar.tsx:746 ('90-day trust gauge') and :769 ('Per-agent trust + decision quality metrics').
  - Update descriptions in the same turn if the pages' trust content is removed — a sidebar promising 'per-agent trust' over a page that no longer shows it is the small-print version of the stale-render problem.

  **Risks:**
  - Stale-UI fabrication (the doc's recorded R-secondary): agent-detail's describeTrust renders present-tense autonomy claims ('acts on their own for most things') from companyAgents.trustScore, which freezes at the flip — company_agents is NOT dropped in stage 4, so the frozen number stays readable forever. Same-turn repoint is mandatory, and consider a small ratchet enumerating client readers of companyAgents.trustScore so a future surface cannot quietly resume rendering it.
  - founder-trends' EmptyState literally instructs 'Come back after the first trust-evolution cron run' — after turn 13 that cron never runs again; on any fresh environment the card promises data that can never arrive. Copy must die with the job.
  - trust_evolution_log is MISSING from the section-3 OD-8 drop list even though turn 13 makes it append-dead; it currently has four readers (inspector trustCurve, systemTrends.agentTrustSeries, morning-briefing trustChanges, detail trustHistory). Either the client/server repoints land first and the table joins the drop list with last-write evidence, or frozen-curve surfaces linger indefinitely. Flag to the program owner.
  - agent-performance's trust tab was NEVER fed — agent_runtime_state has no trustScore column, so the doc's prescribed 'repoint' has no FROM side; the tab already renders a 0% 'Overall trust' bar that was never data (fabrication-adjacent today, pre-existing). The cutover must choose remove-vs-feed explicitly rather than assume a live feed exists.
  - sim-founder-journey test 5 pins agent-detail trust text behind an OR-assertion (:253) that stays green after the trust card is removed — the pin silently stops testing instead of failing. Repin explicitly (lesson: a gate that cannot go red is decoration).
  - Server payloads keep shipping the frozen score even after client renderings die: /search (:2577), /company-agents (:3156-3159), /company-agents/:codename/detail (:3532), /agent-activity (:1730), /company-briefing agentTeam (:3118) and the briefing-writer inputs (:148 — trustScore is fed INTO generateBriefingUpdates, so a stale number can resurface in AI-written founder prose). Strip the field server-side in the same turn or record the residue.
  - Population lesson applied — the doc's five-file list is INCOMPLETE: founder/MorningBriefing.tsx (orphaned, renders trustUpdates) and the dead-but-grep-matching marketplace/achievement-progress surfaces exist; and /api/scp/v2/trust is three routes, not the two the doc names. URL-path greps for scp/v2 and the retiring intelligence endpoints came back clean in client/src, so no useDelegationTokens-style 404 lurks — but re-run those path greps in the cutover commit itself.
  - Marketplace hardcodes '— Verified Investor' for every seller with no verification behind it (dead trustTier branch) — out of turn-13 scope but a standing no-fabrication violation worth its own ticket.

  **Notes:** Read-only map for BRAIN_CONSOLIDATION_STAGE4 turns 12-13 (flip licensed ~2026-09-07 by durable shadow evidence, seamLooser=0). Data-source table: agent-detail + inspector + command-palette + search read companyAgents.trustScore (shared/schema.ts:14927, writers after turn 13: NONE — updateTrustScore at companyAgents.ts:367 retires with routes-scp-v2 trust routes and trustEvolution.ts:181; job registration at server/jobs/runScheduledJobs.ts:2314). Inspector trustCurve + founder-trends trajectories + orphan agent-trust-history + unused detail trustHistory read trust_evolution_log (sole writer: trustEvolution.ts). agent-performance reads agent_runtime_state (no trust column — never fed). ceoAbsenceMode boosts are already derivation-time only (not written to the column, per its :99-110 comment) and lose their consumer at the flip per Decision C. Cascades: AgentTrustChart dies with its card; TrustScoreBar dies with the agent-performance tab; founder/MorningBriefing.tsx deletes outright (zero parents); trust-language.ts loses five exports but the file survives (decisionsDoorPlainLanguage.test.ts pins it); trust-badge.tsx is orphaned only via the out-of-scope marketplace dead branch. Four distinct trust domains exist in client code — agent ledger (dying), Pillar-R graduation, investor-network, public maturity registry — only the first is turn-13 scope. FOUNDER_ROUTE_BASELINE is unaffected by the client-page edits mapped here (agent-performance sits at /agent-performance, outside /founder/*; no /founder/* route is added or removed by the UI repoint itself).

## Stage-4 turn-13 memory retirement: delete server/services/cognitiveMemoryV13.ts and drop the four V13 memory tables (agent_episodic_memory, agent_semantic_memory, agent_working_memory_v13, memory_access_log) as OD-8 batch 7 — executable only after the durable trust-seam shadow window matures (~2026-09-07) with seamLooser=0 per BRAIN_CONSOLIDATION_STAGE4.md turn 11's flip rule.

- **[delete]** `/home/user/AcreOS/server/services/cognitiveMemoryV13.ts:1` — The whole service (363 lines). Sole reader/writer of all four tables; every insert/select/update/delete on them is confined to this file (episodic :78/:93/:114/:265/:294/:302; semantic :130/:139/:148/:165/:176/:196/:201; working :219-:253; memory_access_log inserts :120/:186 — write-only, zero readers anywhere, confirming the doc).
  - Delete after the in-commit zero-caller grep. Grep BOTH the symbols (cognitiveMemoryV13|cognitiveMemoryService) AND URL path strings; today the only non-doc references are the two importers below, one stale test mock, and comments in scripts/migrate.mjs:10786 + migrations/0243:8.
- **[edit]** `/home/user/AcreOS/server/services/feedbackLoopV14.ts:12` — Importer #1 (static import :12; calls extractFact :284 and queryFacts :291 inside propagateLearning's memory step, lines 281-307).
  - DO NOT delete this file — the doc's 'dies with its routes/services' is stale. Three live consumers: founderTodo.ts:36/:188 (getOverrideAnalytics feeds the Letter/todo feed), routes-founder-self-running-company.ts:16/:44-100 (eleven /api/founder/v14 feedback routes), and client useFounderOverrides (use-sovereign-dashboard.ts:151-169 fetches /api/founder/v14/overrides/:orgId, rendered by governance.tsx:200/:268-304 — made LIVE by commit 8ea35036, after the doc's turn-15 note called it dead). Cutover: remove import :12 and convert the memory step (:281-307) to solene ingestAgentMemory (server/services/solene/agentMemoryIngest.ts, turn-17 pattern, sourceRef e.g. 'feedback_learning:<learningId>') or excise it honestly; the queryFacts 'verification recall' dies with V13 — replace with the ingest result's stored boolean, never a fabricated verified:true. PropagationSummary.memory/appliedToMemory shape changes are internal to this service + its routes' JSON.
- **[delete]** `/home/user/AcreOS/server/services/trustAuthorityEscalation.ts:174` — Importer #2 (dynamic await import('./cognitiveMemoryV13') at :174, extractFact :175-180, inside a try/catch in onThresholdCrossed).
  - Dies with the whole service at turn 13 (lane-3 mutation machinery). If turn 13's service deletion slips (see the agentInitiativeEngine risk), the minimal alternative is excising just the :172-181 block — it is fire-and-forget and swallowed. Callers that must flip/retire first: executionEngine.ts:410-419 (turn-12 seam flip), agentInitiativeEngine.ts:20/:211 (isActionAllowed gate — NOT named in the doc's turn-13 list), trustEvolution.ts:211-212 (onThresholdCrossed — dies when the weekly job unregisters).
- **[edit]** `/home/user/AcreOS/server/jobs/runScheduledJobs.ts:2301` — trustEvolution weekly job: startTrustEvolutionJob defined :2301-2321, invoked :4152. Its retirement kills the last runtime path into trustAuthorityEscalation.extractFact.
  - Turn-13 co-requisite (doc cites the same range). Note: startAgentMemoryConsolidationJob (:1827, invoked :4451) is the DIFFERENT substrate (agentMemoryConsolidation → agent_memory_notes/Company Mind) — do not touch it; no V13 memory decay/consolidation cron was ever registered, confirming the doc's 'quasi-static' claim.
- **[repin-test]** `/home/user/AcreOS/tests/unit/cascadePrecedentHit.test.ts:19` — Stale vi.mock of '../../server/services/cognitiveMemoryV13' at :19-24 — vestigial since turn 17 repointed confidenceCascadeV14 to solene retrieval (the module under test no longer imports V13).
  - Remove the mock block in the deletion commit (a mock naming a deleted path is residue). The test's real pins (attach-only precedents, fail-open) survive unchanged. Turn-13 sibling repins for the trustAuthorityEscalation deletion: agentAuthorityCeiling.test.ts:216-246 (directly asserts tiers — repin to the trustSeam equivalents, never delete), executionEngineHonesty.test.ts:55-56 and safetyGateFailClosed.test.ts:101-104 (mocks), absenceGrantExpires.test.ts:9 (prose).
- **[delete]** `/home/user/AcreOS/shared/schema.ts:16446` — agentEpisodicMemory model + AgentEpisodicMemoryEntry type (:16446-16469).
  - No consumers outside cognitiveMemoryV13.ts; no drizzle relations() reference it; no db.query.* relational-API usage anywhere (verified by model-name grep across repo).
- **[delete]** `/home/user/AcreOS/shared/schema.ts:16471` — agentSemanticMemory model + AgentSemanticMemoryEntry type (:16471-16494).
  - Same verification as above.
- **[delete]** `/home/user/AcreOS/shared/schema.ts:16496` — agentWorkingMemoryV13 model + AgentWorkingMemoryEntry type (:16496-16511).
  - Same verification as above.
- **[delete]** `/home/user/AcreOS/shared/schema.ts:16513` — memoryAccessLog model + MemoryAccessLogEntry type (:16513-16525).
  - Write-only table (cognitiveMemoryV13.ts:120/:186 are the only writers; zero readers anywhere) — the doc's claim verified.
- **[lower-baseline]** `/home/user/AcreOS/scripts/ratchets/table-count.json:20` — Strict down-only pgTable-count ratchet, baselineCount currently 729 (enforced by scripts/ratchet.mjs; turn 15's '−2 tables' moved 731→729).
  - 729 → 725 in the SAME commit that removes the four models (wave rule 5 / lesson 3). Do NOT touch FOUNDER_ROUTE_BASELINE for this tranche — the memory drop removes no /founder route (turn 13's /api/scp/v2/trust removal is a separate, non-/founder surface).
- **[edit]** `/home/user/AcreOS/scripts/schema-migrate-mirror.orphans.json` — Reverse-orphan register read by scripts/check-schema-migrate-mirror.mjs (:162/:202-236): a table CREATEd in migrations/*.sql but unmodelled in schema.ts fails CI as a 'new orphan'. migrations/0018_v13_sentient_enterprise.sql still CREATEs all four tables.
  - ADD four entries with reasons in the same commit that removes the models, following the delegation_tokens/trust_enforcement_log precedent wording ('Model removed with cognitiveMemoryV13 (stage-4 turn 13); migration 0248 drops conditionally at every release; entry exists because immutable historical 0018.sql still contains the CREATE'). Turns 14 and 15 both did exactly this. tests/unit/schemaDrift.allowlist.json holds NO entries for these tables — no change there (verified).
- **[migration]** `/home/user/AcreOS/scripts/migrate.mjs:10912` — The authoritative OD-8 drop tranche: append a new DO $mig0248$ block at the end of STATEMENTS (before the closing ] at :10912), mechanism identical to 0241-0243/0246 (:10686-10909): per-table to_regclass presence check → SELECT count(*) → DROP TABLE CASCADE only at n=0 → populated survivor stays with RAISE WARNING '[od8-batch7] … LEFT IN PLACE for founder review' → ONE atomic RAISE NOTICE '[od8-batch7-summary] dropped=% absent=% survivors=%' (Fly log-loss lesson). Header comment must name the ruling, the code-retirement commit, and expected verdict.
  - migrate.mjs has NO CREATE for any of the four tables (grep-verified: only the :10785 comment) — production likely never created them (0016 precedent), so 'absent' is the expected verdict; but 0017 surprised the census by having reached production (saga_instances), so the conditional mechanism must run regardless. Also update the now-stale 0243 header comment at :10780-10792 ('stay OFF until turn 13').
- **[edit]** `/home/user/AcreOS/scripts/migrate.mjs:51` — 0247 od8-ledger block (POSITIONED FIRST in STATEMENTS, :33-75): the whole-program verdict line re-checks every table the batches govern.
  - Add 'agent_episodic_memory','agent_semantic_memory','agent_working_memory_v13','memory_access_log' to the tables ARRAY (:51-62) so the ledger's absent=N/N verdict covers batch 7; the current 23/23-clean verdict (deploy #47) becomes 27/27 once the drops land.
- **[migration]** `/home/user/AcreOS/migrations/0248_od8_batch7_v13_memory_conditional_drops.sql` — Mirror .sql for the tranche (every batch 0241-0247 has one; 'authoritative copy runs from scripts/migrate.mjs' per 0243:3).
  - Same DO-block content as the migrate.mjs copy. Next free number is 0248 (migrations/ currently ends at 0247_od8_ledger.sql).
- **[edit]** `/home/user/AcreOS/docs/autonomous/OWNER_DECISIONS_PENDING.md:719` — OD-8 program record (:655-721): batch-3 deferral of these four tables, the program-complete verdict, and 'remaining drop candidates … join future batches under this same, now-proven mechanism'.
  - Record batch 7 (tables, ruling basis, Decision-G evidence: row counts + max(created_at) predating write-path removal) and later the deploy verdict, exactly as batches 1-6 were recorded. Update BRAIN_CONSOLIDATION_STAGE4.md's turn-13/18 lines to DONE in the same commit.
- **[verify-only]** `/home/user/AcreOS/server/routes-admin.ts:771` — GET /api/admin/trust-seam-shadow (founder-gated) — the evidence gate that licenses turns 12-13 and therefore this whole target.
  - Before executing: durable.{comparisons,seamLooser,seamStricter,bootSessions} over ?sinceDays must show ≥1 week of cadence evidence with seamLooser=0 and a non-vacuous comparison count (persistence via jobHealthLogs in server/services/autopilot/trustSeam.ts:135-168; window restarted at commit a046b7cb, maturing ~2026-09-07). Any seam-looser divergence blocks the flip and reopens ACTION_DOMAIN_MAP.
- **[verify-only]** `/home/user/AcreOS/shared/schema.ts:16999` — reaction_chain_links status (tranche decision pending — CONFIRMED): model alive at :16999-17012 + ReactionChainLinkEntry type :17132, with ZERO code references anywhere outside schema.ts (grep-verified across server/client/tests); not in any drop migration nor the 0247 ledger.
  - Recorded as a future-tranche candidate 'needing its own recorded decision' in OWNER_DECISIONS_PENDING.md:664-666/:690-691 and the 0243 census comment (migrate.mjs:10791-10792). It may join batch 7 ONLY with its own founder decision via the picker — do not silently append it. Contrast: reaction_chain_runs stays LIVE (model :16978; reader autonomyScoreV14.ts:24/:738-742, plain-text chain_id, no FK) and must NOT enter any drop list without the contract adjustment flagged at turn 16.
- **[verify-only]** `/home/user/AcreOS/server/services/agentInitiativeEngine.ts:211` — Undocumented third caller of trustAuthorityEscalation (static import :20; isActionAllowed gate at :211 deciding auto-execute vs escalate) — absent from the doc's turn-13 retirement list (which names only executionEngine, agentAuthorityGate, trustEvolution, scp-v2 routes, client renderings).
  - Must flip to trustSeam (or retire) in turn 13 before trustAuthorityEscalation.ts can delete; otherwise importer #2 of cognitiveMemoryV13 survives and the deletion's zero-caller grep fails. Registered via runScheduledJobs.ts (initiative lane) so it is live at cadence.

  **Risks:**
  - feedbackLoopV14 cannot 'die with its routes' as the doc assumes: since commit 8ea35036 the governance Overrides tab is a LIVE consumer of /api/founder/v14/overrides/:orgId (use-sovereign-dashboard.ts:151-169 → governance.tsx:268-304), and founderTodo.ts:188 feeds the Letter from getOverrideAnalytics. Deleting the service or its routes breaks a live founder surface; the cutover must excise/convert only the memory step (:281-307). The doc's §1e sentence is stale and should be corrected in the cutover commit.
  - agentInitiativeEngine.ts:211 is a live trustAuthorityEscalation caller missing from the doc's turn-13 list. If turn 13 deletes the service without flipping it, the initiative lane breaks — and if turn 13 defers the service deletion because of it, cognitiveMemoryV13 keeps an importer and cannot delete. Either flip it to the seam in the same turn or excise only trustAuthorityEscalation.ts:172-181 as an interim.
  - Populated-survivor likelihood is HIGH, not low: these tables had real writers until turn 17 (agentDebates precedents, bootstrap DEFAULT_MEMORIES, executionEngine store_learning, weekly trustEvolution promotions) and memory_access_log grew on every recall. The batch mechanism will LEAVE populated tables in place with a warning; plan for a founder picker ruling on survivors (0244 reaction_chains precedent) and gather Decision G's evidence (row counts + max(created_at) predating write-path removal) from the deploy notice lines.
  - Off-by-one in the program's own prose: 'the four V13 memory tables + memory_access_log' (migrate.mjs:10784, 0243:8, OWNER_DECISIONS_PENDING.md:658/:689, and the tasking) double-counts. The set is exactly FOUR tables total — agent_episodic_memory, agent_semantic_memory, agent_working_memory_v13, memory_access_log (schema.ts:16446/:16471/:16496/:16513; migration 0018 creates exactly these). A cutover hunting a phantom fifth table stalls; fix the phrasing where touched.
  - Production-existence uncertainty cuts both ways: migrate.mjs (the only DDL path production runs) never created these tables, so 'absent' is the expected verdict — but 0017's tables turned out to exist in production after the census said otherwise (saga_instances). Never skip the conditional mechanism on an 'absent anyway' argument.
  - Evidence gate is a hard precondition: turns 12-13 (and with them this deletion) are licensed only by ≥1 week of DURABLE shadow evidence with seamLooser=0 read from /api/admin/trust-seam-shadow's cross-boot aggregation — process-memory counters and Fly logs were already proven unreadable. Window matures ~2026-09-07; any seam-looser divergence blocks the flip.
  - Ratchet discipline: table-count.json 729→725 and the four orphan-register additions must land in the SAME commit as the model removal, or CI fails in both directions (down-only count; new-orphan detection by check-schema-migrate-mirror.mjs). Do not move FOUNDER_ROUTE_BASELINE — no /founder route is removed by this target.

  **Notes:** Verification summary (claims vs code): (1) Importer count VERIFIED — exactly two production importers of cognitiveMemoryV13.ts today: feedbackLoopV14.ts:12 (static) and trustAuthorityEscalation.ts:174 (dynamic, try/catch); plus one stale test mock (cascadePrecedentHit.test.ts:19) and comments in migrate.mjs/0243. (2) URL-path lesson applied — zero client fetches of /api/founder/v13/* remain (agent-performance.tsx:201, App.tsx:394, use-sovereign-dashboard.ts:8 are prose only); no API path anywhere serves the memory tables since turn 18 deleted routes-founder-sentient-enterprise and scpMemorySystem. (3) DB-touch census — all reads/writes of the four tables are confined to cognitiveMemoryV13.ts; zero db.query.* relational-API usage, zero raw SQL in server/, zero type-export consumers, zero drizzle relations; snake_case grep hits only schema.ts, migrations/0018, 0243's comment, migrate.mjs's comment, and docs. (4) Dynamic-dispatch hunt — no job-name registry, switch, or scheduler references the service (the only 'memory' job, agent_memory_consolidation_weekly at runScheduledJobs.ts:1827/:4451, is the agent_memory_notes substrate and stays); no ratchet register (scripts/ratchets/*.json, reachability, outwardActionCoverage) names cognitiveMemoryV13/feedbackLoopV14. (5) The od8 tranche pattern requires five artifacts in the cutover: migrations/0248 mirror file, the migrate.mjs DO-block (appended before :10912), the 0247 ledger array addition (:51-62), the OWNER_DECISIONS_PENDING.md batch record, and the orphan-register entries — plus table-count 729→725. (6) reaction_chain_links: pending status CONFIRMED — zero callers, model still at schema.ts:16999, awaiting its own founder tranche decision; reaction_chain_runs remains live (autonomyScoreV14.ts:738).

## Turns 12–13 (trust-gate flip to trustSeam + lane-3 retirement): every test pin, ratchet baseline, and register entry the cutover moves — evidence gate per BRAIN_CONSOLIDATION_STAGE4.md turn 11: >=1 week durable jobHealthLogs shadow evidence, seamLooser=0, non-vacuous comparisons (read GET /api/admin/trust-seam-shadow before executing), maturing ~2026-09-07

- **[repin-test]** `tests/unit/safetyGateFailClosed.test.ts:101` — vi.doMock of server/services/trustAuthorityEscalation (:101-109) + companyAgents effectiveTrustScore mock (:110-117); trustThrows case (:139-149) pins refusal text 'Trust authority check could not be evaluated'
  - TURN 12. The invariant (an unevaluable authority gate refuses, names itself, offers escalate_to_founder) survives; the dependency changes. Repin the mocks to trustSeam/domainAutonomy and the refusal wording to the seam's fail-closed terms (unmapped => escalate, seam error => block). The vacuity guard (:127-137) and the turn-14 structural founder-gate block (:159-184) survive unchanged. Never delete — this is the 'a check that cannot run is not a check that passed' pin.
- **[repin-test]** `tests/unit/executionEngineHonesty.test.ts:55` — vi.mock trustAuthorityEscalation (:55-60) + companyAgents effectiveTrustScore (:61-70) to pass gates open
  - TURN 12. Replace with a seam-open mock (seamVerdict => allow) so the honesty suite still isolates executor lookup. All honesty assertions unchanged. Doc already mandates rewrite-not-delete for this file (turn 2 precedent at :7).
- **[repin-test]** `tests/unit/absenceGrantExpires.test.ts:178` — CALL_SITES = [executionEngine.ts, agentInitiativeEngine.ts]; asserts both contain 'isActionAllowed' (:191-196) and 'effectiveTrustScore', and never read .trustScore directly (:198-206)
  - GOES RED AT TURN 12, not 13: the executionEngine flip removes both strings from executionEngine.ts. Repin at turn 12 (repoint that member to the seam call), then at turn 13 the file deletes-with-surface IF Decision C retires ceoAbsenceMode; if absence re-expresses as witness grants, transfer its invariants (grant may not outlive expiry; no ratchet across repeated grants; fail-closed on store error) to the grant TTL. Also note agentInitiativeEngine.ts:206-211 is a THIRD live lane-3 consumer the doc's turn-12/13 spec does not name — it must flip in turn 13 or trustAuthorityEscalation.ts cannot delete.
- **[repin-test]** `tests/unit/trustSeamShadow.test.ts:40` — seamVerdict fail-closed pins (:40-70) + shadow accounting/divergence-direction pins (:72-108)
  - TURN 12/13 split: the seamVerdict fail-closed describes become the LIVE gate's contract — keep, and add the doc's mutation tests (domain to observe => refusal; execute_gated => pass; deleted mapping => escalate). The shadowCompare/getShadowCounters half retires with the shadow machinery once both gates are flipped.
- **[repin-test]** `tests/unit/trustSeamShadowPersistence.test.ts:58` — pins durable jobHealthLogs persistence (trustSeamShadow:flush / :divergence rows)
  - Retire in the SAME commit that removes shadowCompare and the routes-admin.ts:771 GET /api/admin/trust-seam-shadow endpoint (after turn 13) — not before, since this pin protects the flip's own evidence channel until the flip lands. Deletes-with-surface, deliberately, not silently.
- **[repin-test]** `tests/unit/agentAuthorityCeiling.test.ts:118` — db mock serves stored trustScore 95 (:39-52); 'learned trust score does not replace placement' (:118-142); delegation/unclassified blocks (:92-116, :144-202)
  - TURN 13. checkAuthority flips to the seam: repin the mocks and assert the doc's flip-preserving property verbatim — every live proactive:*/reaction:* action stays unclassified => level-2 escalate. NEVER_PROMOTE extraction (:89-90 via tests/helpers/neverPromoteActions) survives because agentAuthorityGate.ts and its OPERATIONAL_NEVER_PROMOTE literal survive.
- **[repin-test]** `tests/unit/agentAuthorityCeiling.test.ts:204` — imports the REAL trustAuthorityEscalation (:216, :229, :241): escalate_to_founder permitted at every tier; 4-band tier table; Observer cannot act
  - TURN 13. The tier-table pins (:228-236) die with the tier ladder. The invariant 'escalate_to_founder is always permitted / the recommended remedy can never be blocked' must be REPINNED against seamVerdict (a structural escalate the seam can never refuse). The Observer-cannot-act direction re-expresses as unmapped-action => escalate.
- **[repin-test]** `tests/unit/trustFromEvidence.test.ts:125` — reads server/services/trustEvolution.ts source (:134), asserts runTrustEvolution exists and computes via trustDeltaFrom (:147-153); pure-rule blocks (:58-123) drive server/services/trustDelta.ts
  - TURN 13. Its own message says 're-adjudicate this check' when runTrustEvolution is gone. trustDelta.ts's ONLY production caller is trustEvolution.ts:138 — post-deletion it is an orphan (law 2). Classify: legitimately-deletes-with-surface (test + trustDelta.ts together), PROVIDED the commit names where the Foundry §18 invariant ('trust may only be granted by evidence the agent did not author') now lives: on the seam, domain promotion terminates only in a founder tap (promotionRequest, untouched). If any evidence-scored promotion survives anywhere, repin instead of deleting.
- **[repin-test]** `tests/unit/jobRosterCoverage.test.ts:73` — exact parity between withJobLock literals and JOB_ROSTER; 'no phantom rows' direction
  - TURN 13. Removing withJobLock('trust_evolution') (runScheduledJobs.ts:2315) without deleting jobRegistry.ts:319 ({name:'trust_evolution', intervalMs:WEEK}) fails this test; deleting both in one commit keeps it green by construction. Also removes the deadman's trust_evolution watch — correct, the job is gone.
- **[edit]** `tests/unit/cascadePrecedentHit.test.ts:19` — vi.mock of server/services/cognitiveMemoryV13 (recallEpisodes/recordEpisode)
  - TURN 13. Already-inert residue (the cascade was repointed off V13 at turn 17; current importers of cognitiveMemoryV13 are only feedbackLoopV14.ts:12 and trustAuthorityEscalation.ts:174). Delete the stale mock when cognitiveMemoryV13.ts deletes, so the test file carries no path to a nonexistent module.
- **[verify-only]** `tests/unit/hardStopLaneCoverage.test.ts:68` — drives every enforcement lane's real gate incl. the agentAuthorityGate lane; shares extraction with tests/helpers/neverPromoteActions.ts
  - Must stay green through both flips — it references agentAuthorityGate (survives) and never touches trustAuthorityEscalation. Run it as part of the flip's mutation-verification.
- **[verify-only]** `tests/helpers/neverPromoteActions.ts:32` — regex-extracts 'const OPERATIONAL_NEVER_PROMOTE = [...] as const;' from agentAuthorityGate.ts source
  - The turn-13 rewrite of checkAuthority MUST preserve that literal (or the extractor and both consumer gates repin). Note the regex falls back to [] silently; the ceiling test's vacuity floor (>=15, contains delete_customer_data) can be satisfied by the constitutional half alone, so a broken extraction would be quiet — check the extraction still returns the operational ids after the rewrite.
- **[verify-only]** `tests/unit/trustGraduationFounderGate.test.ts:167` — pins server/services/trustGraduation + routes-founder-intelligence approve=>hold — a DIFFERENT trust system (graduation tiers), not companyAgents.trustScore
  - NOT lane 3 — do not touch. But routes-founder-intelligence.ts:119-124 reads trustEvolutionLog.promotionSuggested (the shadow-promotion queue trustEvolution populates); after turn 13 that queue never fills again — the card surface freezes empty. Decide its retirement with the turn-13 UI pass.
- **[verify-only]** `tests/unit/outwardActionCoverage.test.ts:144` — UNPROTECTED_SEND_SITES_BASELINE = 54, with an exact-equality stale check (:228-235)
  - Turns 12-13 delete no send sites (trustEvolution/trustAuthorityEscalation/routes-scp-v2 trust section contain none), so expect unchanged — but the exact-equality check means ANY accidental drop forces a same-commit lowering; run it and lock in whatever it measures.
- **[verify-only]** `tests/unit/agents/scpRoutes.test.ts:1` — pins routes-scp-v2 surfaces; contains zero trust/promote/demote pins
  - Tombstoning the /api/scp/v2/trust section (turns 14/15 tombstone pattern) should leave it green; verify.
- **[verify-only]** `tests/unit/__snapshots__/routeManifest.test.ts.snap:233` — file-level route manifest lists routes-scp-v2.ts (:233), routes-founder-intelligence.ts (:133), routes-founder-inspector.ts (:131)
  - Turn 13 deletes no route FILES (trust routes are a section of a surviving file), so the snapshot should not change; if the UI decision retires a founder route file, regenerate the snapshot in that commit.
- **[verify-only]** `tests/unit/schemaDrift.allowlist.json:23` — entries agent_action_log.trust_score_at_time (:23) and org_heartbeat_snapshots.trust_trajectory (:318); detector fails on NEW entries only, no stale check; no V13-memory or trust_evolution_log entries exist
  - Untouched by turns 12-13 (no schema drops in code turns). agent_action_log.trust_score_at_time keeps being written by agentAuthorityGate (:348/:477) — after the flip decide whether that column records the seam's verdict basis or retires; today it would freeze a score no job updates.
- **[lower-baseline]** `scripts/ratchets/reachability.json:118` — allowlist entries trustSeam.ts::seamVerdict + trustSeam.ts::ACTION_DOMAIN_MAP — 'STAGED SEAM, SHADOW HALF', explicit revocation: 'the turn-12 flip makes executionEngine consume seamVerdict directly — these entries then go STALE and the gate FAILS until removed'
  - TURN 12, same commit as the flip: delete both allowlist entries (:118-127). The gate is designed to fail loud if you forget — the loud-adoption pattern the outbound seam used (fulfilled 2026-08-29, turn 6).
- **[lower-baseline]** `scripts/ratchets/reachability.json:7` — current baselines: unreachedExports 375, internalOnlyExports 1173, tablesNoWriter 57, tablesNoReader 67, unregisteredRoutes 4, opaqueExports 20, moduleOrphans 29, unreachedExportsShared 54, internalOnlyExportsShared 429, moduleOrphansShared 1, opaqueExportsShared 15
  - TURN 13: deletions (trustEvolution.ts, trustAuthorityEscalation.ts, cognitiveMemoryV13.ts, trustDelta.ts, ceoAbsenceMode.ts if Decision C retires it, shadow machinery) lower the export counts — lock in via node scripts/lint-reachability. Deletion-revealed UP risks to neutralize in-commit instead of absorbing: (a) trust_evolution_log loses its only writer while keeping live readers => tablesNoWriter +1 unless the reader surfaces retire same turn; (b) companyAgents.updateTrustScore (:367) loses ALL callers (trustEvolution.ts:181, routes-scp-v2.ts:232/:265) — delete the method in-commit; (c) companyAgents.effectiveTrustScore (:348) loses all callers after both flips + initiative-engine flip — delete with Decision C.
- **[lower-baseline]** `scripts/ratchets/table-count.json:12` — baselineCount 729, strict down-only
  - TURN 13, if the four V13 pgTable models are removed with their last importer (shared/schema.ts:16446 agentEpisodicMemory, :16471 agentSemanticMemory, :16496 agentWorkingMemoryV13, :16513 memoryAccessLog) per the turn-14/15 precedent (model removed + conditional DROP migration in-commit): 729 -> 725. migrate.mjs 0243's comment (:10784-10786) explicitly holds their OD-8 drop 'until turn 13 retires lane 3 and cognitiveMemoryV13.ts deletes' — write the next OD-8 batch migration then.
- **[lower-baseline]** `scripts/check-org-scoped-fetch.mjs:531` — BASELINE_FUNCTION_OFFENDERS entry 'server/services/trustEvolution.ts::runTrustEvolution' (rule 1 — platform op with no unscopedForPlatformOps declaration)
  - TURN 13: the register's stale-entry check forces this line out in the same commit the file deletes (down-only; documented behavior at the register's own :98-101 note). Also update the prose naming it as the rule-1 exemplar (:517, and tests/unit/orgScopedFetchCoverage.test.ts:91 — cosmetic).
- **[verify-only]** `scripts/check-org-scoped-fetch.mjs:681` — BASELINE_FUNCTION_UNUSED_ORG entry 'server/services/executionEngine.ts::validateSafetyGates'
  - TURN 12, conditional: the flip rewrites validateSafetyGates' trust block (:408-433). If the rewritten body no longer trips rule 2, the stale check forces the entry out in the flip commit; if the offense sits elsewhere in the function, the entry stays. Run the script either way.
- **[verify-only]** `scripts/check-org-scoped-fetch.mjs:1281` — RULE3_BASELINE (unscoped query chains)
  - Contains ZERO lane-3 chains — no trustEvolution/trustAuthorityEscalation/companyAgents/scp keys. Unchanged by the cutover.
- **[lower-baseline]** `scripts/check-measurement-defaults.mjs:128` — register entry 'server/routes-scp-v2.ts::currentAgent.trustScore ?? 50' (live at routes-scp-v2.ts:231 and :264, inside the promote/demote handlers)
  - TURN 13: stale entries FAIL this gate (:345-352). Remove the entry in the same commit that removes POST /api/scp/v2/trust/promote (:218) and /demote (:251) and GET /trust/promotions (:204).
- **[lower-baseline]** `scripts/ratchets/run-scheduled-jobs-linecount.json:8` — baselineCount 5721, strict down-only on server/jobs/runScheduledJobs.ts
  - TURN 13: deleting startTrustEvolutionJob (runScheduledJobs.ts:2301-2321) and its call (:4152) shrinks the file ~22 lines; stale-high fails, so re-measure (node scripts/ratchet.mjs --measure) and lower in the same commit.
- **[lower-baseline]** `scripts/ratchets/colon-any.json:1` — baselineCount 2714 (colon-any) and as-any.json baselineCount 1253; lane-3 files carry occurrences (trustEvolution ~2/2, cognitiveMemoryV13 1/0, ceoAbsenceMode 2/4 by line-grep)
  - TURN 13: stale-high FAILS these; run the ratchet runner after the deletions and lock in the measured drops (turn-15 precedent recorded -14 colon-any from a lane deletion). Same for inline-provenance and any other regex-count ratchet the deleted files feed — measure, don't precompute.
- **[verify-only]** `tests/unit/founderFourDoors.test.ts:20` — FOUNDER_ROUTE_BASELINE = 81, exact two-sided ratchet (over fails, under demands lowering)
  - Turns 12-13 remove no /founder/* client route by default. CONDITIONAL: if the turn-13 UI decision removes a founder page (founder/inspector.tsx or founder-trends.tsx rather than repointing them), lower to the new count in that commit.
- **[verify-only]** `tests/unit/constitution.test.ts:113` — UNENFORCED_HARD_STOP_BASELINE = 0 ('stays at 0 forever'); pointer-resolution check over every enforcement ref
  - DEFINITIVE for the register-rot question: shared/governance/constitution.ts and statuteRegister.ts contain ZERO references to lane-3 code (grepped for trustEvolution, trustAuthorityEscalation, scp, v13, episodic, companyAgents, executionEngine, agentAuthorityGate, trustSeam — no matches). Nothing rots; no reclassification available; baseline stays 0.
- **[verify-only]** `vitest.config.ts:44` — coverage thresholds: global lines 18; per-file floors creditPool 87, webhookHandlers 70, webhook-idempotency 75, publicParcelReport 88, directMailService 52, smsService 42, dunning 32, tcpaCompliance 50, dncScrub 58, featureGate 90, roleScope 95, permissions 25
  - No per-file floor names lane-3 code — no threshold edits required. But see risk: the global 18% floor was set ~0.2pt under measured, and turn 13 deletes some of the best-covered small modules (trustDelta at ~100% via trustFromEvidence), which shrinks the covered numerator — run coverage before merging.
- **[delete]** `server/jobs/jobRegistry.ts:319` — { name: 'trust_evolution', intervalMs: WEEK, critical: false } — job-name TABLE (dynamic dispatch)
  - TURN 13, same commit as runScheduledJobs.ts:2301-2321/:4152 — jobRosterCoverage.test.ts enforces the pairing.
- **[delete]** `server/routes-admin.ts:771` — GET /api/admin/trust-seam-shadow — durable shadow-evidence aggregator over jobHealthLogs
  - AFTER the turn-13 flip is verified (it is the flip's evidence reader — keep it alive through the decision). Retire with shadowCompare/getShadowCounters and trustSeamShadowPersistence.test.ts in one deliberate commit; the jobHealthLogs rows themselves persist as the historical record.
- **[edit]** `server/services/agentInitiativeEngine.ts:206` — trustAuthorityEscalation.isActionAllowed + companyAgentService.effectiveTrustScore — the third live lane-3 gate consumer, unnamed in the doc's turn-12/13 spec
  - TURN 13 blocker: must flip to the seam (or to an explicit escalate) before trustAuthorityEscalation.ts can delete. Pinned today by absenceGrantExpires.test.ts CALL_SITES — repin them together.
- **[edit]** `server/services/feedbackLoopV14.ts:12` — static import of cognitiveMemoryService from ./cognitiveMemoryV13 — and feedbackLoopV14 itself is LIVE (founderTodo.ts:36, routes-founder-self-running-company.ts:16)
  - TURN 13 blocker: remove/convert its V13 memory write (turn-17 agentMemoryIngest pattern) in the same commit cognitiveMemoryV13.ts deletes; the service itself survives.
- **[edit]** `shared/schema.ts:15063` — trustEvolutionLog pgTable — writer dies at turn 13; live readers remain: routes-founder-intelligence.ts:119/:3483/:3520 (shadow-promotion queue + per-agent history), routes-founder-inspector.ts:66, systemTrends.ts:239-245 (feeds founder-trends.tsx trust chart)
  - NOT on the doc's OD-8 drop list — a gap. Turn 13 must either retire these reader surfaces with the client trustScore repoint (then model joins the drop list, table-count -1 more, tablesNoWriter/NoReader clean) or explicitly label the data historical; leaving them reading a frozen log as live violates the same fabrication-adjacent rule that motivates the same-turn UI fix.
- **[edit]** `client/src/pages/agent-performance.tsx:78` — trustScore badge/score renders (:78-90); plus founder/inspector.tsx:47,:190; founder-trends.tsx:50,:250; agent-detail.tsx:122,:282 (trustScoreAtTime); command-palette.tsx:406,:1250
  - TURN 13, same commit as the job retirement (doc's own rule: no surface renders a frozen score as live). No client code fetches /api/scp/v2/trust by URL string (repo-wide path grep clean — lesson 1 satisfied). Do NOT touch marketplace.tsx:944-946 (seller trust, different system) or achievement-progress.tsx:17,:154 (org score /1000, different system).

  **Risks:**
  - Turn-12 red that looks like turn-13 work: absenceGrantExpires.test.ts pins the literal strings isActionAllowed/effectiveTrustScore inside executionEngine.ts — the turn-12 flip alone breaks it. Repin its CALL_SITES in the turn-12 commit, not later.
  - agentInitiativeEngine.ts:206-211 is a third lane-3 gate consumer the stage doc's turn-12/13 spec never names. Deleting trustAuthorityEscalation.ts with it still importing is a compile break at best; flipping it without a shadow window is an unshadowed gate change — its action population (initiative proposals) overlaps executionEngine's shadow traffic, but confirm before treating the existing evidence as covering it.
  - trust_evolution_log is absent from the OD-8 drop list while keeping three live reader surfaces (founder-intelligence promotion queue :119, inspector :66, systemTrends :239) — retiring the writer without deciding these readers ships frozen trust data rendered as live, the exact fabrication-adjacent failure turn 13's same-turn-UI rule exists to prevent.
  - Global coverage floor (vitest.config.ts lines: 18) has ~0.2pt headroom over the last recorded measure, and turn 13 deletes near-fully-covered modules (trustDelta, trustEvolution's tested paths), shrinking the covered numerator. Run coverage before merging; if it dips below 18 the fix is adding real tests elsewhere, never lowering the floor (raise-only discipline).
  - The Foundry §18 invariant ('learning does not create authority') currently lives ONLY in trustFromEvidence.test.ts + trustDelta.ts, both slated to delete with trustEvolution. The cutover commit must name where the invariant is now enforced (domain promotion terminates only in a founder tap — promotionRequest) or repin it there; deleting the pin without naming the successor is how invariants quietly die.
  - vi.mock factories pointing at deleted module paths (executionEngineHonesty:55, safetyGateFailClosed:101, cascadePrecedentHit:19) can fail at test collection depending on vitest resolution — clean all three in the deletion commit, not opportunistically.
  - Reachability deletion-revealed increases require Iris-CTO sign-off unless named and queued in-commit: pre-plan the trust_evolution_log/updateTrustScore/effectiveTrustScore/trustDelta fates so no baseline goes UP.
  - The shadow evidence reader (routes-admin.ts:771) and its persistence test must OUTLIVE the flip decision — retiring them in the turn-12/13 commits would delete the flip's own license; retire them in a follow-up commit after the flip is verified.
  - Helper regex in tests/helpers/neverPromoteActions.ts falls back to [] silently and the ceiling test's vacuity floor can be satisfied by the constitutional half alone — after rewriting agentAuthorityGate, hand-check that OPERATIONAL_NEVER_PROMOTE extraction still returns the operational ids, or the two hard-stop gates lose half their population without going red (law 3).

  **Notes:** Classification summary — REPIN (invariant survives, assertion changes): safetyGateFailClosed, executionEngineHonesty, agentAuthorityCeiling (both halves), absenceGrantExpires (turn-12 half; turn-13 fate rides Decision C), trustSeamShadow's seamVerdict half, jobRosterCoverage (by parity). LEGITIMATELY-DELETES-WITH-SURFACE: trustFromEvidence + trustDelta.ts (invariant successor must be named), trustSeamShadow's accounting half + trustSeamShadowPersistence (after flip verification), absenceGrantExpires if ceoAbsence retires. VERIFY-ONLY (must stay green, no edits expected): hardStopLaneCoverage, trustGraduationFounderGate (different trust system), scpRoutes, routeManifest snapshot, outwardActionCoverage (baseline 54), schemaDrift allowlist, domainAutonomy pins. RATCHET CURRENT VALUES: reachability {unreachedExports 375, internalOnlyExports 1173, tablesNoWriter 57, tablesNoReader 67, unregisteredRoutes 4, opaqueExports 20, moduleOrphans 29, shared: 54/429/1/15} with the two trustSeam allowlist entries whose revocation condition IS turn 12; table-count 729 (→725 if the four V13 models go at turn 13, per migrate.mjs 0243's explicit hold); FOUNDER_ROUTE_BASELINE 81 (conditional); UNENFORCED_HARD_STOP_BASELINE 0 (unchanged — shared/governance has zero lane-3 refs, so the statuteRegister/constitution rot question answers NO ENTRIES AFFECTED); RULE3_BASELINE contains no lane-3 chains (unchanged); BASELINE_FUNCTION_OFFENDERS loses trustEvolution::runTrustEvolution (stale-forced); BASELINE_FUNCTION_UNUSED_ORG's executionEngine::validateSafetyGates is conditional at turn 12; check-measurement-defaults loses routes-scp-v2::trustScore??50 (stale-forced); run-scheduled-jobs-linecount 5721 (re-measure); colon-any 2714 / as-any 1253 (re-measure); vitest coverage thresholds untouched (no lane-3 floors). URL-path grep for /api/scp/v2/trust across client is clean — the turn-15 useDelegationTokens lesson does not recur here.

## Adversarial audit — touchpoints the mappers missed

- **[edit]** `/home/user/AcreOS/server/services/autonomyFinalMile.ts:40` — MISSED consumer-of-a-consumer: the daily founder digest enumerates agentEvents eventType 'authority_promotion' in its inArray list (:40) and renders a '• N trust promotions' summary line (:84-85), then broadcasts it as founder event 'daily_summary'. The SOLE writer of that eventType is trustAuthorityEscalation.ts:132, which turn 13 deletes. Registered live at runScheduledJobs.ts:4589. No map's job-name/event-name inventory included agentEvents eventTypes.
  - In the turn-13 commit remove 'authority_promotion' from the enumeration and delete the counts line — otherwise the digest carries a permanently-dead category (small-print stale-UI class). The eventMesh 'trust:lifecycle' zero-subscriber claim in map 1 is correct; this agentEvents read path is the one it missed.
- **[delete]** `/home/user/AcreOS/server/services/settingsSeeder.ts:23` — MISSED dead-lever surface via dynamic dispatch: SETTINGS_CATALOG seeds 'trust.tier_breakpoints' (:23), 'trust.promotion_accuracy_gate' (:31), 'trust.promotion_success_gate' (:38) — the ONLY reader of all three is trustEvolution.ts:53-60 (getSetting calls), dying at turn 13. The seeder header (:12-14) states the /founder/studio UI 'renders new keys automatically' as founder-editable dials (generic dial-rendering engine over this catalog, routes-founder-studio-dials.ts / routes-founder-studio.ts). Post-flip a founder can turn three autonomy dials that do nothing — a silent no-op lever in a live founder UI, the exact fabrication-adjacent class Decision C names for ceoAbsence.
  - Remove the three catalog entries in the same commit that deletes trustEvolution.ts; decide whether the already-seeded settings rows are cleaned or left as historical (they re-seed on boot otherwise). No symbol grep could find this — the coupling is a string key through the settings registry, which is why every mapper missed it.
- **[edit]** `/home/user/AcreOS/server/services/financialAuthorityGate.ts:367` — MISSED fourth live trust-gate consumer (money path): requestSpend THROWS when requestingAgent.trustScore < tier.minTrust (:367-371); quorum approver selection filters AND orders candidates by companyAgents.trustScore (:492-495); approver validation re-checks trustScore < tier.minTrust (:561-565). Live via autonomousDecisionExecutor.ts:1026-1027 (the spend executor — a constitutional hard-stop lane). Reads .trustScore RAW, so it also sits outside absenceGrantExpires.test.ts's CALL_SITES population (its 'gates consume the derived value' invariant never covered this gate — ceoAbsence boosts have never applied to spend authority, a pre-existing asymmetry worth recording).
  - The flip commits must adjudicate this gate explicitly: repoint the trust floor to the seam/domain level, or accept-and-document frozen-score semantics. Post-flip consequence if untouched: agents created after the flip (companyAgents.ts:289 seeds 50; agentEvolutionEngine.ts:311 seeds 40) can NEVER cross any minTrust above their seed — the spend-approver pool ossifies permanently. None of the five maps mention financialAuthorityGate at all.
- **[edit]** `/home/user/AcreOS/server/services/intelligence/triage.ts:154` — MISSED stored-data dispatch path for the trust score: agentInitiativeEngine writes trustScore into decisionsInboxItems.contextBundle (:288-292, value from effectiveTrustScore) → autonomousDecisionExecutor.ts:951 runs triageWithLog → triageAgentInitiative reads ctx.agentTrustScore||ctx.trustScore (:154) and AUTO-APPROVES at trustScore>=90 with low risk/impact (:166-173), landing as 'auto_approved_via_triage' (autonomousDecisionExecutor.ts:970). This is a SECOND auto-execution decision consuming the trust score, reached through a DB-stored JSON field — invisible to every import/symbol census the maps ran.
  - The turn-13 initiative-engine flip must decide this field's fate in the same commit: if the engine stops writing trustScore, the triage auto-approve branch goes silently dead (permanent behavior change, unasserted); if it keeps writing a frozen value, pending rows can still license auto-approval on a score no longer maintained. Either repin the triage branch to a seam-derived signal or make it an explicit escalate — and note existing pending inbox rows already carry frozen scores at flip time.
- **[edit]** `/home/user/AcreOS/server/services/ceoCommandBridge.ts:564` — MISSED Decision-C blast radius: handleActivateAbsence (:564-577) is live founder-chat-reachable (founder-chat/tools/action.ts:489 → processCEOCommand; also routes-founder-intelligence.ts:3214 and :3887) and returns the literal string 'Trust scores boosted.' — after the flip that sentence is a fabricated claim (the boosts' only consumers, executionEngine:415 + agentInitiativeEngine:207, move to the seam). handleDeactivateAbsence (:579-581) pairs with it.
  - Whatever Decision C rules (witness-grant re-expression or retirement), this handler's copy and effect must change in the SAME turn — it is the concrete user-facing surface of the 'silent no-op absence' risk the maps stated only abstractly. lint:no-fabrication territory once the boost path is severed.
- **[edit]** `/home/user/AcreOS/server/services/founderWellbeing.ts:234` — MISSED Decision-C blast radius #2: founderWellbeing statically imports ceoAbsenceService (:20), reads getLatest() (:69,:215,:261), and generates recommendations urging 'Activate absence mode' (:234, :267, :299) with claims like 'Trust scores boosted' semantics implied ('Your agents can handle it'). Post-flip it recommends activating a lever that confers nothing.
  - Adjudicate with Decision C in the same turn: repoint the recommendation to whatever replaces absence (witness grants) or drop the absence-mode suggestions. Also a compile-breaker if Decision C deletes ceoAbsenceMode.ts outright — a static importer no map listed.
- **[lower-baseline]** `/home/user/AcreOS/scripts/check-measurement-defaults.mjs:164` — MISSED second stale-baseline forcing entry: 'server/services/scpGoldenSuite.ts::dbAgent.trustScore ?? 50' (:164) matches scpGoldenSuite.ts:373 INSIDE checkTrustPromotions. Every map that touched this register named only the :128 routes-scp-v2 entry. When map 2's own touchpoint deletes checkTrustPromotions, this entry goes stale and the script exits 1.
  - Remove BOTH register entries (:128 AND :164) in the commit that deletes the trust routes + checkTrustPromotions, or CI goes red on the one the maps forgot.
- **[edit]** `/home/user/AcreOS/server/services/outcomeVerifiers.ts:197` — ADJUDICATION RESOLVED (upgrades map 1's verify-only): after trustEvolution.ts:122-136 deletes, VERIFIED outcomeVerificationQueue rows have ZERO readers anywhere — while TWO live job lanes keep producing them: the 'outcome_verification' daily job (jobRegistry.ts:427, runScheduledJobs.ts:5081) and v5_maintenance every 15 min (v5MaintenanceJob.ts:11,:22 → processVerificationQueue). The verification pipeline becomes a closed write-loop (writer :197, self-processor :224-238, no downstream consumer of verified evidence).
  - The cutover must adjudicate the whole pipeline, not just note the queue: either retire the outcome_verification job (roster + withJobLock pair, same jobRosterCoverage parity rules as trust_evolution) and the v5Maintenance call, repoint verified outcomes at the seam's promotion-evidence path (the natural successor for trustFromEvidence's invariant), or record write-only status deliberately. Note outcomeVerifiers also emits 'outcome_verified' agentEvents that autonomyFinalMile.ts:40 counts — those stay meaningful only if the pipeline survives.
- **[edit]** `/home/user/AcreOS/server/services/founderNarrative.ts:686` — MISSED frozen-score AI-prose feeder — the monthly LETTER itself: generateMonthlyLetter (job founder_letter_monthly, critical:true, runScheduledJobs.ts:2069; routes at routes-founder-intelligence.ts:1944-1975) feeds '- codename: N decisions, trust ${a.trustScore}, trend' (:686, collected :341, typed :84) into AI-written founder prose. The maps' five-file client list and their 'server payloads' risk never named the Letter — the flagship founder door — as a frozen-score renderer.
  - Add to the same-turn repoint list: drop or re-derive the trust field in the Letter's agent lines. Same class, same commit: companyBriefingGenerator.ts:151 (daily company_briefing_generator job), aiBriefingWriter.ts:23 (input type for generateBriefingUpdates), companyAgents.ts:480 and :515-516 (status-report prose + healthScore:=trustScore alias), agentPerformanceReviews.ts:92-143 ('Trust score: N/100' review prose).
- **[edit]** `/home/user/AcreOS/server/services/autonomyHealth.ts:189` — MISSED frozen-gate consumer: lowTrust = agents.filter(a.trustScore < 40) feeds autonomy-health grading — live via runScheduledJobs.ts:2089/:2099 and routes-founder-intelligence.ts:1787/:1801, and consumed by founderNarrative (:377). Post-flip this health signal is computed from a frozen column forever.
  - Repoint to domain-autonomy levels or drop the lowTrust dimension in the same turn. Sibling minor members of the same class: agentInitiatives.ts:142 (trustScore<60 initiative filter, reached via attentionOptimizer.generateFocusCard ← ceoCommandBridge:743) and agentEvolutionEngine.ts:360 ('Low trust score' issue flag, weekly evolution job runScheduledJobs.ts:1769).
- **[edit]** `/home/user/AcreOS/server/services/agentEvolutionEngine.ts:311` — MISSED post-flip semantics trap: the evolution engine CREATES new company agents with trustScore: 40 (:311). Once updateTrustScore deletes, any agent born after the flip is permanently below the 60 initiative filter, the 40 lowTrust health line, every TRUST_THRESHOLDS band above 'always escalate', and every financialAuthorityGate minTrust — with no path to ever change.
  - Not a compile issue — a designed-in permanent floor the flip creates silently. The cutover commit should either stop seeding a score that pretends to be earnable, or the same-turn adjudication of the remaining trustScore readers makes the seed value irrelevant (preferred). Record explicitly either way.
- **[verify-only]** `/home/user/AcreOS/server/services/autopilot/trustSeam.ts:36` — WRONG-CLASSIFICATION RISK on the flip mechanics all maps prescribe: trustSeam.ts:36 STATICALLY imports isNeverPromote from agentAuthorityGate, while agentAuthorityGate today reaches trustSeam only via DYNAMIC import (:117-121). The maps' turn-13 instruction 'flip checkAuthorityInner to consume seamVerdict' implemented as a static import creates a static module cycle agentAuthorityGate ↔ trustSeam. isNeverPromote is a hoisted function declaration so runtime call-time use likely survives, but const-binding TDZ (ACTION_DOMAIN_MAP) and vitest vi.mock hoisting across a cycle are real failure modes for the repinned tests.
  - Flip implementation constraint, not a file to change per se: either keep the seam consumption behind a dynamic import in agentAuthorityGate (matching today's shadow wrapper), or extract isNeverPromote/NEVER_PROMOTE_ACTIONS into a leaf module — the latter also has to preserve the source-regex extraction in tests/helpers/neverPromoteActions.ts:32 (which reads agentAuthorityGate.ts by path and silently returns [] if the literal moves — law-3 quiet-population loss).
- **[verify-only]** `/home/user/AcreOS/client/src:1` — VERIFIED map claims (adversarial re-run, no gaps found): zero 'scp' strings anywhere in client/src (case-insensitive); zero client callers of /api/founder/intelligence/agent-trust-history, /api/admin/trust-seam-shadow, system-trends/morning-briefing path strings in tests/ or scripts/; trustDelta.ts's only production importer is trustEvolution.ts:28 (agentPerformanceReviews.ts:104 confirmed an unrelated local property); cognitiveMemoryV13 has exactly two production importers (feedbackLoopV14.ts:12 static, trustAuthorityEscalation.ts:174 dynamic) plus the stale cascadePrecedentHit mock; shared/governance/constitution.ts has zero lane-3 enforcement refs (the :157 'getTier' hit is financialAuthorityGate prose inside the spend hard-stop note); no founder-chat/AI-tool dispatch case, workflow-live-trigger, or agentReactionEngine hook names any trust action; no barrel re-exports of any dying module; no drizzle relations() on trustEvolutionLog or the V13 memory tables; eventMesh 'trust:lifecycle' has zero subscribers; MorningBriefing.tsx orphan confirmed; 'WEEK' roster const has other users (no orphan on entry removal).
  - These confirmations mean the maps' delete/lower-baseline actions are sound as far as they go — the gaps in this report are additive, not corrective, except the trustSeam cycle note and the two-entry measurement-defaults miss.

## Audit risks

- MONEY-PATH FREEZE: financialAuthorityGate (requestSpend :367, quorum selection :492-495, approver validation :561) is a live constitutional spend lane consuming raw companyAgents.trustScore, named by ZERO maps. Post-flip its trust floors run on a frozen column, and agents seeded at 40/50 after the flip are permanently ineligible — the approver pool ossifies. It was also never inside absenceGrantExpires' scanned population, so no existing pin will go red to force the adjudication.
- STORED-SCORE AUTO-EXECUTION: the trust score escapes the import graph via decisionsInboxItems.contextBundle (agentInitiativeEngine:288-292) into intelligence/triage.ts:154-173, which auto-approves initiative items at trust>=90 (executed as 'auto_approved_via_triage', autonomousDecisionExecutor:970). Pending rows at flip time carry frozen scores that still license auto-approval; post-flip rows silently kill the branch. This decision point was never shadowed by trustSeam and is invisible to symbol greps — exactly the dynamic-dispatch class the tasking warned about.
- DEAD FOUNDER DIALS: the /founder/studio generic dial engine renders the three seeded trust.* settings (settingsSeeder.ts:23-44) as live founder controls whose only reader dies at turn 13 — a founder can edit promotion gates that gate nothing. String-keyed registry dispatch; remove the catalog entries in the deletion commit.
- FABRICATED SUCCESS COPY: ceoCommandBridge.handleActivateAbsence (live via founder chat action.ts:489 and two intelligence routes) literally replies 'Trust scores boosted.' — after the flip that is an invented effect (no-fabrication hard-stop territory), and founderWellbeing keeps recommending absence activation (:234/:267/:299). Decision C's ruling must reach these two files, and founderWellbeing's static import breaks compilation if ceoAbsenceMode deletes wholesale.
- THE LETTER RENDERS THE FROZEN SCORE: founderNarrative feeds 'trust N' per agent into AI-written monthly Letter prose (:686; critical roster job), beyond the briefing-writer input the maps flagged; companyBriefingGenerator (:151), autonomyHealth lowTrust grading (:189), companyAgents healthScore alias (:515) and agentPerformanceReviews prose (:143) are further unmapped members of the frozen-render population. The maps' same-turn UI repoint list is materially incomplete on the SERVER side.
- WRITE-ONLY VERIFICATION LOOP: turn 13 leaves outcomeVerificationQueue's verified rows with zero readers while two scheduled lanes (outcome_verification daily + v5_maintenance 15-min) keep verifying — the evidence machinery that was supposed to justify trust keeps burning cycles producing evidence nothing consumes. Retire, repoint at the seam's promotion path, or record deliberately.
- STATIC-CYCLE FLIP HAZARD: implementing the turn-13 agentAuthorityGate flip as a static seamVerdict import creates a module cycle with trustSeam.ts:36's isNeverPromote import; keep it dynamic or extract isNeverPromote to a leaf module — and if the literal moves files, tests/helpers/neverPromoteActions.ts's path-bound regex silently extracts [] (its consumers' vacuity floor can still pass on the constitutional half alone).
- DOUBLE BASELINE ENTRY: check-measurement-defaults.mjs has TWO trust entries (:128 routes-scp-v2 AND :164 scpGoldenSuite); every map named only the first. Deleting checkTrustPromotions with only :128 removed turns CI red on the entry nobody listed.
- DIGEST DEAD CATEGORY: autonomyFinalMile's daily summary enumerates 'authority_promotion' agentEvents whose only writer dies at turn 13 — remove the enumeration member and its '• N trust promotions' line in the same commit, or the digest carries a forever-zero category (and any per-member vacuity discipline applied later would count it as a silent parser gap).
